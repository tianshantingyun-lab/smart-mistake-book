# 错题本学习内核

学生做过什么、以及系统据此认为他掌握了什么——前者是不可变的事实，后者是可重算的推导。
本文件只定义这套内核里那些**用错就会导致误解**的词。

## Language

### 两个源头

**学习账本（learning ledger）**：
不可变、只追加、序列号连续无空洞的学习事件记录，是唯一的事实来源。
_Avoid_: 事件表、日志、事件流

**投影（projection）**：
从账本推导出的学习者状态，不是事实来源，必须能由账本重新算出。
_Avoid_: 缓存、派生表、快照（snapshot 另有所指，见下）

### 投影怎么推进

**增量投影（incremental projection）**：
只投影检查点之后的那些事件，然后推进检查点。

**全量重放（full replay）**：
把整份账本从头重算一遍。

**前缀重放（prefix replay）**：
只重放账本的一段前缀、提交中间检查点再继续往后算。**本工程禁止这种做法。**
_Avoid_: 分块重放、分段重放

**重放可信化**：
两件事的合称——该重放的时候真的重放，且重放结果与逐条增量投影一致。

### 账本的位置标记

**检查点（checkpoint）**：
「投影已消费到账本的哪里」的三元组：最后消费的序列号、当时的投影版本、投影时刻。
_Avoid_: 游标、偏移

**投影版本（projector version）**：
投影语义的标识符。它一变，既有安装上的旧投影就必须全量重放。
_Avoid_: schema 版本、数据库版本

**向已知账本头追平（caught up）**：
检查点到达已知账本头。不表示「最新」，只表示没有已分配但未投影的事件。

**快照（snapshot）**：
某一检查点处的投影内容。它是投影的一帧，不是账本的替代品。
_Avoid_: 状态、存档

### 证据与归因

**证据（evidence）**：
关于学习者的一次观测——一次作答、一次自评、讲题里的一句话。
_Avoid_: 记录、样本、行为数据

**归因（attribution）**：
把一条证据绑到某个知识点上，附带角色、权重与确定性。
**归因在写入时解析并冻结进事件指纹，它是写入时的不可变事实。**

**真绑定（real binding）**：
题目版本与知识点之间的、经审校知识包或用户组织产生的绑定。

**伪归因（pseudo attribution）**：
没有真实绑定时，把证据落到一个人造的科目级节点上。它照常参与投影并沉淀掌握度，
但沉淀在一个学生看不见、知识库也不认的节点上。
_Avoid_: 兜底绑定、默认绑定

### 修正

**修正（correction）**：
替换某次早先作答之证据的事件。它追溯到那次作答，而那次作答在账本里排在它之前。
修正不是增量投影能接收的事件类型。

### 读取时的检查

**指纹（fingerprint）**：
对一条事件的各字段规范化后求的哈希，每次读取时重算并与存下的值比对。
_Avoid_: 校验和、签名

**冲突（conflict）**：
指纹对不上。它表示账本与投影对同一条事件的描述不一致，处置是停止投影而非继续。

### 掌握度

**知识点（knowledge node）**：
审校知识包里被掌握度追踪的最小单元。
_Avoid_: 卡片、题目、知识条目

**掌握度（mastery）**：
投影在知识点上的学习状态，取值 `[0,1]`。
_Avoid_: 熟练度、得分

**记忆强度（stability）**：
某道题的记忆状态本身。它是**持久状态**，由投影产出。
_Avoid_: 稳定度、半衰期

**可提取性（retrievability）**：
在给定时刻回忆起某个知识点的概率。它是**读取时估算**的量，不是持久状态。
_Avoid_: 记忆强度、掌握度、`library_catalog.retrievability`（同名的另一物，见下）

**没掌握（least mastered）**：
按知识点掌握度下界排序的读法。注意它**不是**按可提取性排序——
`library_catalog` 视图里那一列恒为 NULL，任何按它排序的读法都是空转。
_Avoid_: 最不熟练、按 retrievability 排序
