# 向量议题判定模板 · R3 同构评测出数后一次性填写（2026-09-22 立）

> **回答的问题**：去截断后的生产同构 B 路（词面 SQL 倒排）能不能服务金标集？能不能的话，向量议题关闭；不能的话，按预注册预案开"同层 dense 兜底"议题。
> **依据**：决策台账 D1/D12（`docs/kb-architecture-refactor-decisions-2026-09-21.md` §3 向量预注册规则全文）、架构审计 P3/R3（`.jez/artifacts/research-brief-kb-architecture-audit.md`）。
> **性质**：**判定模板 + 预注册规则存档**，不是施工文档。§3 判定表已按 §1 规则**一次性**填写（2026-09-22，判定依据 = 修复前生产路由的真 SQL 首测），填完即锁，本议题不再改判；修复后统一路由的数作为**补充证据**附于 §3 表下，不重新判定。**2026-09-22 当日（FIX2）**：去截断实验 + 统一路由两组数均实测为词面栈净伤害，生产形状回滚至 **v1 裸 B5（零回归）**——回滚记录与缺失的 v1 基线数见 §3.2，同样不改 §3 判定。
> **2026-09-23（WP-A，用户新裁定）**：运行时预算放宽、质量性能优先 → **§4 形态预案按此改写**（原稿的"总预算 ≈30MB"与"bge-small-zh int8 + LiteRT"字面形态**作废**），并新增 **§7 附录 A · 更正与外部证据**（62% 截断口径更正等）。§1 预注册规则、§3/§3.1–§3.3 判定与封存数、§6 最终判定**一字不动**（"立项仍需用户另裁"的结论不变）。
> **2026-09-23（WP-E2，Stage-1 词面检索实验判读）**：Stage-1（FTS5+bm25 四臂实验）**未过 0.75/0.60 判读线**（生产口径下臂 A 0.5333 < 基线 0.5444）⇒ **生产形状仍 = v1 截断索引 × 裸 B 路 limit=5，未改**；dense 议题维持开启、立项仍待用户另裁。现状段与时戳见 §5 末"§3.2 现状同步"；全阶段证据见 `docs/kb-stage1-report-2026-09-22.md`。

---

## 1. 预注册规则（全文，自决策台账 §3 逐字存档）

**裁定状态**：RAG 结构性不需要、向量本轮不上（D1）。以下规则为**预注册**——阈值与判分对象现在就定死，R3 同构评测跑完后按本规则判读，不允许事后调阈值、事后换判分对象。

1. **金标集**：80–100 条，由 agent 语义切片 + 机械验证产生，**一次性冻结**（现值 90 条，`tools/kb_coverage/tables/golden_queries_v1.json`，sha256 封存）。冻结后不增、不减、不改（"不许为评测集打补丁"的 R3 纪律；提升指标只能改检索器/索引/内容）。
2. **判分对象**：**去截断后生产同构 B 路**——生产 SQL 倒排召回路径（`knowledge_search_feature`）在取消别名索引有损截断（`MAX_SEARCH_FRAGMENTS=16` / `MAX_NODE_FEATURES=192`，审计 §1.4：62% 节点别名被截断）之后的同构评测（同一代码路径、Room in-memory），按 R3 同构评测的指标 B 路 Recall@5（审计 R3 验收中"从未存在过"的那个数）判分。
3. **预注册阈值**：**主集 ≥ 0.90 且逐章 ≥ 0.80**（两条同时满足）。
4. **通过**：词面路径（25k 别名 + 册·章·节门控）足以服务，向量议题**关闭**，不上。
5. **不通过**：开**同层 dense 兜底议题**（只开议题、不直接施工）。预注册的形态预案见 §4。
6. **本轮不施工**：向量只是预注册的决策门，不在 D4 的施工范围（R2+R1+R3+R4a+R5）内；议题开启后的立项需另一次用户裁定。

### 1.1 判分口径的落地说明（施工时钉住的实现细节，判定引用时以此为准）

- **真 SQL 数（判定主依据）**：`GoldenRetrievalInstrumentedTest`（`:core:data` androidTest，档 2 跑）——真实 `BundledKnowledgeBaseInstaller.install` 装包 + 当时施工形态的索引与路由判分。
  - 施工期（FIX 轮，已封存于 §3/§3.1）：去截断 v2 索引（`INDEX_VERSION=2`）+ 生产统一路由（limit=512 宽召回 → `KnowledgeContextRetriever.select` limit=5）。
  - **现行形态（§3.2 形状回滚后，2026-09-22 FIX2 起）**：v1 截断索引（`INDEX_VERSION=1`）+ **v1 生产形状 = 裸 B 路 limit=5**（`readSubjectKnowledgeRecallCandidates` 直接取前 5，无 A 路精排）——与回滚后的 KNOWLEDGE_READ 实跑同构。
  - **路由口径差异（必读）**：首测（真 SQL）是在**修复前生产路由 = 裸 B 路 limit=5**（store 的 `parents + matched` 返回形态直接取前 5，无 A 路精排）上判分的；其数（主集 0.5222 / MRR 0.1402）保留为**『裸 B5 路由基线』**（§3 判定即基于它）。生产路由改为统一两段式后，判分口径只跟随生产路由（召回修复任务裁定）；两个数**不可直接比较**——召回网宽度（5 vs 512）、有无精排、top5 预算在 topic 父节点与原子节点间的分摊方式都不同。
  - 实现注记：任务书写的"Room in-memory"在 `:core:data` 不可达（`openInMemory` 是 `:core:database` 的 internal，而金标集/安装器在 `:core:data`）；本测试沿用参照测试 `KnowledgeContextRetrievalInstrumentedTest` 的具名文件库 + 跑完即删。**同一代码路径**（同一 store 方法、同一 SQL、同一索引构建、同一 select）不受影响。
- **JVM 镜像数（交叉核对，不作判定依据）**：`GoldenRetrievalJvmTest` + `RetrievalBenchmark.BRouteMirrorIndex`（档 1 跑）——`BRouteMirrorIndex.recall` 是 B 路 SQL 排序的逐条 JVM 镜像（含 store 的 `parents + matched` 返回形态），索引内容跟随 `KnowledgeSearchFeatureExtractor.fromNode` 的**当前规则**。
  - 施工期（FIX 轮，已封存）：镜像升级为 B512→A 统一路由镜像（limit=512 → select limit=5）；
  - **现行形态（§3.2 回滚后）**：镜像 = **v1 裸 B5**（limit=5 裸排序 top-5，无 select），与回滚后的真 SQL 判分目标同构；B512→A 口径退役封存（其数在 §3.1）。
  - 产出 `build/golden-jvm-metrics.txt`（主集/逐章/逐题，A 路精排（参考上界）与 B 镜像两套数）。JVM 镜像与真 SQL 的差值 = 口径漂移，应当可见且小（历史锚点：统一路由 31/90 = 31/90、旧裸 B5 47/90 = 47/90，均漂移 0；裸排序口径下排序键确定、镜像保真不依赖 parents 行序）；若差值大，先查镜像保真度再引用。
- **A 路数（参考）**：`KnowledgeContextRetriever.select` 于按科隔离的原子节点全集（与 `FourSubjectRetrievalBenchmarkTest` 守门同一取法，D-04 口径不动）。A 路不进判分对象，只回答"精排能捞回多少"。
- **逐题稳定性口径**：仪表化逐题 5 样本；判定表里的 Recall@5 取"全样本命中"口径（最严），同时记录"任一样本命中"口径备查。
- **金标集完整性**：两份测试各自在运行时复核 sha256（JVM 对仓库封存、仪表化对 assets 副本封存），题面被改过就红，不进判定。

## 2. 出数与复现（收尾代理填表前跑一遍）

| 档 | 命令 | 产出 |
|---|---|---|
| 1 JVM | `./gradlew :core:data:testDebugUnitTest --tests "*GoldenRetrievalJvmTest*" --rerun` | `build/golden-jvm-metrics.txt`（A/B 两套数 + 索引规模） |
| 2 真 SQL | `./gradlew :core:data:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=...GoldenRetrievalInstrumentedTest` | 仪表化日志：主集/逐章 Recall@5、MRR、p95、MISS 逐题清单 |
| 2 索引重建 | `./gradlew :core:database:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=...KnowledgeSearchIndexStateMigrationInstrumentedTest` | v50→51 迁移 + 旧版本特征行整科重建 + 锚点推进（回归钉住） |

金标集本身：`tools/kb_coverage/tables/golden_queries_v1.json` + `.sha256`（冻结，禁改）。

## 3. 判定表（已填——2026-09-22 按 §1 规则一次性填写，填完即锁）

**判定对象**：修复前生产路由（真 SQL，裸 B 路 limit=5，§1.1 第一项的『裸 B5 路由基线』口径）。**判据**：主集 Recall@5 ≥ 0.90 **且** 逐章 Recall@5 全部 ≥ 0.80。

| 项 | 值 | 来源（日志行/文件行号） |
|---|---|---|
| 金标集条数 / sha256 | 90 条 / `7c004b763bdd49556e11ff1c9500c9461b09a7383b77f230fa8fd35754e6ae39` | `tools/kb_coverage/tables/golden_queries_v1.json(.sha256)`；两测运行时复核 |
| 真 SQL B 路 主集 Recall@5（全样本命中） | **0.5222（47/90）** | 档 2 首测（修复前生产路由）仪表化日志（记录于召回修复任务书"已核实事实"） |
| 真 SQL B 路 主集 Recall@5（任一样本命中，备查） | 首测日志未留存该口径（本会话不可复核） | — |
| 真 SQL B 路 MRR | **0.1402** | 同上 |
| 逐章 Recall@5（最低章 → 值） | 0.3333（生物学必修2·第一章第二节 / 化学必修第一册·第三章 / 数学必修第一册·第三章 / 物理必修第一册·第三章，四章并列最低） | 首测日志；同口径 JVM 镜像逐章值见 `core/data/scratch/diag-freefall/golden-jvm-metrics-prefix-route-baseline.txt`（本会话存档副本，gitignored） |
| 逐章 ≥0.80 的章数 / 总章数 | **1/10**（仅 化学必修第二册·第六章 = 1.0） | 同上 |
| p95（真 SQL，ms / CI 系数） | 首测日志未留存（本会话不可复核）；新路由档 2 复跑仍受同一 150ms×CI 门 | — |
| JVM 镜像 B 路 主集 Recall@5（交叉核对） | 0.5222（47/90，旧路由镜像） | 修复前 `build/golden-jvm-metrics.txt`（存档副本同上） |
| JVM↔真 SQL 差值（镜像漂移） | **0 个百分点**（47/90 = 47/90，锚点成立） | 上行两条对照 |
| **判定**：□ 通过（向量议题关闭，不上） ☒ **不通过（开同层 dense 兜底议题）** | 主集 0.5222 < 0.90 且 9/10 章 < 0.80，两判据均不满足 | 规则 §1-3；议题已于首测后开启（§4 形态预案，立项需另一次用户裁定） |

不通过时的 MISS 分布（逐章/逐题，附仪表化日志的 MISS 清单）：

- 逐章（旧路由，1 − 上表 Recall@5）：生物学必修2 0.6667、化学必修1 0.6667、数学必修1 0.6667、物理必修1·第三章 0.6667、数学选修2 0.6667、物理选修3 0.3333、数学选修1 0.2222、化学必修2·第五章 0.3333、物理必修1·第一章 0.5556、化学必修2·第六章 0。
- 逐题明细：首测仪表化 MISS 清单在上轮会话记录（本会话不可复核）；同口径逐题 hit/miss 见存档副本 `core/data/scratch/diag-freefall/golden-jvm-metrics-prefix-route-baseline.txt` 的"逐题"段（镜像与真 SQL 漂移 0，可作逐题分布参考）。
- MISS 的共同形态：题面是**自然语言问句**（"为什么…""…到底是什么"），预期节点名是**词条式**（如"自由落体运动""合力范围"）——公共 2/3-gram 少 → 二值 TF count 低（自由落体例：count=1，裸 B 路 rank 499/546）；公共 gram 多的大题节点（"运动的描述""相互作用"）挤占 top5。即登记册 D-01（无 IDF/无长度归一化）+ 题面-节点名式差（natural question vs terminology）的叠加。

### 3.1 补充证据：统一路由（B512→select→top5，2026-09-22 召回修复后）

**性质**：修复后生产路由的补充测量，**不改变 §3 判定**（议题已开、阈值未动、不自动关、不重判）。

| 项 | 值 | 来源 |
|---|---|---|
| JVM 镜像（B512→select→top5）主集 Recall@5 | **0.3444（31/90）** | `build/golden-jvm-metrics.txt`（`B-route-mirror+A-select` 段，2026-09-22 修复后重跑） |
| 同上 MRR | 0.1315 | 同上 |
| 同上逐章 | 0.1111（生物学必修2）～0.7778（物理必修1·第一章），10 章全部 < 0.80 | 同上 |
| 真 SQL（同一判分目标，档 2 复跑） | **0.3444（31/90）**；MRR 0.1315；逐章 0.1111（生物学必修2）～0.7778（物理必修1·第一章），10 章全部 < 0.80；"任一样本命中"口径与"全样本命中"口径相同（0.3444——5 样本全命中/全 miss，无抖动样本） | 档 2 复测#1 与复测#2（设备空闲窗）两次出数完全一致；仪表化日志 `route=B512->A-select->top5(生产统一路由) indexVersion=2 cases=90 samplesPerQuery=5` |
| 真 SQL ↔ JVM 镜像差值（新路由） | **0 个百分点**（31/90 = 31/90；MRR 0.1315 相同；逐章 10 章逐项相同）——§5 的 >5pp 镜像保真度红线未触发 | 复测#2 仪表化日志 vs `build/golden-jvm-metrics.txt`（B-route-mirror+A-select 段，2026-09-22 修复后） |
| 金标集 sha256 封存复核（复跑时） | `7c004b763bdd49556e11ff1c9500c9461b09a7383b77f230fa8fd35754e6ae39`（与仓库根封存一致，运行时断言通过） | 复测#2 仪表化日志 `golden-assets sha256=...` |
| p95 门（150ms×CI，本地系数 1） | **两次复跑均断言失败**：复测#1 p95=663ms / p50=457ms / max=40901 / overBudget=444/450；复测#2 p95=695ms / p50=467ms / max=35253 / overBudget=446/450（n=450） | 失败消息 `real-SQL recall p95 was Xms (budget 150ms)`（`GoldenRetrievalInstrumentedTest` L153-157 断言）。**定性**：同设备同会话的 `KnowledgeContextRecallInstrumentedTest` 控制测（同形路由 512 宽召回+select，20k 合成点）p95=62ms 通过、19/19 命中——设备有履行能力，整分布平移（p50≈460ms，非单点毛刺）来自真实 v2 去截断索引规模（`totalFeatureRows=873465`、`maxFeaturesPerNode=1672`，`build/golden-jvm-metrics.txt` 头）；每次复跑恰有一个 ~35-41s 的最大样本（max=40901/35253），未归因（疑似整科索引重建/一次性设备停顿），不影响 p95 定性 |
| 19 例回归（真机，档 2 复跑） | **19/19 命中、cross-subject=0**（`bundledSubjectsRecallExpectedKnowledgeWithoutCrossSubjectCandidates` PASS，422s；同套 p95 门 62ms 通过） | 2026-09-22 档 2 复跑仪表化日志 `bundled-knowledge-recall regression: hit=19/19, cross-subject=0` |
| 相对裸 B5 基线的逐题变化 | 得 7 / 失 23（47→31） | 本次重跑 vs 存档副本逐题对照；复测#2 真 SQL 逐题 MISS 清单与 JVM 镜像逐题（B 列）一致，漂移 0 |

**解读**（事实分开写）：
- 统一路由**修好了**"少公共 gram 的精确节点进不了召回池"这一失败类（自由落体例：裸 B rank 499 → 进 B512 池 → select 精排后 top64 第 8；19 例回归 18/19→19/19 预测——**真机档 2 复跑已实测 19/19、cross-subject=0**，预测兑现）。
- 统一路由的**金标 top5 主集数反降**（0.5222→0.3444，镜像口径——**真 SQL 档 2 两次复跑均 0.3444（31/90），与镜像漂移 0，实锤**）：机制是 select 的 limit 预算在"原子节点 + 其 topic 父节点"间分摊（`KnowledgeContextRetriever.select` 的既有语义，归类/拍照两路同形，未引入新参数），top5 里 2~3 席被 topic 占去；叠加 A 分偏大题节点。失 23 的题多为"裸 B 的 top5 恰好有答案、精排把它挤出去"的题面。
- 两条路由的数都远低于预注册 0.90——**词面路径服务自然语言题面的不足是路由无关的**（裸 B5 与 B512+A 都不过线），这是 dense 兜底议题（§4）的补充证据，不是统一路由的回归。
- **p95 门事实（2026-09-22 档 2 复跑新增）**：统一路由在真实 v2 索引规模（873,465 特征行）下，test_device 上 p95=663/695ms（两次复跑）、p50≈460ms，444-446/450 样本超 150ms 本地预算——`GoldenRetrievalInstrumentedTest` 的 p95 断言红。同设备同形路由的合成 20k 点控制测 p95=62ms 通过，故定性为**内容规模成本**（裸 B5 首测时该门通过，limit=5 无精排），非设备故障、非镜像问题。处置未定（预算是否该随路由形态与索引规模重定标）——本模板只记录事实，不改 §3 判定、不调预注册阈值；档 2 该项按 failed_code 上报。

### 3.2 形状回滚记录：生产形状回 v1 裸 B5（2026-09-22 当日，FIX2）——三组数并列

**性质**：**不是改判**。§3 判定（不通过、开 dense 兜底议题）由首测触发、已锁、规则冻结——
形状回滚不改变该结论。本节记录：两组实验数为何判为词面栈净伤害（逐字引档 2 报告）、
回滚动作、以及**缺失的 v1 基线数**（本轮补测）。

**实测事实（档 2 报告，逐字引用）**：

- **v2 索引（去截断）× 裸 B5 首测**：金标主集 **0.5222（47/90）**、MRR 0.1402、
  p95=78ms（预算 150ms 内，档 2 首测运行记录）；但 **19 例回归的自由落体例在 v2 窄路
  失败**——去截断放大公共 gram 通胀（大别名量节点的二值 TF count 被公共 2/3-gram 抬高），
  打掉了窄路召回（少公共 gram 的精确节点被挤出 64 宽网）。
- **v2 索引 × B512→A（统一路由）**：19 例恢复 **19/19**（档 2 复跑仪表化日志
  `bundled-knowledge-recall regression: hit=19/19, cross-subject=0`），但金标主集
  **0.3444（31/90）**（比裸 B5 更差）、**p95=663ms / 695ms**（两次复跑，超 150ms 预算
  4.4×）——同设备同形路由的 2 万合成点控制测 p95=62ms（成本来自宽召回 + A 对 512 候选的
  Kotlin 计数打分，非设备故障）。仪表化日志行（复测#1/#2）：
  `route=B512->A-select->top5(生产统一路由) indexVersion=2 cases=90 samplesPerQuery=5`；
  v2 索引规模 `totalFeatureRows=873465 maxFeaturesPerNode=1672`。
- **结论**：去截断与 B512→A 统一路由**均被实测为词面栈净伤害**（词面栈两条路都验证过），
  生产回零回归形状。

**回滚动作（FIX2，测量设施全保留）**：

1. 索引回 v1 语义：`KnowledgeSearchFeatureExtractor` 恢复节点侧截断
   （`MAX_SEARCH_FRAGMENTS=16` / `MAX_NODE_FEATURES=192`，WP4 前原值）+
   `INDEX_VERSION` 2→1——靠 WP4 版本锚点表 `knowledge_search_index_state`（v51 迁移，
   保留基建）的"锚点缺失或不等 ⇒ 整科换血"自动触发：v2 实验库（锚点=2）首次召回即回建
   v1；Room 51 迁移本身不滚。
2. 路由回滚：KNOWLEDGE_READ（裸 B 路 limit=5）与 MASTERY_READ 聚焦解析（裸 B 路
   limit=24）回到 FIX 前生产形状；归类/拍照路径的 B512→A 不动（既有、消费者不同）。
3. 19 例回归 limit 512→64 回原形状（断言一字不改）。
4. 金标判分目标回 v1 裸 B5：`GoldenRetrievalInstrumentedTest`（真 SQL）与
   `BRouteMirrorIndex`（JVM 镜像）同步，零漂移纪律不变；金标冻结集（sha256
   `7c004b76…`）与预注册阈值（主集 ≥0.90 且逐章 ≥0.80）一字不动。

**三组数并列**（同一 90 条金标 sha `7c004b76…`、同一 p95 门 150ms×CI）：

| # | 索引 | 路由 | 金标主集 Recall@5 | MRR | p95（真 SQL） | 19 例回归 |
|---|---|---|---|---|---|---|
| 1 首测（档 2） | v2 去截断（873,465 特征行） | 裸 B5 | **0.5222（47/90）** | 0.1402 | 78ms（预算内） | v2 窄路失败（自由落体例） |
| 2 实验（档 2） | v2 去截断 | B512→A | **0.3444（31/90）** | 0.1315 | **663ms / 695ms**（超预算 4.4×） | 19/19 |
| 3 生产形状（本轮） | v1 截断（583,271 特征行） | 裸 B5 | **真 SQL 0.5444（49/90）= JVM 镜像 0.5444（49/90），零漂移** | 真 SQL 0.1511 / 镜像 0.1517 | **68ms**（预算 150ms 内；p50=18ms，overBudget 3/450） | **19/19**（v1 实测，档 2） |

第 3 组逐章 Recall@5（真 SQL，本轮档 2）：生物学必修2·第一章第二节 0.7778、化学必修第一册·第三章 0.2222、
化学必修第二册·第五章 0.7778、化学必修第二册·第六章 1.0、数学必修第一册·第三章 0.4444、
数学选择性必修第一册·第二三章 0.7778、数学选择性必修第二册·第四章 0.3333、物理必修第一册·第一章 0.2222、
物理必修第一册·第三章 0.2222、物理必修第三册·第十一十二章 0.6667（10 章，≥0.80 的仅 1 章——
仍远低于预注册 0.90/0.80，判定不变）。仪表化日志行（逐字）：
`route=B-bare->top5(v1生产形状) indexVersion=1 cases=90 samplesPerQuery=5 p95=68ms p50=18ms max=7584 budget=150ms overBudgetSamples=3`、
`golden-assets sha256=7c004b763bdd49556e11ff1c9500c9461b09a7383b77f230fa8fd35754e6ae39`、
`主集 Recall@5(全样本命中) = 0.5444444444444444 (49/90)`、`MRR(最优名次) = 0.15111111111111114`。

**p95 波动注记（如实）**：同设备同形状档 2 的有效跑里 p95 为 180ms（红，max=25,641ms 单次停顿）、
159ms（红，max=25,467ms 单次停顿）、68ms（绿，max=7,584ms）、**143ms（绿，max=16,616ms 单次停顿，
§3.3 复验轮三）**——每次恰有一个 ~7.5–25.6s 的单样本停顿（4 轮档 2 报告过的 ~35–41s 停顿同族，
未归因，疑似设备/宿主一次性停顿），p50 恒为 18–20ms（路由本身远快于预算）。p95 门是 150ms×CI 的
墙钟门（KD-2 族），设备空闲窗内可绿；CI 上按 4× 系数跑。

**判分口径注记（回滚施工中发现并修正）**：裸 B5 的判分必须取 store `parents + matched` 返回形态的
**前 5**（§1.1 首测钉定口径）——matched 的父 topic 会前置占席，expected 原子节点可在 matched 前 5
而返回形态第 6–8 位。回滚初版漏了 take(5)（对全返回判分，虚高到 58/90），用真库探针
（DIAG scratch 测试，已删）逐题钉死后修正；修正后真 SQL 49/90 与 JVM 镜像 49/90 零漂移——
**41 例 MISS 逐题同一批题面（集合对称差 = 0，已复算）**，§5 的 >5pp 保真度红线未触发。

**判定不变 + 缺陷归宿**：

- **D12 预注册规则已被首测触发**（0.5222 < 0.90 且 9/10 章 < 0.80）→ 同层 dense 兜底
  议题已开；**形状回滚不改变该结论**——三组数全部低于 0.90（v1 生产形状 0.5444 亦仅
  1/10 章 ≥0.80），词面栈服务自然语言题面的不足是形状无关的。
- **v1 在裸 B5 同路由上比 v2 还高 2.2pp**（0.5444 vs 0.5222）：去截断连"原路由"都
  没救回来——公共 gram 通胀在窄路由同样压低主集；加上宽路由的 0.3444/663ms 与 19 例
  自由落体，v2 三个形状全部净伤害，回滚无争议。
- **dense 兜底结构性消灭别名截断缺陷**（KD-24）：28,932 条向量（3,572 知识点 + 25,360
  别名，审计 §2.4-1 口径）×512 维全扫，向量侧**无截断损失**——"别名只有一部分进了索引"
  这一失败类在 dense 兜底里不存在；词面侧不再以去截断方式修（实测净伤害），词面索引的
  截断不再承载召回正确性，只承载 p95 与体积预算。
- **生产形状 = v1 裸 B5（零回归）**：19 例回归 19/19、20 条守门、本地档 1 自证全绿；
  金标 v1 基线真 SQL 数用于镜像零漂移交叉核对与 dense 议题立项时的基线对照。

### 3.3 复验轮三真 SQL 重测（2026-09-22，设备 test_device/emulator-5554，v1 生产形状）

**性质**：§3.2 第 3 组数（v1 裸 B5 基线）的**复验**——FIX2 形状回滚后全部旧轮结果作废，
本轮档 2 逐项重跑、不复用旧数。真 SQL 判分目标未变（v1 截断索引 × 裸 B5，金标冻结集 sha
`7c004b76…` 运行时复核通过），预注册阈值一字未动，**判定不变**（仍不通过、dense 议题保持开启）。

- **复验命令**：`./gradlew.bat :core:data:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.tingyun.smartmistakebook.core.data.knowledge.GoldenRetrievalInstrumentedTest`（先 `:app:assembleLocalFirstDebug` 安装；XML `tests=1 failures=0 errors=0 time=331.4s`，2026-09-22T04:58:46Z）。
- **仪表化日志（逐字，logcat System.out 04:58:48）**：
  - `route=B-bare->top5(v1生产形状) indexVersion=1 cases=90 samplesPerQuery=5 p95=143ms p50=20ms max=16616 budget=150ms overBudgetSamples=19`
  - `主集 Recall@5(全样本命中) = 0.5444444444444444 (49/90)`
  - `主集 Recall@5(任一样本命中) = 0.5444444444444444`（全命中/全 miss，无抖动样本）
  - `MRR(最优名次) = 0.15111111111111114`
  - `golden-assets sha256=7c004b763bdd49556e11ff1c9500c9461b09a7383b77f230fa8fd35754e6ae39`
- **逐章 Recall@5（全样本命中，9 例/章）**：生物学必修2·第一章第二节 0.7778（7/9）、化学必修第一册·第三章 0.2222（2/9）、化学必修第二册·第五章 0.7778（7/9）、化学必修第二册·第六章 1.0（9/9）、数学必修第一册·第三章 0.4444（4/9）、数学选择性必修第一册·第二三章 0.7778（7/9）、数学选择性必修第二册·第四章 0.3333（3/9）、物理必修第一册·第一章 0.2222（2/9）、物理必修第一册·第三章 0.2222（2/9）、物理必修第三册·第十一十二章 0.6667（6/9）——≥0.80 的仍仅 1 章。
- **MISS 41 例**（逐题清单见本轮仪表化日志；逐章分布 2/7/2/0/5/2/6/7/7/3，与 §3.2 第 3 组同批题面形态）。
- **与 §3.2 第 3 组（上轮封存数）对照**：主集 49/90 = 49/90、MRR 0.1511 = 0.1511、逐章 10 值逐项相同——**复测零漂移**（JVM 镜像上轮封存 0.5444/0.1517 本轮未重跑，真 SQL↔镜像对照以封存数为准）。
- **p95 门**：本轮 **143ms < 150ms，断言通过**（p50=20ms；max=16,616ms 单样本停顿，overBudget 19/450，与 §3.2 波动注记同族，已并入该注记）。

**三组数并列**（§3.2 表第 3 行即本组生产形状；本轮复验实测值与该行一致，故三组数仍为：
① v2 去截断 × 裸 B5 = 0.5222 / 0.1402 / p95 78ms；② v2 去截断 × B512→A = 0.3444 / 0.1315 / p95 663–695ms；
③ **v1 截断 × 裸 B5（生产）= 0.5444 / 0.1511 / p95 143ms（本轮）——19 例回归 19/19**）。

## 4. 形态预案（仅"不通过"时启用；只开议题、不施工）

> **改写说明（2026-09-23）**：本节原稿写的是"bge-small-zh int8 + LiteRT，总预算 ≈30MB / 融合 RRF"。用户 2026-09-23 裁定：**运行时预算放宽、质量性能优先**——原稿的**预算字面与模型字面作废**（原稿留在 git 历史；§6.2 点 3 的历史记录按"冻结条目不改写"保留，其结论"立项需另一次用户裁定"仍有效）。本节为现行形态预案。

- **档位（用户裁：直接上大档）**：模型按**质量优先**选型，量级 = **Qwen3-Embedding-0.6B**（C-MTEB 66.33，外部取证口径见 §7/附录 A.5）。不以 16MB/30MB 体积为第一约束。
- **运行时**：可选 **ORT（arm64 24.63MiB）** 或 **LiteRT（5.25MiB）**，两者**均实测 16KB 页对齐合规**（附录 A.5）。选型（质量/延迟/依赖面）在立项时定，本节不锁死。
- **定位**：仅同层（册·章·节门控已把候选空间压小）dense 兜底同义表述——**词面主、dense 补**的混合形态；不做通用 RAG。
- **融合**：**归一化分数融合**（两路分数各自归一到可比区间后加权）；**RRF 降为对照臂**。依据：OpenSearch 官方混合检索资料在 **6/6 BEIR** 数据集上的对照——RRF 均低于分数归一化，平均 **−3.86% NDCG@10**（附录 A.5，单一来源待验证）。
- **索引**：**无 ANN 保持**——28,932 条（3,572 知识点 + 25,360 别名，审计 §2.4-1 口径）×512 维**暴力余弦 12–23ms**（附录 A.2）；该规模自建 cosine 零依赖。
- **sqlite-vec**：**维持不引入**——实测 `vec0.so` 的 `p_align=0x1000`，不满足 Android 16KB 页合规（附录 A.5）。
- **立项**：议题开启后的实施需**另一次用户裁定**——质量收益/体积增量/端侧可部署性与延迟都要真机数支撑（含 APK 增量实测，审计 §9 未验证项 9/10 仍在）。

## 5. 本轮不施工（声明）

向量不在 D4 施工范围（R2+R1+R3+R4a+R5）。本轮交付的是**判定基础设施**：版本锚点重建（R3-2）、金标集双路测量台（R3-1）、本模板（R3 验收"决策门不架空"）。模板填完之前，"上不上向量"没有任何可引用的数字；填完之后，答案是规则给的，不是感觉给的。

**§3.2 回滚后现状（2026-09-22 FIX2）**：施工期一并上的"去截断索引"（v2）已随形状回滚退回 v1 截断规则——它是 R3-2 的**实验臂**，不是交付形态；保留下来的是它的测量与重建基建：`knowledge_search_index_state` 版本锚点（规则一变即整科换血，含回滚方向）、金标集双路测量台、本模板。生产形状 = v1 裸 B5（零回归）。

**§3.2 现状同步（2026-09-23，WP-E2，Stage-1 词面检索实验判读后）**：**生产形状仍 = v1 截断索引 × 裸 B 路 limit=5（未改，零回归）**——本日 Stage-1 把排序引擎换成 **SQLite FTS5 + `bm25()`**（四臂 + 对照臂，判读线 0.75/0.60 出数前写死）后**未过线**（chosenArm = 臂 A：主集 0.6556 为 **matched-only 诊断口径**；**生产口径（`parents + matched` 取前 5）下臂 A = 0.5333 < 基线 0.5444**，逐章最小 0.2222 < 0.60），故**生产检索代码一字未改**、生产形状与 §3.2/§3.3 记录的 v1 裸 B5 完全相同（真 SQL 主集 0.5444 / MRR 0.1511 / 逐章 ≥0.80 仅 1/10 章）。**判定不重开**：dense 兜底议题维持开启（§3/§6 不变），立项仍待用户另裁（§4/§6.2）；本轮新增的待裁项与完整证据见 `docs/kb-stage1-report-2026-09-22.md` 与 `docs/kb-lexical-stage1-experiments.md` §7（含"是否改返回形态 D1"——FTS5 的表观增益只在 matched 优先的返回形态下出现，且该形态要改既有消费者语义）。

**纪律**：不允许事后调阈值、不允许换判分对象、不允许为金标集打补丁；提升指标只能改检索器/索引/内容（R3-4）。若档 2 出数时真 SQL 与 JVM 镜像差值异常（>5 个百分点），先修镜像保真度、重出数，再判定——差值本身是评测缺陷，不是词面路径的成绩。

## 6. 最终判定（收尾定稿 · 2026-09-22，三组数并列——一次性，不再改判）

**判定依据**：§1 预注册规则（冻结）+ §3 判定表（首测，已锁）+ §3.1/§3.2 封存实验数 + §3.3 本轮档 2 复验（v1 生产形状真 SQL）。本节是本议题的收尾定稿：不重判、不调阈值、不换判分对象；§3 判定表与 §3.3 维持原样，本节只做三组数并列与结论收口。

### 6.1 三组数并列（同一 90 条金标，sha256 `7c004b763bdd49556e11ff1c9500c9461b09a7383b77f230fa8fd35754e6ae39`，p95 门 150ms×CI）

| # | 索引 × 路由 | 金标主集 Recall@5 | MRR | p95（真 SQL） | 19 例回归 | 性质与归宿 |
|---|---|---|---|---|---|---|
| 1 首测（档 2） | v2 去截断（873,465 特征行）× 裸 B5 | **0.5222（47/90）** | 0.1402 | 78ms（预算内） | v2 窄路失败（自由落体例） | **D12 预注册规则触发依据**：0.5222 < 0.90 且 9/10 章 < 0.80 → 同层 dense 兜底议题开启（§3 判定表，一次性填写即锁） |
| 2 FIX 实验（档 2） | v2 去截断 × B512→A | **0.3444（31/90）** | 0.1315 | **663ms**（复测#1；复测#2 695ms，超 150ms 预算 4.4×） | 19/19 | 词面栈两形状均实测净伤害，已回滚（§3.2，KD-24） |
| 3 本轮新测（档 2 复验轮三 = 当前生产形状） | v1 截断（583,271 特征行）× 裸 B5 | **0.5444（49/90）**（全样本 = 任一样本） | 0.1511 | **143ms**（p50=20ms，max=16616ms，overBudget 19/450） | **19/19** | 零回归生产形状；逐章 10 值 0.7778/0.2222/0.7778/1.0/0.4444/0.7778/0.3333/0.2222/0.2222/0.6667（≥0.80 仅 1 章，判定不变） |

第 3 组本轮档 2 仪表化日志行（逐字，2026-09-22，test_device/emulator-5554，logcat System.out 04:58:48，已存档 §3.3）：
`route=B-bare->top5(v1生产形状) indexVersion=1 cases=90 samplesPerQuery=5 p95=143ms p50=20ms max=16616 budget=150ms overBudgetSamples=19`、
`主集 Recall@5(全样本命中) = 0.5444444444444444 (49/90)`、`主集 Recall@5(任一样本命中) = 0.5444444444444444`、
`MRR(最优名次) = 0.15111111111111114`、
`golden-assets sha256=7c004b763bdd49556e11ff1c9500c9461b09a7383b77f230fa8fd35754e6ae39`（运行时复核通过）。

（§3.2 三组数表第 3 行封存的 p95=68ms 为本轮之前的同设备同形状有效跑；本轮复验 143ms 同族——质量数 49/90、MRR 0.1511、逐章 10 值与封存数**零漂移**，p95 差异为单样本停顿族，见 §3.2 波动注记。）

### 6.2 判定结论（最终，四条）

1. **议题开启结论不变。** D12 预注册规则已被**首测**触发（第 1 组：0.5222 < 0.90 且 9/10 章 < 0.80），同层 dense 兜底议题于首测后开启；规则随 §3 判定表一次性填写即冻结——**形状回滚（第 1/2 组实验数）与 v1 基线（第 3 组 0.5444，仍仅 1/10 章 ≥0.80）不重开预注册判定**：三组数全部低于主集 0.90，不存在改判"通过"的路径；词面栈服务自然语言题面的不足是形状无关的（裸 B5、B512→A、v1/v2 四种组合全部实测）。
2. **dense 兜底结构性消灭别名截断缺陷（KD-24）**：28,932 条向量（3,572 知识点 + 25,360 别名，审计 §2.4-1 口径）×512 维全扫（1.4–2.8ms，内存带宽估算），向量侧**无截断损失**——"别名只有一部分进了索引"这一失败类在 dense 兜底里不存在；词面索引的截断不再承载召回正确性，只承载 p95 与体积预算。词面侧不再以去截断方式修（第 1/2 组实测净伤害，登记 KD-24）。
3. **立项需用户另裁**：形态预案（bge-small-zh int8 + LiteRT ≈30MB / RRF / 无 ANN / sqlite-vec 维持不引入，§4）只开议题、本轮不施工；立项时预算/体积/质量收益需真机数支撑（含模型端侧可部署性与延迟实测，审计 §9 未验证项 9/10）。
4. **生产形状记录为 v1 裸 B5（零回归）+ KD 登记号 KD-24**（`docs/known-defects.md`，open）：v1 截断索引（`MAX_SEARCH_FRAGMENTS=16` / `MAX_NODE_FEATURES=192`）× 裸 B 路 limit=5，19 例回归 19/19、本地档 1 自证全绿；别名截断缺陷（62% 节点，审计 §1.4）登记 KD-24，归宿为已开启的 dense 兜底议题。
   （本节按"冻结条目不改写"保留原字面：点 3 的形态字面已由 §4 改写取代，点 4 的"62% 节点"口径见 **§7 附录 A.1** 更正。）

## 7. 附录 A · 更正与外部证据（2026-09-23 立）

**性质**：**更正与取证附录**——只更正口径、只记录证据来源与置信，不改任何判定（§3/§6 一字不动）、不改 §4 的裁定内容。

### A.1 62% 截断口径更正（本次会话复算）

- **原文口径**：审计 §1.4「实测 **62%** 的节点（2,209/3,572）片段数超 16，即大多数节点的别名只有一部分在生产索引里」；同口径被引用于 §3.2、§6.2 点 4、`KnowledgeSearchFeatureExtractor.kt` 文档注释与 KD-24。
- **复算（2026-09-23，本会话，命令与结果可复跑）**：对成品包 `core/data/src/main/resources/knowledge/moe-2025-four-subjects-v1.json` 的 **3,572** 个原子知识点，按 `KnowledgeSearchFeatureExtractor.fromNode`（`core/database/src/main/kotlin/.../KnowledgeSearchFeatureExtractor.kt:44-57`）的实际切片规则复算——texts = `canonicalName` + `aliases`（排序）+ `boundaryMarkdown`；片段 = 小写后按 `[^\p{L}\p{N}]+` 切分；判据 = 片段数 > `MAX_SEARCH_FRAGMENTS`(16)。`boundaryMarkdown` 的取值链已核对（`BundledKnowledgePackResources.kt:458`：`boundaryMarkdown = point.boundary`）。
  - **纯名称 + 别名超 16 片段：661 / 3,572 = 18.51%**
  - **含 boundary 片段超 16 片段：2,208 / 3,572 = 61.81%**（审计记 2,209，差 1 条为口径边界，不影响结论）
  - 逐科分布（纯名称+别名 / 含 boundary）：数学 136/539、物理 201/663、化学 158/702、生物 166/304。
- **更正的含义**：截断确实存在，但 **62% 是"含 boundary 摘录"口径**——按"别名是否被截断"读，节点面是 **18.51%**，不是 62%。"大多数节点的别名只有一部分在生产索引里"这一表述过宽，应更正为"**18.51% 的节点的别名被截断；61.81% 的节点的 boundary 摘录被截断**"。
- **归宿不变**：该缺陷仍归已开启的同层 dense 兜底议题（结构性消灭——向量侧无截断损失），词面侧仍不以去截断方式修（v2 实测净伤害，§3.2）。KD-24 的**重开条件不变**（见 `docs/known-defects.md`）。

### A.2 dense 全扫与向量存储的账

- **全扫**：28,932 条（3,572 知识点 + 25,360 别名，审计 §2.4-1 口径）× 512 维**暴力余弦 12–23ms**（Stage-0 端侧口径，见 A.5）。**旧注的 1.4–2.8ms（§3.2 与审计 §2.4-1）是 int8 内存带宽估算下界，不作为端侧实测**——两者不冲突，量级差异来自估算 vs 实测，引用时以 12–23ms 为准。无 ANN 因此保持。
- **存储**：28,932 × 512 维 —— **int8 ≈ 14MB**（复算 14,813,184 B = **14.1 MiB**）、**fp32 ≈ 56MB**（复算 59,252,736 B = **56.5 MiB**）；两条与记录值一致（MiB 口径）。按"质量性能优先"裁定，**fp32 亦在放宽后的预算内**。

### A.3 Android 混合检索生态空白（Stage-0 取证，A.5 标注置信）

- **ObjectBox**：**无 FTS/BM25**（三重证据）→ 不采用。
- **Lucene**：上游跑不了 Android → 不采用。
- **kromus**（Apache-2.0，101KB 纯 Kotlin BM25+HNSW+RRF；0-star / pre-1.0）：**只作参照实现**（BM25/RRF 交叉校对），不作生产依赖。
- **HybridSearch.kt**：0-star，**只作参照**。
- **shiji-kb**：别名扩展算法可借（per-surface 并查集 + 泛称剔除 + 组内 OR/组间 AND），**仓库无 license → Kotlin 重写、不抄码**。
- **USearch-KMP**（6MiB+、停更 15 个月）、**端侧 RAG 项目**（模型全是英文）→ 不采用。
- 结论：Android 侧**没有可直接采用的混合检索成品**——词面用官方 FTS5（本轮 Stage-1），dense 侧自建 cosine（无 ANN）是当前唯一可解释的组合。

### A.4 许可证陷阱（Stage-0 取证；本轮纪律沿用计划 §5）

| 资产 | 结论 |
|---|---|
| AI-Pocket-Chat | **GPL-3.0，不可抄码**（copyleft 传染） |
| onnx-community 转换件 | **无 license 声明**——不可分发、不作依赖 |
| sentence-embeddings | **必须钉 v6**，否则 4KB 页不合规（16KB 设备兼容） |
| bge-small-zh 原版 | **MIT，可再分发**（若作对照/备选） |

纪律：**不采用任何 0-star / pre-1.0 库作生产依赖；不抄 GPL / 无 license 代码**（已写入 Stage-1 实验规则 §6 与计划 §5）。

### A.5 来源与置信（诚实标注）

- **本次会话独立复核（可复跑）**：金标集 90 条与 sha256 `7c004b76…`（复算一致）；A.1 的 661/18.51% 与 2,208/61.81%；A.2 的存储字节数；§3.2/§3.3 基线数（复读原文）。
- **Stage-0 取证、本次会话未独立复核（按"单一来源待验证"对待）**：Qwen3-Embedding-0.6B C-MTEB 66.33；ORT arm64 24.63MiB / LiteRT 5.25MiB 及两者 16KB 合规实测；OpenSearch 的 RRF −3.86% NDCG@10（6/6 BEIR）；28,932×512 全扫 12–23ms；`vec0.so` `p_align=0x1000`；ObjectBox"三重证据"；A.3/A.4 各条。
- **本次会话的取证失败实况（按"零命中不等于不存在"记录）**：`WebSearch` 通道不可用（provider 返回 `does not encode provider-native WebSearch`）；`huggingface.co/Qwen/Qwen3-Embedding-0.6B` 直连超时；OpenSearch 文档页换 3 个 URL 抓取均未取到 −3.86% 那句原文（命中的是导航页/404）。**因此 OpenSearch 那条按"单一来源待验证"记录，不作为已独立核实的证据**——立项若依赖它，需先补一次可溯源的取证。
- 上述未核实项**不支撑也不推翻**任何判定：§3/§6 的判定依据（金标实测数）与 §4 的档位选择（用户裁定）都不依赖它们。
