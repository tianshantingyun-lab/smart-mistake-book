# 知识库问题登记册 · 2026-09-15

> **来源**：2026-09-15 知识库深度全量研究（内部 + 外部）的全部发现，逐条登记。
> **口径**：每条含 编号 / 症状 / 位置 / 失败路径 / 证据 / 严重度 / 归属。证据来源分三级，逐条标注：
> - `[本轮实测]` —— 本轮亲自运行命令或解析源文件得到，可重跑
> - `[审计记录]` —— 来自 `docs/audit-2026-09-12-kernel-readiness.md`（独立证据源，本轮未逐条复跑）
> - `[外部研究]` —— 来自外部公开来源，附证据等级
>
> **本登记册只记录问题，不含修复方案。** 施工顺序由第 0 节的用户决定与依赖关系决定。

---

## 0. 用户决定（2026-09-15）

| # | 议题 | 决定 | 对本册的影响 |
|---|---|---|---|
| D-1 | 知识库是否做成学生可见对象 | **不做** | E-05 / E-06 维持现状，不视为缺陷，仅记录 |
| D-2 | 材料生产靶子 | **先补 998 个零材料节点** | A-01 升为最高优先级；A-03/A-04 降级 |
| D-3 | 前置关系 | **不留空** | B-15 / A-15 成为必做项，需建"未定"一等状态 |
| D-4 | 检索评分改动（IDF / 长度归一化） | **不急，依赖链上靠后** | D-01 降级为待办；D-03 基准扩充成为其前置 |
| D-5 | "全覆盖高考考点"口径 | **研究** → 结论见 `docs/research/coverage-baseline-design-2026-09-15.md` | B-01～B-06 的方向由该文定 |

---

## A. 内容层（质量门红项 · 21 条）

**统一的根因**：A-01～A-14 中多数不是"内容写错"，而是**定稿动作未完成**——两张权威表不存在（A-16）。

### A-01 · 998 个知识点没有任何教学材料 【P0 · 决定 D-2 直指本条】
- **症状**：2573 个原子知识点中 **998 个（38.8%）零材料**。分科：数学 80/406（19.7%）、生物 99/338（29.3%）、物理 312/732（42.6%）、化学 507/1097（46.2%）。
- **失败路径**：学生在这些节点上没有可讲的依据。讲题链路走到该节点时，`referencesFor` 返回空 → 模型只能凭自身知识作答，知识库在这一格上等于不存在。
- **证据**：`[本轮实测]` 解析 `core/data/src/main/resources/knowledge/` 全部 6 个 sidecar 得到（脚本见研究文档附录 A）。
- **归属**：**D-2 的施工对象**。

### A-02 · 892 条教学材料没有任何绑定，导入时被静默丢弃 【P0】
- **症状**：10356 条材料中 892 条 `bindings` 为空。
- **失败路径**：`BundledKnowledgePackResources.kt` 在合并侧车时过滤掉无绑定材料（第 78–88 行）→ 这些材料**写进了仓库、随 APK 分发、但在数据库里不存在**。写作成本 100% 浪费，且**没有任何报错或计数**。
- **证据**：`[本轮实测]` 10356 材料 / 9464 绑定 ⇒ 892 条零绑定；`[审计记录]` 门指标 `unbound_materials = 892` 与之一致。
- **备注**：与 A-01 是同一问题的两面——材料总量够，但**没有落到需要它的节点上**。

### A-03 · 材料类型只有 2 种，缺 5 种（含自评优先级最高的那一种）【P1】
- **症状**：`CONCEPT_EXPLANATION` 6900 / `METHOD_MODEL` 3456；`MISCONCEPTION_GUIDE`、`WORKED_EXAMPLE`、`DERIVATION`、`REPRESENTATION_GUIDE`、`COMPLETE_SOLUTION` **各 0 条**。
- **失败路径**：`docs/kb-content-conformance-spec.md` §3.2 自评 `MISCONCEPTION_GUIDE` 为**选材优先级 0（最高）**、`WORKED_EXAMPLE` 为 1，但两者都是 0 条 → 最该有的类型一条没有。
- **证据**：`[本轮实测]` 计数；`[审计记录]` 门 `unbound_materials` 同一数据源。
- **缓解**：五三管线已产出 16 条 `MISCONCEPTION_GUIDE`（见 A-19），方向正确。
- **归属**：D-2 之后。

### A-04 · 每节点材料数分布倒置：649 个节点超配、998 个节点全空 【P1】
- **症状**：`0→998 / 1→344 / 2→245 / 3→185 / 4→152 / 5+→649`。
- **失败路径**：运行时 `TutorPlanInput.MAX_TEACHING_REFERENCES = 4`（`core/model/.../TutorTasks.kt:287`），`RoomTutorTeachingReferenceRepository` 按 role→type→title 排序后取前 4 条。**第 5 条起永远不可见**。所以 649 个超配节点里超出 4 条的部分是**死内容**，而同期 998 个节点一条都没有。
- **证据**：`[本轮实测]` 直方图 + `[本轮实测]` 源码常量；`[审计记录]` §5.3.4 登记了相关的预算饥饿路径（DAO 的 LIMIT 先于字符预算截断）。
- **含义**：这不是"材料不够"，是**生产没对准消费约束**。

### A-05 · 584 个名称不是知识点名 【P1】
- **症状**：`配方法：主要用于二次函数或可化为二次函数的函数，要特别注意自变量的取值范围．`（整句话当名称）；`偶次方根的被开方数的被开方数必须大于等于零，即中`（公式被剥离）。
- **失败路径**：这些名称进入 `KnowledgeSearchFeatureExtractor` 生成检索特征 → 名称噪声污染检索；同时作为学生端可见的"知识点"名字出现（掌握度页、错题标签）。
- **证据**：`[本轮实测]` 门输出 `bad_names = 584`，含 8 条样例。

### A-06 · 96 个名称含 ★ 难度星号 【P2】
- **症状**：`病毒★★★☆☆`、`实验：使用高倍显微镜观察几种细胞★★☆☆☆`。
- **失败路径**：星号是教辅的难度标注，被当作名称一部分抽入。既污染检索特征，也直接违反 `docs/kb-content-conformance-spec.md` §2.2「禁用形态：含 ★」。
- **证据**：`[本轮实测]` 门输出 `starred_names = 96`，含 8 条样例。

### A-07 · 118 个同名重复知识点需合并 【P1】
- **症状**：`二面角 x2`、`分类 x3`、`意义： x2`、`倾斜传送带 x2`（"xN" 为重复份数）。
- **失败路径**：同名节点各自持有材料与绑定 → 检索时同一概念被切成多个候选互相竞争 top-5 名额；学生在掌握度页看到两条同名条目。
- **证据**：`[本轮实测]` 门输出 `duplicate_names = 118`。
- **备注**：生成器干跑投影已含"并 108"的计划，说明合并清单已在 `node_actions_reviewed.csv` 里定稿一部分。

### A-08 · 847 条幽灵别名（与当前绑定脱节）【P2】
- **症状**：别名表里 847 条指向的节点已不存在或已改名。
- **失败路径**：别名是检索特征的主要来源之一（精排权重 7），幽灵别名会**把查询引向错误的节点**——这是"误召回"的一个直接来源。
- **证据**：`[本轮实测]` 门输出 `ghost_aliases = 847`。

### A-09 · 968 条 boundary 含第三方原文摘录（分发风险）【P0 · 合规】
- **症状**：`boundaryMarkdown` 里含从教辅/教材直接摘录的原文，而非独立重写的边界描述。
- **失败路径**：这些字段随 APK 分发给学生。项目自己的来源政策（`docs/knowledge-source-governance.md`）要求全部来源默认 `REVIEWED_SYNTHESIS_ONLY`——**不复制来源表达**。968 条是**政策与产物直接冲突**。
- **证据**：`[本轮实测]` 门输出 `boundary_excerpt = 968`；`[审计记录]` 同一指标。
- **修法已有**：`tools/kb_build/textfix.py` 的 `strip_boundary_excerpt` / `has_verbatim_excerpt` 已实现剥离，`test_kb_textfix.py` 有 19 条测试守着。

### A-10 · 589 条 boundary 只有定位串，没有真边界 【P1】
- **症状**：`定位：数学必修第一册 第三章·函数的概念与性质。（见知识清单/教材）`。
- **失败路径**：boundary 是**出题范围的硬锚**——讲题时告诉模型"这道题的知识边界在哪"。定位串提供不了这个信息，等于该节点没有边界约束。而 boundary 同时是检索特征来源（权重 1）。
- **证据**：`[本轮实测]` 门输出 `locator_boundary = 589`。
- **根因**：`boundary_map.csv` 不存在（A-16）。生成器投影里"2141 条 boundary 待定稿"是同一件事的全量。

### A-11 · 194 处 LaTeX 命令丢失反斜杠 【P1】
- **症状**：`\frac` 之类被抽成 `frac`，公式失去语义。
- **失败路径**：数学/物理内容直接进入模型提示词 → 模型读到损坏公式；学生在材料里看到损坏公式。
- **证据**：`[本轮实测]` 门输出 `latex_damage = 194`。
- **修法已有**：`tools/kb_build/textfix.py:180 repair_latex_commands`，`test_kb_textfix.py` 有一条测试遍历**全部真实 sidecar 材料**断言 0 残留。
- **已知代码缺陷**：`gate.py` 中 `_is_latex_damaged` 被**重复定义两次**（第 132 行与第 146 行），后者静默覆盖前者，第一套基于 `_REAL_LATEX_COMMANDS` 的实现是死代码 → **门用的判定函数不是作者以为的那一个**。

### A-12 · 80 条材料含控制字符 【P2】
- **症状**：材料正文出现 `\n\r\t` 之外的控制字符（历史上还有两条字面 NUL 字节，在 2026-09-09 审计中修过源码，但侧车数据仍有）。
- **失败路径**：导入契约 `KnowledgeTeachingMaterialContract` 会拒绝含控制字符的材料 → **这 80 条在导入时被拒**（与 A-02 的"静默丢弃"不同，这是契约拒绝）。
- **证据**：`[本轮实测]` 门输出 `control_chars = 80`。
- **修法已有**：`tools/kb_build/textfix.py:293 strip_control_junk`。

### A-13 · 2163 个节点的定位串与章节表不一致（占 84%）【P1】
- **症状**：节点定位写作 `数学必修第一册 函数的概念与性质`，章表是 `数学必修第一册 第三章 函数的概念与性质`——**缺章序号**。
- **失败路径**：定位串是内容可追溯性的载体（"这个知识点依据哪一章"）。84% 不一致意味着**追溯链在绝大多数节点上是断的**；同时它让"哪些章的节点还没材料"这类统计无法可靠计算。
- **证据**：`[本轮实测]` 门输出 `chapter_locator_mismatch = 2163`，样例为数学必修一第三章。
- **根因**：生成器 `_rebuild_topics` 会从章表**派生**定位串，但该重建尚未执行到成品包。

### A-14 · 6 处标记为跨章拆分却缺节点级覆盖 【P2】
- **症状**：物理"专题11 恒定电流"缺 `电流：`、`电动势：`；"专题12 磁场"缺 `定义：通电导线在磁场中受到的力叫安培力。`；"专题14 交变电流"缺 `电磁波：`；"专题17 热学"缺 2 条。
- **失败路径**：这些是拆分时**掉在地上的碎片**——原文被拆到别的节点去了，留下一个空的壳。
- **证据**：`[本轮实测]` 门输出 `chapter_split_missing_override = 6`，含逐条样例。

### A-15 · 1489 条前置边全部"未在权威表声明" 【P1 · 决定 D-3】
- **症状**：1489 条 `prerequisiteSlugs` 一条都没有对应的权威表条目。
- **失败路径**：门无法区分"这条前置是对的"和"这条前置是编的" → 1489 条边全部处于**不可信状态**。项目此前的处置是整体留空。
- **用户决定 D-3**：**不留空**。因此需要的是"未定"一等状态 + 逐条裁定通道，而不是删除。
- **证据**：`[本轮实测]` 门输出 `undeclared_prereq = 1489`；`[本轮实测]` 包内实测 prereqEdges = 1489。
- **外部对照**：`[外部研究]` cn-k12-math-knowledge-graph 对 596 个零入度主题的处理是 **438 个显式保留"未定"、拒绝写成"确认无前置"**，并公开 trace/release hash〔一手项目文档〕。这是可直接照抄的处置形态。

### A-16 · 两张权威表根本不存在 【P0 · 多数 A 类问题的根因】
- **症状**：`tools/kb_build/tables/boundary_map.csv` 与 `prereq_map.csv` **在磁盘上不存在**，但 `tools/kb_build/tables.py` 与 `build.py` 都引用它们。
- **失败路径**：`load_boundary_map` / `load_prereq_map` 读不到 → 所有 boundary 与前置都判"未声明" → 直接产生 A-10（589）、A-15（1489），并间接产生生成器投影里的"1571 条 boundary 剥离、2141 条待定稿"。
- **证据**：`[本轮实测]` `tables/` 目录清单（16 个文件，无这两个）；`[外部研究]` 无需。
- **含义**：**这不是内容质量问题，是定稿流程没走完**。补上这两张表是 A 类里杠杆最大的一步。

### A-17 · 节点是"空壳"：没有 definition / formula / examples 字段 【P1】
- **症状**：一个知识节点的全部内容是 `slug, name, aliases, kind, boundary, sourceLocator, prerequisiteSlugs`——**没有学科内容本身**。内容全部寄托在"材料"侧车的 4 个 markdown 字段上。
- **失败路径**：没有材料的那 998 个节点（A-01）就是彻底的空壳；有材料的节点也只有在材料被选中（≤4 条、≤20000 字符）时才"有内容"。
- **证据**：`[本轮实测]` 解析 `moe-2025-four-subjects-v1.json` 的键集（7 键）。
- **外部对照**：`[外部研究]` K12-KGraph 的 Concept 节点带 `definition / formula / aliases / examples`，边带 evidence 回指教材原文〔一手项目文档〕；Marble 的 micro-topic 带 `evidence`（掌握证据判据）与 `assessmentPrompt`〔一手项目文档〕。**本项目缺这两类字段**。

### A-18 · 材料—节点绑定正确率抽样仅 42% 【P0】
- **症状**：抽样核对显示只有 42% 的绑定"完全一致"，主要误绑发生在同章相邻概念（光合作用↔细胞呼吸、电场力做功↔弹力做功）。
- **失败路径**：绑定错误 = **讲错知识点**。学生做错一道题，系统认为他错在邻近概念，掌握度记到错误的 KC 上，后续排程与讲解全部偏。
- **证据**：`[技术文档]` `docs/kb-content-conformance-spec.md` §4.2 自述"现行抽样只有 42% 完全一致，必须做实"。
- **外部对照**：`[外部研究]` cn-k12-math 的做法是每条边带 stable ID + trace ID + source type + release hash〔一手项目文档〕。**本项目缺"可审计绑定"**。
- **备注**：这是全册里唯一直接影响**学习正确性**的内容问题，严重度不低于 A-01。

### A-19 · 五三管线已产出的材料尚未并入成品包 【P2 · 正常在途】
- **症状**：`knowledge-production/wusan/materials.jsonl` 134 条（化学 128 / 物理 6），类型 `CONCEPT_EXPLANATION 103 / METHOD_MODEL 15 / MISCONCEPTION_GUIDE 16`。**这些不在任何成品包里**，运行时不可见。
- **失败路径**：暂无——这是正常在途状态。记录它是为了后续统计时不要把"已写"当成"已生效"。
- **证据**：`[本轮实测]` 文件行数、逐行解析的类型分布。

### A-20 · 五三候选约一半未解析到既有节点 【P2】
- **症状**：`wusan_resolved.csv` 6538 行解析成功、`wusan_unresolved.csv` 6321 行未解析（设计上**刻意不做模糊匹配**）。
- **失败路径**：未见得是缺陷——未解析项一半可能是噪声（页眉、碎片），一半可能是**真缺的知识点**。未逐条裁定前不能当缺口，也不能当噪声。
- **证据**：`[本轮实测]` 行数；`[文件]` `tools/kb_build/resolve_points.py:144`。

### A-21 · 逐章补材料章节仍有 28.2% 的节点没材料 【P2】
- **症状**：`wusan_plan.csv` 95 章覆盖 2170 个节点，其中 1557 有材料 → 缺 613 个；95 章里 **75 章标"部分无材料"**、20 章标"有节点有材料"。
- **失败路径**：五三管线当前的工作队列本身就是"还差 613 个节点"。
- **证据**：`[本轮实测]` 解析 `wusan_plan.csv`。

---

## B. 覆盖基线层（"全覆盖"的判定基础 · 12 条）

### B-01 · 外部不存在可核验的权威"高考考点总数" 【P0 · 决定 D-5】
- **症状**：无法用"一个权威数字"来闭合"全覆盖高考考点"这个验收标准。
- **证据**：`[外部研究]`
  - 国办发〔2019〕29 号原文：**"实施普通高中新课程的省份不再制定考试大纲"**〔官方〕
  - 教育部教育考试院高考栏目"考试大纲"分类页**最新条目 2019-01-31**〔官方站点〕
  - 《中国高考评价体系》配套答记者问原文：**"中国高考评价体系不是考试大纲，也不是界定考试范围的规范性文件"**〔官方〕
  - 学科网/菁优网/组卷网均不可访问（反爬），**未找到任何可核验的分科考点计数**〔未找到〕
- **归属**：D-5 → 结论见 `docs/research/coverage-baseline-design-2026-09-15.md`。

### B-02 · 课标 2025 提取是机械提取，全部 DRAFT_UNREVIEWED 【P1】
- **症状**：`knowledge-coverage-ledger-2025-v1.json` 九科 125 模块 / 1322 条候选知识点，**九科的 `mappingState` 全是 `DRAFT_UNREVIEWED`**；模块级 `requirementType` 分布 `REQUIRED 35 / SELECTIVE_REQUIRED 27 / ELECTIVE 63`。
- **失败路径**：这份清单**不能作为覆盖证据**——门 `audit-knowledge-coverage-ledger.ps1 -RequireComplete` 要求 `REVIEWED`，而 `REVIEWED` 又要求来源为 `ACQUIRED_REVIEWED`。
- **证据**：`[本轮实测]` 解析 ledger，逐科计数（数学 24 模块/305 点、物理 9/138、化学 11/73、生物 27/142）。

### B-03 · 九份课标 PDF 只剩三份，提取管线不可重跑 【P0】
- **症状**：`.artifacts/research/high-school-standards-2025/` 只有 `physics.pdf`(2,389,263 B/74 页)、`chemistry.pdf`(2,696,884 B/108 页)、`biology.pdf`(2,185,395 B/94 页)。**数学、语文、英语、政治、历史、地理六份不在磁盘上**，且 `.artifacts/` 被 gitignore。
- **失败路径**：`python tools/extract-curriculum-coverage.py --check` 报 `ERROR: Missing acquired curriculum PDF: …\chinese.pdf`，exit 1；`build-curriculum-warning-evidence.py --check` 报缺 `math.pdf`，exit 1。**四科里的数学那一份缺失**，所以四科的基线也无法完整重算。
- **证据**：`[本轮实测]`（由清点代理执行并复述命令与输出）。
- **缓解**：三份在盘 PDF 的 SHA-256 与 `source-register` 的 `contentFingerprint` **逐条吻合**，字节数吻合 → 可用同一镜像地址重新取得其余六份（`documentUri` 指向济南十一中镜像，非教育部原始地址）。

### B-04 · 教材目录与课程活动目录文件全部缺失 【P2】
- **症状**：`source-register` 声称已取得并审校两个 SmartEdu 目录（教材元数据 3229 条、课程活动 17005 条），但**磁盘上这些文件一个都不存在**，从未提交过 git。
- **失败路径**：`tools/audit-smartedu-textbook-catalog.ps1 -RequireNineSubjects` 与 `…lesson-activity-catalog.ps1` **必然失败**——发布门里有两道门在当前检出上无法执行。
- **缓解**：即使文件在，它们也**只有元数据（版本、册次、资源ID、提供者），没有章节目录**。见 B-05。

### B-05 · 不存在"课标 → 人教版章节 → 知识库节点"的三方映射 【P1 · 决定 D-5】
- **症状**：全仓 grep 确认：没有任何文件把课标条目连到教材章节或 KB 节点。
- **现存的最接近物**（都不是同一件事）：
  - `tools/kb_build/tables/wusan_page_placement.csv`（719 行，699 行有 `renjiao_chapter`）—— **五三页 → 人教版(册,章)** 路由，管线未跟踪。
  - `tools/kb_build/tables/chapter_map.csv`（358 行）—— 节点 → 人教版(册,章)，**覆盖仅部分**：数学 41 节点/3 册/7 章、物理 230/5/17、化学 33/2/8、生物 54/3/12。
  - `tools/kb_build/tables/chapter_by_node.csv`（2541 行）—— 名称匹配派生，**有噪声**（如物理的 book 值出现 `物理必修二`、`物理其余`；107 行缺 book/chapter）。
- **证据**：`[本轮实测]`（清点代理逐文件解析）。
- **含义**：覆盖对照表要建立，**教材章节这一层必须从零建**（或声明放弃，只做课标↔节点两层）。

### B-06 · 无任何人工审校：182 条 AI 拆分提案 0 决策 【P1】
- **症状**：`curriculum-warning-decomposition-proposals-2025-v1.json` 有 182 条候选（数学 3 模块 + 生物 22 模块），**全部 `PENDING_HUMAN_REVIEW`**；`curriculum-warning-human-review-decisions-2025-v1.json` 的 `reviewState = NOT_STARTED`、`decisions = []`。
- **失败路径**：这 25 个模块是**机械提取失败、需要人工原子化**的模块（warning `SCOPE_SUMMARY_REQUIRES_MANUAL_ATOMIZATION`）。它们在最终覆盖表里将是永久空洞，除非有人按下这 182 个决定。
- **证据**：`[本轮实测]` 三个 JSON 的状态字段；`[本轮实测]` 提案审计 exit 0、`promotionReady=false`。
- **正面**：提案校验器（`proposals.py`）要求每个候选的 `evidencePhrase` **在捕获的课标原文里逐字出现**，且 `reviewBoundary` 强制 `promotionAllowed:false` ——**这套"AI 只起草、人才能晋升"的机制设计是对的**。

### B-07 · 仓库里有两个都叫"候选"的东西，极易混淆 【P2】
- **症状**：同名不同物，两处计数相差一个数量级，且分属不同层级、不同目录、不同跟踪状态：

  | | 内容候选层 | 课标覆盖候选层 |
  |---|---|---|
  | 位置 | `knowledge-research/candidates/` | `knowledge-production/knowledge-coverage-candidates-2025-v1.json` |
  | **git 状态** | **被 gitignore**（`.gitignore:46`） | tracked |
  | 实测计数 | `manifest-summary.json`：**totalEntries 13889** = 教学支持 10353 + 目录节点 3536 | `candidateStatements` **1322** = CURRICULUM_STATEMENT 1297 + SCOPE_SUMMARY 25 |
  | 含义 | 将要/已经变成知识包的**内容**条目 | 课标**内容要求**的机械提取条目 |
  | 时间 | 2026-09-05 | 2026-07-25 |

- **失败路径**：把"13892 条候选已就绪"当成覆盖基线就绪 → **高估课标对齐完备度约 10 倍**（13889 是内容，1322 才是基线）。反过来，把 1322 当成"内容只有 1322 条"也会严重低估。
- **证据**：`[本轮实测]` 两个文件分别解析；`[本轮实测]` `git check-ignore` 确认内容候选层被忽略。
- **备注（本条初版有误，已订正）**：本登记册初版曾断言"13892 是错记、实际 1322"——**那是错的**。13892 是内容候选层的真实计数（manifest 记 13889，记忆记 13892，差 3 条属口径微差）。两个数字都真、只是量的不是同一件事。

### B-07b · 2026-09-05 那次"高考考点联网核对"没有留下可复算证据 【P1】
- **症状**：`knowledge-research/candidates/manifest-summary.json` 的 `audit` 字段记录：`gaokao-2025-gap-check: "联网核对补缺18+1条"`、`textbook-concepts-check: "人教版教材(化学/生物重点)标题级核对，补齐…"`、`spotCheck: "抽查12条…"`。
- **失败路径**：这些是**一句话结论**，没有留下：核对了哪些来源、URL、核对时的清单、判定规则、缺口逐条记录。因此**同一件事无法被复核也无法被重跑**——而它正是项目记忆里"知识库最终验收要求"第 1 条（联网核对高考考点全覆盖）的执行记录。
- **证据**：`[本轮实测]` 解析 `manifest-summary.json`；`[记忆]` `knowledge-base-final-audit-requirements`。
- **含义**：这不是"没做"，是**做了但没留痕**。设计文档 3.2 节要求的"基准文件 + 计数脚本 + 口径 + 重算命令"四件套，正是为了消除这一类不可复核。

### B-08 · 发布门要求九科，项目范围是四科 → 结构上永不通过 【P1】
- **症状**：`tools/audit-knowledge-source-register.ps1` 的 `requiredSubjects` 硬编码九科且要求每科 ≥2 个已审校教学参考 + 1 个方法参考 + 1 个完整例题参考；`audit-knowledge-packs.ps1 -RequireFullCoverage` 要求九科齐全。
- **失败路径**：项目已定范围为数学/物理/化学/生物。**这两道门在当前结构下永远失败**，而它们正是 `docs/knowledge-source-governance.md` 里宣称的发布门。
- **证据**：`[本轮实测]` 脚本源码 + `[技术文档]` `docs/knowledge-source-governance.md` 发布门一节；`[记忆]` "知识库范围定为四科——九科补完永久取消；审计九科门槛为已知边界"。
- **含义**：门与范围必须对齐：要么把门收窄到四科，要么显式声明这两道门**不适用于本产品**并移出发布门清单。**现状是"文档宣称有门、门不可能通过"，比没有门更危险。**

### B-09 · 生成器承诺的 `--build` 参数不存在 【P3】
- **症状**：`tools/build-knowledge-pack.py` 的模块 docstring 写"`--build` 依权威表生成成品包"，但**参数解析器里没有这个 flag**。实际构建要直接跑 `PYTHONPATH=tools python tools/kb_build/build.py`。
- **失败路径**：照着文档调用会失败。
- **证据**：`[本轮实测]`（清点代理读源码）。

### B-10 · `build/kb-staging` 快照没有 provenance 【P2】
- **症状**：`build/kb-staging/` 的包与 6 个 sidecar（2026-09-14 23:55）与已发布的成品包字节不同（如 sidecar-06 是 332,345 B vs 成品 148,044 B），但**目录里没有任何文件记录它是用哪些权威表、哪个 commit 生成的**。
- **失败路径**：无法回答"这个暂存包能代表当前权威表吗"。
- **证据**：`[本轮实测]` 目录清单 + 文件时间戳。

### B-11 · 课标 2025 版把学业质量水平级数改了，旧映射不能照搬 【P2】
- **症状**：2020 版四科的高考对应层级是**数学水平二、物理水平4、化学水平4、生物水平四**（各科官方原文明确）；而 2025 日常修订版据修订组署名解读，**物理与数学的学业质量水平已从五级/多级重构为三级**。
- **失败路径**：若以 2025 版为基线却沿用 2020 版的层级映射，会把"高考对应层级"标错。
- **证据**：`[外部研究]` 2020 版映射为各科官方 PDF 原文〔官方〕；2025 版变化为修订组组长署名解读文章〔**单一来源待验证**——课程教材研究所官网 412 反爬，官方发布文本未取得〕。
- **含义**：基线选择必须**以 2025 版 PDF 的"学业质量水平与考试评价的关系"原文为准**，不能用解读文章。

### B-12 · 课标没有官方公布的"内容要求条目数" 【P2】
- **症状**：教育部/考试院/教材所**从未公布**分科条目计数〔未找到〕。而各科编号体系不同（数学用圈号①②③、化学只到 `x.y`、物理/生物到 `x.y.z`），**"条目数"完全取决于计数口径**。
- **失败路径**：任何引用"课标有 N 条内容要求"的说法都不可复核，除非同时公布计数规则与重算脚本。
- **证据**：`[外部研究]` 官方 2020 版 PDF 现场重计数得数学 132 / 物理 137 / 化学 63 / 生物 120，**与项目 2025 版的 305/138/73/142 无法用同一规则对齐**（版本差异 + 口径差异叠加）〔重计数，非官方数字〕。

---

## C. 生产工具链层（8 条）

### C-01 · 整套内容生产线从未提交过 git 【P0 · 最高工程风险】
- **症状**：`git ls-files tools/kb_build` → **0**；`git ls-files knowledge-production/wusan` → **0**。而磁盘上 `tools/kb_build/` 有 46 个 Python 模块（约 9000 行）+ 16 张 CSV 权威表，`materials.jsonl` 有 134 条新材料。
- **失败路径**：误删/误改不可回溯；`--verify-roundtrip` 的保真证明依赖的工具本身没有版本锚；任何"这套流程是可重跑的"说法**在版本控制层面不成立**。
- **证据**：`[本轮实测]` `git ls-files` 计数。
- **同批未跟踪**：`docs/kb-content-conformance-spec.md`、`docs/audit-2026-09-12-kernel-readiness.md`、`tools/tests/test_kb_*.py`（6 个文件）、`tools/build-knowledge-pack.py`。

### C-02 · 权威表内部一致性只有部分被测试覆盖 【P2】
- **症状**：`verify_tables.py` 守的是"改名前后文本重叠 ≥0.5"，`test_kb_verify_tables.py` 只 5 条测试；`node_actions_reviewed.csv` 184 行已复核覆盖 `node_actions.csv` 871 行。
- **失败路径**：权威表是**唯一事实源**（生成器从它派生成品包），但它的正确性主要由人工保证。
- **证据**：`[本轮实测]`（清点代理统计的行数与测试数）。

### C-03 · `gate.py` 的 LaTeX 判定函数被重复定义 【P2】
- **症状**：`_is_latex_damaged` 在 `gate.py:132` 与 `gate.py:146` 各定义一次，**后者静默覆盖前者**；第一套基于 `_REAL_LATEX_COMMANDS` 的实现成为死代码。
- **失败路径**：门的判定行为与作者意图不一致；改动第一处不产生任何效果（同族问题见 `[审计记录]` §5.3.7 的"重复死常量"模式）。
- **证据**：`[本轮实测]`（清点代理读源码定位两处 def）。

### C-04 · 材料侧车数量上限是硬编码的，开第 7 个文件会被拒 【P3 · 设计边界】
- **症状**：`build.py:_apply_new_materials` 拒绝打开第 7 个 sidecar，因为 Kotlin 侧 `BundledKnowledgePackResources.kt` 的 sidecar 名单是硬编码的 6 个。
- **失败路径**：内容增长超过 6×2048 条时**必须同时改 Kotlin 与 Python 两侧**。
- **证据**：`[本轮实测]`。

### C-05 · 生成器干跑投影的规模变化尚未执行 【P1】
- **症状**：`PYTHONPATH=tools python tools/kb_build/build.py`（干跑）投影：知识点 2573 → **2168**（删 324、并 108）、topic 445 → 331、+27 新点、+134 新材料、708 条绑定改指 / 37 条解除、1571 条 boundary 剥离、2141 条待定稿、262 条材料文本修复。
- **失败路径**：这是"已设计好但未执行"的修订全集。在它执行前，A 类指标不会改善。
- **证据**：`[本轮实测]` 干跑输出。

### C-06 · 五三路由/解析管线依赖未跟踪的中间产物 【P2】
- **症状**：`harvest_placement.py` 从 `knowledge-research/candidates/wusan/` 的转录文本取数，而 `knowledge-research/` 被 gitignore。
- **失败路径**：即使把 `tools/kb_build` 提交，**输入仍不可复现**——从扫描页图到转录这一步没有落在版本控制里。
- **证据**：`[本轮实测]`（清点代理读脚本与 `.gitignore`）。

### C-07 · `review_draft.py` 明确不做内容相似度校验 【P3 · 设计取舍】
- **症状**：脚本注释说明"明确不评分内容相似度，该做法已被基线证伪"。
- **失败路径**：子代理草稿的质量只能靠人工读，没有自动化的第一道筛。
- **证据**：`[本轮实测]`。

### C-08 · 五三材料生成器是 8 个手写模块 【P2】
- **症状**：`wusan_chem_b1_ch3.py`、`wusan_chem_b2_ch5.py`、`wusan_chem_x1_ch3.py`、`wusan_chem_x3*.py`、`wusan_phy_x3_ch5.py` 等 8 个文件，每个把一章的材料硬编码在 Python 里，再追加到 `materials.jsonl`。
- **失败路径**：这是**当前唯一在产出新内容的机制**，但它不是可扩展的流水线——95 章的工作量对应 8 个手写文件。决定 D-2（补 998 个零材料节点）会立刻撞上这个形态的上限。
- **证据**：`[本轮实测]`（清点代理列出文件与行号）。

### C-09 · `node_actions` 表与成品包**不同坐标系**，build 全流程产出错乱 【P0】
- **症状**：`node_actions.csv`（871 行：rename 439 / delete 324 / merge 108）里的 slug **大量对不上成品包**——实测 324 个 delete 的 slug **一个都不在成品包**里（`pack_io.iter_points` 找不到）。
- **失败路径**：`build.py` 从成品包起步应用 `node_actions`，大部分操作 no-op（slug 对不上），但 `chapter_map` 重组 + 部分命中 + `_rebuild_topics` 的 theme 派生失败退回章层，**综合产出错乱的 staging**：总点 2573→2168，**章层挂点从成品的 167 恶化到 352**。即 build 出的 staging 比成品**更差**。
- **根因**：`node_actions` 大概率基于**内容候选层**（`knowledge-research/candidates/`，13889 条，不同 slug 体系，被 gitignore）建，不是基于成品 2573。候选层 slug ≠ 成品 slug。
- **含义**：
  1. **build.py / node_actions 这条"生成链"当前不可用于成品**——它不是"成品的可复现生成器"，roundtrip 只保真格式、不保真业务逻辑。
  2. 本轮结构修复因此**绕过 build，直接表驱动改成品**（`shorten_topic_names` / `relocate_chapter_points`），每个转换独立/幂等/无损/有门。
  3. `node_actions` 要可用，需**基于成品 slug 重建**（对齐坐标系），或明确它只作用于候选层。
- **证据**：`[本轮实测]` 324 delete slug 全不在成品；成品 167 章层 vs staging 352 章层。

- **裁定（2026-09-19 收口）**：`node_actions.csv` **作废**，不再作为任何生成链的权威输入；`build.py` 继续停用。「知识点动作」的权威日志改为**五张表 + git 历史**：`point_rename.csv`（改名）、`point_delete.csv`（删除，含 drop_materials）、`point_merge.csv`（合并）、`material_rebind.csv`（材料改绑）、`chapter_point_relocation.csv`（归位/主题跨章）。理由：这五张表全程经过"逐条校验 + 幂等 + 无损 + 门"的实际执行，与成品坐标系一致且有 git 提交可溯源；`node_actions` 的 871 行既对不上成品、也无人再执行。若将来要恢复生成链，应基于**当前成品**从这五张表重放，而不是复活 `node_actions`。
- **处置**：已绕过（直接改成品）。node_actions 的重建/作废**单列待决**——它牵涉"候选层→成品"的晋升链是否还存在。

---

## D. 检索层（8 条）

### D-01 · stage-1 排序无 IDF、无长度归一化 【P1 · 决定 D-4 降级为"靠后"】
- **症状**：`ProblemOrganizationDao.searchSubjectKnowledgeRecallCandidates` 的排序是 `ORDER BY COUNT(DISTINCT feature) DESC` ——**等于"二值 TF、无 IDF、无长度归一化"的 BM25**。
- **失败路径**：中文高频 2-gram（`的`、`是`、`函数`…）在 2573 个节点里 df 极高，却被与稀有 n-gram **等价计票**；同时**名称越长产生的特征越多，无归一化时越长越占优**（系统性长度偏置，不是随机噪声）。
- **证据**：`[本轮实测]` 源码；`[外部研究]` 缺 IDF 是教科书级缺陷（《Introduction to IR》§6.2）〔A 级〕；Lucene `BM25Similarity` 与 FTS5 `bm25()` **默认都含 IDF**〔官方文档，B 级〕。
- **用户决定 D-4**：不急，依赖链靠后。

### D-02 · 精排权重全部硬编码、无依据 【P2】
- **症状**：`名称×8 + 别名×7 + boundary×1 + 精确短语 80/70 + 父级×3`，全部为内联字面量。
- **失败路径**：违反项目自身的 `algorithm-parameters-science-grounded` 要求（算法参数须研究 + 交叉验证 + 标注证据等级）。改变任一权重只改分数、不改已知结论，**因为没有能分辨差异的数据集**（见 D-03）。
- **证据**：`[本轮实测]` `KnowledgeContextRetriever.kt:69-95`。

### D-03 · 检索基准只有 20 条手写题面，太小 【P1 · D-01 的前置】
- **症状**：`FourSubjectRetrievalBenchmarkTest` 用 `RetrievalBenchmark.queries` 的 **20 条**手写题面，`expected` 用"名称子串"近似，阈值 `Recall@5 ≥ 0.85 / crossSubject == 0 / MRR ≥ 0.7`。
- **失败路径**：IR 文献下限是 **50 条 query**，且 Voorhees 2009 证明即使 50 条"显著差异也可能错"〔A 级〕。当前 20 条足以做冒烟门，**不足以支撑任何参数裁决**。
- **证据**：`[本轮实测]` 我复跑该测试：**0 failures**，`Recall@5 = 0.95 (19/20)`、`Precision@5 = 0.70`、`MRR = 0.842`、跨科误召 0；`[外部研究]` Voorhees & Buckley 2002 / Voorhees 2009 / Sakai 2015〔A 级〕。
- **含义**：D-01 的施工顺序应是**先扩基准到 ≥50 条并切分调参集/报告集，再改评分**。

### D-04 · 逐章题面 Recall@5 只有 0.733，且刻意不进阈值 【P1】
- **症状**：新增的 `chapterQueries`（15 条，覆盖正在逐章补材料的章节，预期节点全部取自这些章里**已绑定材料**的节点）在暂存包上 `Recall@5 = 0.733 (11/15)`、`Precision@5 = 0.253`、`MRR = 0.70`。它**不参与** `FourSubjectRetrievalBenchmarkTest` 的阈值断言。
- **失败路径**：这是"材料 → 标题 → 别名 → 召回"这条链的**唯一可见水位**，也是五三内容投入能否转成检索收益的唯一凭证。0.733 说明这条链目前是漏的。
- **证据**：`[本轮实测]` 我复跑出的指标（`build/benchmark-metrics.txt` 写入了两套数字）。
- **备注**：作者在注释里说明"口径保持不变，要不要并进门槛是将来的单独决定"——**这个决定现在到了**。`[外部研究]` 支持先把金标集切分调参/报告集再谈门槛〔Anserini 官方方法 B 级 + Voorhees 2009 A 级〕。

### D-05 · Python 检索质量门是脚手架，不能作为门 【P2】
- **症状**：`tools/retrieval_quality_gate.py` 默认阈值 `recall5 ≥ 0.8 / mrr ≥ 0.6 / ndcg5 ≥ 0.6`；但金标集 `retrieval_quality_golden_set.json` **只有 5 条查询**、节点 id 是编造的（`math:monotonicity`，生产格式是 `kb:moe-2025-four-subjects-v1:math:atomic:<slug>`），`evaluate()` **完全忽略 `subject` 字段**（因此不断言分科隔离），且**仓库里没有任何生产者输出它要的结果文件、CI 也不调用它**。
- **失败路径**：`[审计记录]` §13.1 把它称为"机器门"——**这个说法会让读者以为检索有 CI 级门禁，实际没有**。
- **证据**：`[本轮实测]` 金标集内容、`evaluate` 源码、grep 无调用点。

### D-06 · `KnowledgeRetriever` / `KnowledgeRetrievalBenchmark` 是死代码 【P3】
- **症状**：`core/domain/.../KnowledgeRetrievalBenchmark.kt` 定义 `interface KnowledgeRetriever` 与 `class KnowledgeRetrievalBenchmark`，**生产中零实现、零调用**。
- **失败路径**：读者会以为存在一个"可替换的检索抽象"，实际检索写死在 core:data。
- **证据**：`[审计记录]` §5.6.6 `knowledge-retrieval-benchmark-orphan`（R-06 清理清单内）；`[本轮实测]` 清点代理 grep 确认 main 上仍在，删除只在 `audit/kernel-readiness` 分支。

### D-07 · 召回收 `USER_CONFIRMED`，精排却拒绝它 【P1】
- **症状**：`ProblemOrganizationDao` 的召回 SQL 的 `verification_status IN ('CURATED','SOURCE_GROUNDED','USER_CONFIRMED')`，但 `KnowledgeContextRetriever.select` 只保留 `CURATED`/`SOURCE_GROUNDED`。
- **失败路径**：**用户自建知识点能过召回、必被精排丢弃** → 永远进不了整理提示词。而 `RoomMistakeOrganizationRepository` 写入侧注释明说用户修正的节点"必须能被后续复用"。
- **证据**：`[本轮实测]` 两处源码。

### D-08 · `ensureKnowledgeSearchIndex` 绕过 port 层的 512 上限 【P3】
- **症状**：自愈路径 `RoomKnowledgeBaseStore.ensureKnowledgeSearchIndex` 直接调 DAO 的无特征重载，`missingCheckCount` 可超过 `readSubjectKnowledgeRecallCandidates` 的 `1..512` 端口守卫。
- **失败路径**：只在索引损坏需要重建时可达；影响是内存/事务规模，不是正确性。
- **证据**：`[本轮实测]` 两处源码；清点代理独立标出。

---

## E. 运行时消费层（8 条）

> **决定 D-1 已判定"知识库不做成学生可见对象"** → E-05、E-06 记为**现状描述而非缺陷**。E-01～E-04 是"知识库在已有入口没被调用"，与 D-1 无关，仍然是缺陷。

### E-01 · 大厅自由提问完全不检索知识库 【P1】
- **症状**：`RoomModelTaskRepository.toolDeclarationsFor` 对 `TutorLobbyInput` 只声明 `NOTEBOOK_READ`；大厅提示词允许模型凭自身知识作答。
- **失败路径**：学生在讲题大厅问一道知识问题（"为什么浓硝酸使铁钝化"），**知识库里可能有完全对口的材料，但这条路径从不查库**。
- **证据**：`[本轮实测]` 清点代理源码定位。

### E-02 · 拍照 → 讲题链路注入零知识 【P1】
- **症状**：`ConfirmedTutorSession.toTutorQuestionContext()` **硬编码** `relatedKnowledgeNodeIds = emptySet()`、`reviewedTeachingReferences = emptyList()`（`feature/tutor/TutorModelTaskPolicy.kt:127-134`）。
- **失败路径**：**刚拍完一道题直接问讲题**——这是产品最主流的入口——模型拿不到任何绑定与教学材料；只有**从错题本里打开**同一道题的讲题（`SavedMistakeTutorRoute`）才加载。
- **证据**：`[本轮实测]` 清点代理源码定位。

### E-03 · `KNOWLEDGE_READ` 名不符实 【P1】
- **症状**：工具描述是"读取这道题相关的知识点**讲解材料**"，实现（`RoomTutorToolRunner.knowledgeRead`）只返回 `displayName` + 截断的 80 字 `boundaryMarkdown`。
- **失败路径**：**没有任何代码路径让模型通过工具拿到教学材料**；`readKnowledgeTeachingMaterialsForNodes` 只在讲题参考仓库与复习规划里被调用。模型以为自己在读材料，实际只拿到一个名字。
- **证据**：`[本轮实测]` 清点代理源码定位 + 我复核的研究结论。

### E-04 · `MASTERY_UPDATE` 要求的知识点 id 从不披露给模型 【P1】
- **症状**：提示词语法要求 `terms:["<知识点id>"]`，但证据 JSON 只带 `label`、教学参考不含 id、`KNOWLEDGE_READ` 输出也不含 id。而锚定检查要求 id 真实存在且同科。
- **失败路径**：**聊天路径的掌握度写入在无注入 id 的前提下不可达**——模型给不出合法 id，写出必被拒。这与"掌握度只由客观证据更新"的初衷可能一致，但当前形态是**静默不可达**，不是"设计上拒绝"。
- **证据**：`[本轮实测]` 清点代理源码定位（提示词语法 vs 锚定检查）。
- **备注**：`KnowledgeQuizFeedbackWriter` 那条通道是**结构完整**的（`[审计记录]` §7.3 列为 healthy），所以掌握度并非全断，只是聊天这条不通。

### E-05 · 学生端没有"知识库"这个对象 【现状 · 决定 D-1 不做】
- **症状**：无全树浏览、无知识库搜索、无章节树。学生只在 ① 错题上的"知识点："标签 ② 掌握度列表（**只列有掌握记录的节点**）③ 由材料生成的复习测验题 ④ 模型答案里见到知识库。
- **证据**：`[本轮实测]` 清点代理 UI 面盘点。

### E-06 · 覆盖自报是死胡同 【现状 · 决定 D-1 不做】
- **症状**：`StudyExperienceSnapshot.knowledgeCoverage` 被构造、被映射（`StudyExperienceMappers.kt:214-240`），**没有任何 UI 或 feature 读它**；没有任何功能按覆盖度放行。
- **证据**：`[本轮实测]` 清点代理 grep 全仓无消费者。

### E-07 · `source-register-2025-v1.json` 随 APK 分发但 Kotlin 从不读 【P3】
- **症状**：50,819 字节的来源登记被默认打进 APK（我已从构建出的 release APK 的条目清单中确认），但**没有任何 Kotlin 代码引用它**——只有 PowerShell/Python 审计读它。
- **失败路径**：白占分发体积；且读者会以为运行时按来源政策做了过滤，实际没有。
- **证据**：`[本轮实测]` 清点代理 grep `.kt` 无引用。

### E-08 · `:knowledge-production` Kotlin 模块是死代码 【P2】
- **症状**：`settings.gradle.kts` include 了 `:knowledge-production`，但**没有任何模块依赖它**，模块内也没有任何测试文件（`[审计记录]` 批 4 记录 `:knowledge-production:test` 是空任务）。
- **失败路径**：它自带一套与 Python 生产线无关的历史硬编码模板（`SeniorHighMathContent` / `SeniorHighPhysicsContent`）。读者会误以为它是现行内容生产模块。
- **证据**：`[本轮实测]` 清点代理 grep `settings.gradle.kts` 之外的依赖；`[审计记录]` 批 4 进度条。

---

## F. 契约、门禁与代码卫生层（5 条）

### F-01 · 没有包级清单 / 哈希 【P2】
- **症状**：10 个内容 JSON 里没有任何聚合的描述文件（无 counts、无 hash、无 schemaVersion 汇总）。来源 `sourceFingerprint` **只做格式校验不重算**；材料 `contentFingerprint` 重算比对（这道是好的）。
- **失败路径**：无法从包本身回答"这一版内容是什么"。版本身份靠文件名里的 `v1`/`v2`。
- **证据**：`[本轮实测]` 清点代理确认 resources 目录内无 manifest 文件。

### F-02 · `KNOWLEDGE_QUIZ_CONVERSATION_ID` 定义但无人使用 【P3】
- **症状**：`RoomBackedStudyExperienceRepository.kt:955` 声明后**单次出现、无读取**；实际 id 由 `KnowledgeReviewSessionViewModel` 自拼。`[审计记录]` §13.1 声称它已被移除——**该声称与当前源码不符**。
- **失败路径**：读者按审计记录认为已清理，实际仍在。
- **证据**：`[本轮实测]` 清点代理 grep。

### F-03 · `MasteryWriteGate` 实际是 10 个拒绝分支，不是 7 道 hook 【P2 · 记忆订正】
- **症状**：项目记忆"7 道 hook 门控数值"；实测 `MasteryWriteGate.evaluate` 有 **10 个拒绝分支**（`core/domain/MasteryWriteGate.kt:279-338`），而 spec `docs/specs/2026-09-06-mastery-judgment-gate-evolution.md:78` 描述的是 **8 个有序阶段**。三个数字互不相同。
- **失败路径**：任何按"7 道门"做的验收或覆盖面论证都不成立。
- **证据**：`[本轮实测]` 清点代理逐分支列举。

### F-04 · 审计对 `KnowledgeContextRetriever` 调用点的归属描述需修正 【P3】
- **症状**：研究简报把 `RoomTutorToolRunner.kt:157-163` 列为 `KnowledgeContextRetriever.select` 的调用点；实测**不是**——`select` 在生产中**只被 `RoomMistakeOrganizationRepository` 调用**（第 182、204 行），讲题走的是 `readSubjectKnowledgeRecallCandidates` 这条不同的路径。
- **失败路径**：把两条路径当成一条，会得出"讲题也按题面语义检索"的错误结论。
- **证据**：`[本轮实测]` 清点代理 grep 调用点。

### F-05 · 状态报告只有一种可能取值 【P2 · 来自审计】
- **症状**：`docs/status.md` 的 Overall Status 恒为 `PASS`，且把"没测"（`NOT_MEASURED`）与"没过"读成同一件事；AAB 体积列曾写错。
- **失败路径**：读到 `PASS` 不能推出任何东西。`[审计记录]` N-31 登记为 `P2 · over-reporting`，部分已修。
- **证据**：`[审计记录]` §13.1；`[本轮实测]` 我读 `docs/status.md` 看到的确实是 `**PASS**` 且大量 `NOT_MEASURED`。

---

## G. 合规与来源层（6 条）

### G-01 · 968 条 boundary 含第三方原文摘录 → 与来源政策直接冲突 【P0 · 同 A-09】
- **失败路径**：随 APK 分发即构成分发来源表达。项目政策要求 `REVIEWED_SYNTHESIS_ONLY`（不复制来源表达）。
- **证据**：`[本轮实测]` 门指标；`[技术文档]` `docs/knowledge-source-governance.md`。

### G-02 · 《人工智能生成合成内容标识办法》未纳入项目政策 【P1 · 新增合规项】
- **症状**：该办法 **2025-09-01 已施行**：生成合成内容（含"利用人工智能技术生成、合成的**文本**"）需加**显式标识**（起始/末尾/中间适当位置）与**元数据隐式标识**；且第八条要求**用户服务协议明确说明标识的方法与样式**。
- **失败路径**：知识库的讲解材料若由生成式 AI 参与生成，即落入该办法射程。项目现行的来源政策文件（`knowledge-source-governance.md`）与隐私/数据流文档**均未提及这一条**。
- **证据**：`[外部研究]` 中国网信网官方原文〔官方〕；条文号为第三条/第四条/第五条/第八条/第十四条。

### G-03 · 学科网与智慧教育平台的使用条款取不到正文 【P2】
- **症状**：`basic.smartedu.cn/statement`、`/copyright` 实测返回 **403 + SPA 壳**（12 条路径 + 3 种 chunk 命名全失败）；学科网全站返回混淆 JS 壳，注册协议在登录墙后。
- **失败路径**：无法确认这两个来源是否允许"阅读后独立归纳"。按项目的保守原则应默认按最严处理，但**这一点在来源登记里应记为"条款未读"而不是"已核对"**。
- **证据**：`[外部研究]` 实测 HTTP 状态与响应体。

### G-04 · 曲一线《5·3》没有任何公开条款页 【P2】
- **症状**：官网只有"正版验证/授权网店/纠错平台"；**3 轮关键词检索未找到任何著作权声明或使用条款页**；也未找到曲一线起诉任何平台的著作权判决。
- **失败路径**：五三正是当前内容生产的主要来源。它的授权状态**是未知**，不是"已按 REFERENCE_ONLY 处理就没事"。
- **证据**：`[外部研究]` 检索记录。

### G-05 · 人教社与菁优网条款明确禁止，已核对 【非缺陷 · 记录】
- **事实**：人教社条款 7.4(3)/7.5 禁止复制/衍生/商用，要求书面授权；菁优网服务条款一/七.4(8) 禁止复制发布与抓取。**两家的条款都已被读到原文**，项目按 `REFERENCE_ONLY` 处理是正确处置。
- **证据**：`[外部研究]` 条款页原文。

### G-06 · 中国法上没有 TDM / 训练数据例外 【非缺陷 · 记录】
- **事实**：《著作权法》第 24 条 13 项合理使用中**无**此项；最高人民法院亓蕾文中把"输入端合理使用"明确自述为**立法论建议**；《生成式 AI 服务管理暂行办法》第 7 条要求"合法来源"且"不得侵害知识产权"。截至 2026-09，相关司法解释**尚未出台**。
- **含义**：项目"禁把源文本放进 LLM 提示词"的做法有多重依据支持。

---

## H. 汇总与优先级

### H-1 严重度分布

本册共 **69 条**，其中 **65 条是问题**、4 条是现状/非缺陷记录（E-05、E-06、G-05、G-06）。逐条统计：

| 严重度 | 条数 | 编号 |
|---|---|---|
| **P0** | **9** | A-01, A-02, A-09, A-16, A-18, B-01, B-03, C-01, G-01 |
| P1 | 24 | A-03, A-04, A-05, A-07, A-10, A-11, A-13, A-15, A-17 · B-02, B-05, B-06, B-07b, B-08 · C-05 · D-01, D-03, D-04, D-07 · E-01, E-02, E-03, E-04 · G-02 |
| P2 | 24 | A-06, A-08, A-12, A-14, A-19, A-20, A-21 · B-04, B-07, B-10, B-11, B-12 · C-02, C-03, C-06, C-08 · D-02, D-05 · E-08 · F-01, F-03, F-05 · G-03, G-04 |
| P3 | 8 | B-09 · C-04, C-07 · D-06, D-08 · E-07 · F-02, F-04 |

> 分工说明：**A-09 与 G-01 是同一问题在内容层与合规层各记一次**（968 条 boundary 摘录），施工时按一条处理。
> **命名说明**：P0 是"直接影响学习正确性、合规分发或工程可恢复性"的级别，不是"必须马上做"——例 A-18（绑定正确率 42%）是 P0 但从未被排进任何批次；C-01（生产线未提交）是 P0 但成本极低。**优先级由第 0 节的用户决定与依赖关系决定，不由本表决定。**

### H-2 根因归并：四类问题解释了绝大多数条目

| 根因 | 直接产生的条目 | 说明 |
|---|---|---|
| **① 两张权威表不存在 + 修订未执行** | A-10, A-13, A-15, A-16, C-05 | 不是内容写错，是**定稿动作没做完**。执行生成器投影 + 补两张表，一次性收掉最多条目。 |
| **② 内容没对准消费约束** | A-01, A-02, A-03, A-04, A-19 | 材料总量够（10356 条），但 892 条没绑定、998 个节点没材料、649 个节点超配。**决定 D-2 正对此类**。 |
| **③ 知识库在多个入口没被调用** | E-01, E-02, E-03, E-04, D-07 | 管道通了，但主流入口（大厅、拍照讲题）不查库；一个工具名不符实；一条写入不可达。 |
| **④ 覆盖基线不可复算** | B-01～B-07, B-07b, B-12 | 课标 PDF 缺 6 份、提取未审校、无三方映射、无权威外部总数、**且此前的核对没留证据**。**决定 D-5 正对此类**。 |
| ⑤ 工程连续性与合规 | C-01, G-01, G-02 | 生产线未入版本控制；968 条摘录与政策冲突；一条新合规项未纳入。 |

### H-3 与用户决定的对应

| 决定 | 覆盖条目 | 建议的施工入口（不作为本册结论） |
|---|---|---|
| D-2 补 998 个零材料节点 | A-01 为主，连带 A-02/A-04/A-19/C-08 | 需先确认产能形态（当前是 8 个手写 Python 模块） |
| D-3 前置不留空 | A-15, A-16 | 需先定义"未定"状态与裁定通道 |
| D-4 检索改动靠后 | D-01；前置 D-03/D-04 | 先扩基准 |
| D-5 覆盖口径 | B-01～B-07, B-07b, B-12 | 见 `docs/research/coverage-baseline-design-2026-09-15.md`；**阻塞已解除**——四科 2025 版 PDF 可从镜像重取（本轮实测字节吻合） |
| （无决定但 P0） | C-01 生产线未提交、G-01/A-09 摘录合规 | 待用户裁定 |

---

## 附录：本册的证据来源与未验证项

- **可重跑命令**：见 `docs/research/kb-deep-research-2026-09-15.md` 附录 A。
- **`[审计记录]` 的引用**：均指向 `docs/audit-2026-09-12-kernel-readiness.md` 的具名小节，该文档本身标注了每条 finding 的审计者自评信心（`verified` / `likely`）。
- **未验证项**：
  - 仪器化测试（九科 19 例、查询计划断言、p95 延迟）本轮**未在设备上复跑**。
  - B-11 的 2025 版学业质量水平变化来自**修订组署名解读文章**（单一来源待验证），未经 2025 版 PDF 原文核对。
  - 外部研究受检索通道限制（moe.gov.cn 部分路径不可达、ictr.edu.cn 412 反爬、pep.com.cn 维护页、学科网/知乎 403）。凡未取到的来源，**不代表不存在**。
  - 五三扫描件 → 转录文本这一步未落在版本控制内，本轮无法验证转录保真度。

---

## I. 结构层（2026-09-16 追加 · 由用户判断带出）

> **I 类施工进展（2026-09-16，逐轮提交，全部 roundtrip + :core:data + 工具链验证）**
>
> | 指标 | 起点 | 现在 |
> |---|---|---|
> | 主题名重复父路径 (I-01) | 421 | **0**（c374155a 层内名收敛） |
> | 章层挂点 (I-02/I-06) | 167 | **20**（真知识点全归位；余 20 是"题目节点"，转绑定门） |
> | 星号名 (I-03 关联) | 96 | **0**（a0a5682c 去星） |
> | 知识点总数 | 2573 | **2383**（删 ~190 个题干残渣/碎片/残渣名重复） |
>
> 新增 4 个表驱动转换机制（均独立/幂等/无损/有测试，绕过坏的 build.py，见 C-09）：
> `shorten_topic_names`（层内名）、`relocate_chapter_points`（章层→主题）、
> `rename_points`（坏名→知识点名，只改 name 不动 slug）、`delete_points`（删残渣，
> 自动清 chapter_map/alias_map 悬空引用）。
>
> **核心结论（I-03/I-04 的深化）**：生物"材料密度 9.9/节点"不是"节点太粗要拆"，
> 而是**绑定错了**——`光合作用` 一个节点挂着 97 条材料，横跨光合/呼吸/神经内分泌/
> 细胞基础/生态/免疫。实测 53 个肥节点上的 1933 条材料里：898 条标题命中其他已有
> 生物节点（应重绑过去）、48 条只命中自身、987 条不命中任何节点（生物只有 338 个节点，
> 缺大量应有节点，需新建）。**所以"生物粗节点拆分" = "绑定重建 + 建缺失节点"**，
> 是同一个 Phase 5 绑定门工作，不是独立的结构手术。

### I-01 · topic 的 name 是路径字符串，不是层内名 【P1 · 已解决 c374155a】

> **来源**：用户在得知"单节点最多 97 条材料"后指出——"如果单个节点普遍太大，那么就说明数据的结构还有问题，渐进式披露的结构还不清晰"。以下为该判断的实测验证。**验证过程中我两次给出错误结论（先说"树是平的"、又说"错绑不存在"），两次都是我自己用错口径，订正过程记在每条里。**

### I-01 · topic 的 name 是路径字符串，不是层内名 【P1】
- **症状**：每一层的 `name` 都重复父名的全文。实测冗余占比：

  | 科 | 层 | 样本名 | 名字长度 | 继承父名 | 冗余 |
  |---|---|---|---|---|---|
  | PHYSICS | L2 | `物理必修第一册·第一章` | 11 | 7 | 64% |
  | PHYSICS | L4 | `物理必修第一册·第一章·运动的描述·1 质点 参考系` | 26 | 17 | 65% |
  | CHEMISTRY | L4 | `化学必修第一册·第一章·物质分类与转化·物质的分类及转化` | 28 | 19 | 68% |
  | BIOLOGY | L4 | `生物学必修1·第一章·细胞与生命活动·细胞核的结构和功能` | 28 | 18 | 64% |

- **失败路径**：任何逐层展开的界面都会把同一段文字重复 4 遍；名字本身不携带该层独有的信息，因此"渐进式披露"在数据层就不可表达。
- **证据**：`[本轮实测]` 逐层解析 name 与前缀继承长度（445 个 topic 全量）。
- **处置（已解决 c374155a）**：`tools/kb_build/shorten_topic_names.py` 把每个 topic 的 name 还原成层内名（`slug` 一个字节未动），门指标 `topic_name_carries_path` 421 → 0，roundtrip 7/7 仍绿。残留的「综合」退化链归 I-06/章表修复。

### I-02 · 知识点挂在哪一层，四科各不相同 【P0】
- **症状**（实测，按 `parentSlug` 解析真实深度）：

  | 科 | L1 | L2 | L3 | L4 | 知识点横跨 |
  |---|---|---|---|---|---|
  | MATH | 0 | 0 | **406** | — | **1 个层** |
  | PHYSICS | 0 | 0 | **531** | 201 | 2 个层 |
  | CHEMISTRY | 0 | **157** | 748 | 192 | **3 个层** |
  | BIOLOGY | 0 | 10 | 167 | 161 | **3 个层** |

- **失败路径**：**"原子知识点"在不同地方指的不是同一件事**——数学的 L3 是主题级、化学的 L2 是章级。于是"一个知识点挂几条材料算正常"没有统一答案，粒度规范（`kb-content-conformance-spec.md` §4.3「一个原子节点的一个面」）无法被执行。化学有 **157 个知识点直接挂在"章"这一层**，那正是"章被当成知识点用"。
- **证据**：`[本轮实测]` 全量 2573 个知识点按深度归位。
- **订正记录**：本条初版我用了 `children` 键判定层级，得出"树是平的、四科最大深度都是 1"——**那是错的**，pack 用 `parentSlug` 表达层级。重新按 `parentSlug` 解析后是上表（最大深度 3–4）。错误结论未进入任何施工。

### I-03 · 材料密度在生物一科异常 【P0】
- **症状**（实测）：

  | 科 | 节点 | 材料 | 材料/节点 | >20 条的节点 |
  |---|---|---|---|---|
  | MATH | 406 | 1767 | 4.4 | 5（1.2%） |
  | PHYSICS | 732 | 2237 | 3.1 | 8（1.1%） |
  | CHEMISTRY | 1097 | 2124 | 1.9 | 1（0.1%） |
  | **BIOLOGY** | **338** | **3336** | **9.9** | **53（15.7%）** |

- **失败路径**：生物用四分之一的节点数承载了三分之一的材料。前 15 个最肥节点**全部是生物**，且名字本身就暴露了颗粒度：`光合作用与细胞呼吸过程、物质和能量的关系`（97 条）是两个完整过程加它们的关系；`实验：检测生物组织中的糖类、脂肪和蛋白质`（55 条）是三个不同实验。**它们是被当成"原子点"的"节"。**
- **证据**：`[本轮实测]` 全量材料按节点聚合。
- **含义**：I-01/I-02 是全局的层级语义问题，本条是它在生物上的后果——**不是"普遍太大"**，数学/物理/化学的密度是健康的。

### I-04 · 已核实的具体错绑样例 【P0 · 同 A-18】
- **症状**：单条 PRIMARY 绑定到语义无关的节点：

  | 材料 | 绑定到的节点 |
  |---|---|
  | 下丘脑是血糖调节中枢，通过神经作用于胰岛 A/B 细胞 | 光合作用与细胞呼吸过程、物质和能量的关系 |
  | 细胞壁是全透性的，对水、溶质均无选择透过作用 | 光合作用与细胞呼吸过程、物质和能量的关系 |
  | 染色质与染色体的关系 | 光合作用与细胞呼吸过程、物质和能量的关系 |
  | tRNA的结构与功能（反密码子） | 细胞膜的结构和功能 |
  | 脂肪的结构与功能 | 细胞膜的结构和功能 |
  | 肿瘤免疫逃逸：PD-L1 与 PD-1 结合 | 细胞膜的结构和功能 |

- **证据**：`[本轮实测]` 直接读取这 6 条材料的全部绑定（均为 single PRIMARY，无多绑解释）。

### I-05 · 绑定正确率**无法**用字符串度量（订正 A-18 的可用口径）【P1】
- **症状**：我先后用两个词面口径测量 9464 条绑定，得到两个互相矛盾的数：
  - **宽松口径**（二元组重合）：98.1% 命中 —— **虚高**。`下丘脑…胰岛 A 细胞` 里的"细胞"与节点名 `光合作用与细胞呼吸…` 里的"细胞"对上了，通用词污染。
  - **严格口径**（标题/节点名互为子串）：16.4% 命中 —— **虚低**。`离子方程式正误判断之一看：是否符合客观事实` → `离子方程式的正误判断方法` 是**正确**绑定，只是标题用了"之一看"的面相写法，不含节点名。
- **失败路径**：材料标题是**面相式**写法（`之一看：…`、`曲线先降后升→…`），刻意不重复节点名——这是好的内容写法，但它使**词面匹配既不能验证也不能生成正确绑定**。任何用字符串规则做的绑定门都会给出无效数字。
- **证据**：`[本轮实测]` 两个口径各自的全量结果与反例（各 3 条，打印在测试输出里）。
- **含义**：**Phase 5 的绑定门必须是语义判定**（模型判定 + 人工抽样复核），不能是正则或子串检查。规范 §4.2 里那个"42% 完全一致"来自人工抽样，正则复现不了它，也不该试图复现。

### I-06 · 章层直接挂了 167 个知识点（规范 §2.1 违规）【P1】
- **层级职责定义**（规范 §2.1，本条据以判定）：`科 → 册 → 章 → 主题 → 子主题 → 知识点`，其中**册与章只作空壳分组、不挂知识点**，知识点只允许挂在**主题或子主题**层。
- **症状**：深度按 `parentSlug` 计（册=0、章=1、主题=2、子主题=3），**章层（深度 1）直接挂了 167 个知识点**。分布：

  | 章 | 科 | 点数 | 所在 |
  |---|---|---|---|
  | 有机化学基础 | CHEMISTRY | 108 | 选择性必修3 |
  | 物质结构与性质 | CHEMISTRY | 37 | 选择性必修2 |
  | 实验与方案设计 | CHEMISTRY | 12 | `CHEMISTRY·综合` 桶下（见 I-07） |
  | 生物技术与工程 | BIOLOGY | 10 | 选择性必修3 |

  形态：这些章**既有直接挂的点、又有主题子层**（如 `物质结构与性质` 章下既有 37 个点，又有 `共价键`/`分子的空间结构` 等子主题）——**点是没归位到子主题的残留**。
- **为什么不能机械下移**：每个点该归到哪个主题需要**语义判断**（见 I-05：词面匹配已证伪）。用脚本强行下移会制造新的错绑。因此本条**只立门、不定规则**——门指标 `chapter_layer_has_points`（=167）让它可见、可跟踪，归位列为待语义裁定的提案（I-05 的语义判定通道）。
- **证据**：`[本轮实测]` 全量 445 topic 按 `parentSlug` 深度归位；门指标 167。
- **对照**：MATH 与 PHYSICS 章层为 0 点，干净；只有 CHEMISTRY/BIOLOGY 违规——这与 I-03（材料密度只在生物异常）指向同一科系的选择性必修册建设不完整。

> **Phase 2 管线进展（2026-09-18 夜，a451348d / 403e210d / 57dc9fa3）**
>
> - 覆盖台账 v2 建成：四科 661 条 2025 课标候选全判定（UNDECIDED=0）。COVERED：数学 118/308、物理 71/138、化学 51/73、生物 100/142（`kb-coverage-alignment-2026-v2.json`，判定者显式标 AI）。
> - 九科课标 PDF 指纹全部逐字节核验（6 重下 + 3 在盘）；提取管线重跑成功（1300 候选）。
> - 提取状态机上线：10482 文件账本（PENDING 9418 / SCANNED 403 / STRUCTURED 250 / MEDIA 43 / SKIPPED 411），迁移守卫 + output_ref 悬空门。
> - Office 直抽器 + 材料入库器上线（heading 感知切块、CJK 指纹去重、sidecar 索引化卷滚动）。
> - 判定入库主循环 8 批：累计 80 条材料、15 个新节点（数学集合/统计/函数模型/概率 + 化学 14 个基础节点 + 生物 10 个核心节点等），点 2309、节点 2771、材料 10436、unbound_points 701→657。
> - 全量直抽完成：9325 文件扫描，9309 成功 / 16 ERROR（4 legacy .doc 无解析器 + 12 损坏 docx：zip 缺件 10、XML 标签错配 1、包损坏 1），去重 32435，共 **171749 块**（数 55434/物 53080/化 35424/生 26407/未归属 1404），JSONL 258MB 已 gitignore（可确定性重生成，权威状态在 extraction_state.csv）。判定入库按"台账缺口优先"逐轮推进。


> **Phase 3/4 进展（2026-09-18 深夜—09-19，c100a15a→1f1c3ce3）**
>
> - **unbound_points 清零**：2115 个知识点全部有材料；**bad_names 382→0**（116 残渣删 + 74 句名收敛 + 26 同物合并 + 4 题干壳删）。材料总数 10938→11946（含五三 225 条、直抽 357 条等）。
> - **五三精讲册收口**：四科精讲册 743 页已渲染（`build/wusan-render/`，1839×2600 JPG）；视觉转写代理 5 波共 **185 条材料**入库（含此前跳过的 4 个语料空白节点：油脂/醚/饱和汽/饱和一元醛酮通式）；`knowledge-production/wusan/materials.jsonl` 累计 319 条，17 处悬空绑定全修（新建 11 个真缺节点：同素异形体/游离态/化合态/纯净物/混合物/单质/化合物/离子化合物/共价化合物/化学与传统文化/第一次人工核反应）。
> - **Phase 3 计量（2026-09-19 真机补测）**：debug APK 142.26MB（含调试信息）；知识资源 16.5MB 原始 / 3.3MB 压缩，最大单卷 2.80MB。**冷启实测（emulator-5554/API34，导入完成态，am start -W）2.897s**；首启导入路径（10,412 条材料）在安装后 25s 内完成并出主界面。单资源 12M 字符门与导入总量上限均已按用户决定取消（见 KD-15）。
> - **新发现（工具/映射缺陷，登记）**：`wusan_page_points.csv` 的 pdf_page↔科目/页码对照不可靠——物理册前 17 页被标为其他科、各册页码偏移不同（数 −4、生 −4/−5、化 −2）、p0023 一带有一行错位。转写页号一律以渲染图号为准（note 已标 `P<图号>`）。另一处：`office_extract.py` 成功不回写状态，导致 extraction_state 的 PENDING 同时混着"未切块"（真 1232 份）与"已切块未判定"（7864 份）。

### I-08 · 生物必修1「细胞与生命活动」是整册综合主题 + 全册约 15 处跨章错挂/空重复主题 【P0 · 2026-09-18 登记并收口（余 5 个空壳主题无删除机制）】
- **来源**：修细胞器肥节点（74 材料）时发现——该节点挂在 `生物学必修1·第一章·细胞与生命活动·细胞器之间的分工合作`，而 `生物学必修1·第三章·细胞结构` 下已有同名主题。
- **订正**：初判"章层挂了 15 点"是错的——`细胞与生命活动` 是深度 2 的**主题**（挂点合法），章层门（=20，化学17+生物3）不受影响。真问题是该主题**吸收了必修1 全册约 30 个别的章的散点**（元素化合物→第二章、细胞膜/细胞器/细胞核/生物膜→第三章、呼吸→第五章第三节、光合→第五章第四节、周期/死亡/分化/有丝分裂→第六章）。
- **处置（2026-09-18 已完成第一步）**：`chapter_point_relocation.csv` 追加 36 行跨章归位（`to_topic_slug` 指向目标章，主题名在章内查找），点数 2301 守恒、slug 集合不变、章层门 20→20，:core:data 绿。
- **残余（2026-09-18 已合并 6 对）**：`merge_points` 消解 `细胞器`↔`细胞器的结构和功能`、`细胞核`↔`细胞核的结构和功能`、`细胞膜`↔`细胞膜的结构和功能`、`对细胞膜的探究历程`↔`细胞膜成分和结构的探究历程`、`生物膜系统及其功能`↔`细胞的生物膜系统`、`膜的流动镶嵌模型`↔`流动镶嵌模型`（跨主题）；点 2301→2295、材料 10356 恒定。合并暴露两个缺陷并已修：merge 前置并集会把占位点的垃圾边带给存活节点（新增 `drop_prereq` 列丢弃 2 条）；relocate 只查章直接子级、找不到深层主题就建 bare-slug 浅壳（同册出现 7 个同名重复分支，已并入深层主题 + 工具改子树深查找 + 2 个机制测试）。
- **其他章错挂散点（2026-09-18 已收口 4 点）**：选1·体液调节→`种群数量的变化` 主题下实为`反馈调节`（前置反射弧，属神经调节）→ 选1·第二章·神经调节；选3→`DNA是主要的遗传物质` 主题下实为`DNA连接酶`（基因工程工具酶，主题名错）→ 选3·基因工程的基本操作程序；`协同进化与生物多样性的形成`→ 必修2·第五六章·种群基因组成的变化与物种的形成；`生物有共同祖先的证据`→ 必修2·第五六章·变异与进化。**订正**：初判"选1 两主题各挂一条`体液调节与神经调节的关系`"是误读——那是两个主题同名，主题下的点名不同（`体液调节和神经调节的区别与联系`/`激素调节的特点`，各自归位正确），无需动作。
- **残余空壳主题（无删除机制，gate 容忍，仅登记）**：必修2·第一章→`人类遗传病`(0)；必修1·第四章→`DNA是主要的遗传物质`(0)；选2·第三章·生态系统→`生物多样性及其保护`(0)；必修1·第一章→`减数分裂和受精作用`(0)；选3→`DNA是主要的遗传物质`(0，散点移走后空)。
- **附带**：材料 `bio-kb3-f3-s3-2-n22`（"在成人心肌细胞中数量显著多于"）标题截断且 body 空，无法判单细胞器归属，暂留父节点，Phase 2 重抽时补全。

### I-07 · 9 个「综合」桶 topic 挂 105 个无章归属的知识点 【P1】
- **症状**：`MATH·综合`/`PHYSICS·综合`/`CHEMISTRY·综合`/`BIOLOGY·综合` 这类 topic（名字含 `综合` 段）是**没有章归属**的知识点容器，共 9 个 topic、105 个知识点。规范 §2.1 规定跨册内容统一叫 `跨册综合`，而**这个名字一个都没出现**——说明这些不是"合法的跨册归并"，是章表匹配事故（`chapter_by_node.csv` 里 `物理其余`/噪声 book 值）的残留。
- **失败路径**：这些知识点在逐层展开时挂在"综合"这个无意义分组下，既无章、无主题语义，也无法参与"按章补材料"（wusan_plan）的队列。
- **证据**：`[本轮实测]` 全量 topic 名含 `综合` 段的计数（9 topic / 105 点）；`test_kb_shorten_topic_names.py::test_unassigned_buckets_stays_visible` 钉住该数。
- **归属**：章表修复（`regroup_aggregates.py` / `chapter_map.csv`），与 I-06 同源。

## J. 视觉转写轮次（2026-09-19 · 解题觉醒大招册 951 条入库）

### J-01 · 扫描件"是否已处理"过去只靠记忆，导致重复劳动 【P0 · 已收口 b7861e00】

**现象.** 五三精讲册整本 743 页已转写完，其 4 个 PDF 仍挂在 `PENDING_SCANNED`；
五三 A版【01】的分节精讲册（56 个文件）与整本精讲册同页，若按文件名判断会再扫一遍。

**处置.** 新增 `tools/kb_coverage/tables/scan_decisions.csv`：407 个扫描 PDF 逐文件给
决定（TRANSCRIBED / DUPLICATE_OF_BOOK / TRANSCRIBE_THIS_ROUND / TRANSCRIBE_LATER /
SKIP_EXERCISES / SKIP_ANSWERS / PENDING_UNCLASSIFIED）+ 规则名 + 理由；
重复判定用页图网格逐页比对（判据脚本 `dedupe_scans.py`），不靠文件名。
已隔离 60 个文件 / 2.4GB 到 `<源根>/zz-已扫描隔离/`，脚本可 `--undo` 原样搬回。

**遗留.** `PENDING_UNCLASSIFIED` 7 个（数学 1 页讲义 ×2、课堂笔记 1 页等），下轮判；
物理按章"讲册PDF"（266 页）实测与整本不是同一版式，未判重复，排后续轮次。

### J-02 · 节点 kind 越界：Python 工具允许 App 契约里不存在的枚举 【P2 · 已修 b7861e00】

**现象.** `create_points.py` 的 KINDS 含 `MISCONCEPTION_GUIDE`，但 App 的
`KnowledgeNodeKind`（TOPIC/CONCEPT/PROCEDURE/REASONING/REPRESENTATION/EXPERIMENT/EXPRESSION）
没有它 —— 那是**材料类型**的枚举。本轮 2 个新节点因此越界，被
`BundledTeachingMaterialsContractTest` 抓住（7 条用例连锁变红）。

**处置.** 包内 2 个节点改 `REASONING`；**未**收紧 Python 侧 KINDS —— 这是漏修，
记在此处：应把 `create_points.KINDS` 改成 App 契约的 7 值集合，
否则同一类越界还会由 agent 生成的表再次引入（Kotlin 用例只会在最后一刻抓住）。

### J-03 · 来源时间戳多卷不一致（KD-19）【P1 · 已修 b7861e00】

见 `docs/known-defects.md` KD-19：写入 2 批后 284 条材料"早于来源导入"，
根因是同一 sourceId 在不同卷各自盖 writing-time 时间戳。已修工具（复用最早条目）+ 数据。

### J-04 · 新增 149 个节点的归属策略（需下一轮复核） 【P2 · open】

**做法.** 跨章节的方法/思维类节点统一落在各科既有 `·综合·综合·综合` 主题
（PHYSICS 59 → 71 点、BIOLOGY 3 → 30 点等）；教材内概念落到对应章节主题
（107 条补了 chapter_map 归属）。sourceLocator 统一 `《解题觉醒》名师大招册（2027版）视觉转写`，
来源单元表新增一行 decision=`keep_per_node`（这些节点逐条自带归属，不参与单元级推断）。

**待复核.** 数学 97 个方法节点里有约 30 个（如 数形结合找临界 / 分类讨论法 / 隐函数的求导）
按关键词落到了"函数的概念与性质/函数与导数"两个主题，逐条看应当更细；
下一轮用 `relocate_chapter_points` 做一次主题级归位，并复核 综合 桶是否过胖。

### J-05 · 别名污染实况（复核 I-04/A-18 的样本） 【P1 · open】

本轮读包时发现 `数学必修第一册·第三章·函数的概念与性质` 里「函数的概念」的 aliases 含
`导数的概念与导函数`、`排列与组合的概念及公式`、`绝对值型函数的处理方式` —— 与节点名无关。
这与门指标 `幽灵别名（与当前绑定脱节）920` 是同一批历史遗留；绑定正确率优先，需专轮清理。

## K. 五三 B版/讲册/讲义/主书轮（2026-09-19 · 30 代理 · 3,609 条材料）

### K-01 · 单代理 100+ 页会丢尾：30 个代理里 6 个未读完分配范围 【P1 · 本轮已补跑】

**现象.** 按用户"一次开 30 个都行"授权，把 3,221 页按 97–122 页分给 30 个代理。
6 个代理在上下文耗尽后停止：bbio_1 缺 p102–122、bbio_4 缺 p462–486、bchem_4 缺 p418–465、
bmath_6 缺 p591–660、bbio_3 缺 40 页、phyjc_1 缺 28–133。其中 3 个连汇报都没返回。

**处置.** 用覆盖率差集（页图 chunk 是否出现在产出行里）算出未读页，派 7 个补跑代理
（`.agent_b_*x.csv`），补出 531 行。**教训**：60–80 页/代理是安全区间；
过百页必须让代理分批落盘并在汇报里报"未读页"，否则缺口只能靠事后差集发现。

### K-02 · "跳过判定"抽查：13 页里 1 页误跳 【P2 · 已修】

**做法.** 派独立代理抽 13 个被别的代理判为"纯题目/答案页"的页面重看图，
要求逐页回答"是否含知识框"。结果：12 页确为纯题/答案（其中 2 页的知识框已被相邻行覆盖），
**1 页误跳**（bmath p0640 的"归纳总结 常见的隐圆类型"），已补 1 行。
抽查同时暴露一个整块缺口（`bc0b63ec32` p632–647 零覆盖），事后确认是 bmath_6 未读完所致，
已被 bmath_6x 补上（21 行）。

### K-03 · 契约级控制字符：门与 App 不同源（见 KD-20） 【P2 · 数据已修、门未同源】

### K-04 · 本批指标与 pin 变化 【P2 · 已收口】

- 材料 12,253 → **15,862**；知识点 2,264 → **2,443**（新增 179 个方法/概念节点）。
- 门指标：`unbound_points 1→0`（最后一个零材料点拿到材料）、`ghost_aliases 920→919`；
  其余与上轮逐项一致（892/1246/677/581/194/80/1690/2/12）。
- 修数据（不靠改门）：10 条 LaTeX 受损材料、13 条带 \t\r 材料、3 个 C1 控制符字段；
  并补 `\otimes/\oplus/\ominus/\odot/...` 进 `_REAL_LATEX_COMMANDS`
  （`otimes` 以 `imes` 结尾，会被"`\times` 丢反斜杠"的模式误伤）。

### K-05 · 407 个扫描件全部有了处置决定（PENDING_UNCLASSIFIED 归零）【P2 · 已收口】

**做法.** 把 7 条待判定逐条落地：其中 6 条其实**上一轮已经转写**（4 个生物"图文版知识清单" +
2 个数学单页讲义，见第 10 号代理的 `.agent_jjdx_misc.csv`，合计 56 行材料），只是决定表没回头看；
最后 1 条 `第五章　平面向量、复数/课堂笔记.pdf` 渲染后人工看图，整页是
**微信资料群二维码广告**（"资料共享/最牛鸡娃教育资料共享群"），零知识内容 → 判 `SKIP_NO_CONTENT`。

**结论.** 407 个扫描 PDF 现在分布为：TRANSCRIBE_LATER 227、SKIP_EXERCISES 100、
DUPLICATE_OF_BOOK 56、TRANSCRIBED 10、TRANSCRIBE_THIS_ROUND 9（本轮批次，已随批次做成 TRANSCRIBED）、
SKIP_ANSWERS 5、SKIP_NO_CONTENT 1，**无待判定**。

### K-06 · 真机（模拟器）冒烟：15,862 条内置材料导入成功、无横幅 【P1 · 已实测 2026-09-19】

**命令与结果**（emulator-5554 / API34，设备时钟比主机慢 8 小时）：

```bash
./gradlew :app:assembleLocalFirstDebug                     # BUILD SUCCESSFUL（APK 143,021,822 字节，09:39）
adb install -r app/build/outputs/apk/localFirst/debug/...  # Success
adb shell pm clear com.tingyun.smartmistakebook.localfirst # Success（强制首启导入）
adb shell am start -W -n .../MainActivity                  # COLD，TotalTime 3660ms
adb shell run-as ... sqlite3 .../smart-mistake-book.db "select count(*) from knowledge_teaching_material;"
```

| 查询 | 设备值 | 与包对照 |
|---|---|---|
| `knowledge_teaching_material` | **14,972** | 包内 15,862 − 892 条无绑定残渣 = 14,970（±2，聚合层剔除口径）|
| `knowledge_node` | **2,933** | 包内 2,905（462 topic + 2,443 point）+ 2020 旧包节点 |
| `knowledge_source` | 58 | 6 卷来源去重后 |

**副作用与结论.** `logcat` 里没有 `FATAL` / `AndroidRuntime` / `DatabaseContractViolation` /
知识包导入失败记录；截图（`build/device-smoke-0919.png`）显示首页正常、**无启动横幅**。
KD-15 那类"整批被拒 → 横幅"的失败在 15,862 条规模下不复现。

## L. J-04/J-05 收口轮（2026-09-19 · 节点归位 + 别名重建）

### J-04 · 新节点主题归位 【P2 · 已收口 2026-09-19】

**做法.** 新建表驱动工具 `tools/kb_build/relocate_points.py`（`point_relocation.csv` 114 行、
`new_topics.csv` 1 行），把两轮视觉转写里"落点偏粗"的方法类节点移进对应章主题：

| 落点 | 数量 | 说明 |
|---|---|---|
| 物理必修第三册·第十一十二章·恒定电流 | 14 | 电路/电表/测电阻类 |
| 物理必修第三册·第十三章…·磁场与电磁感应 | 11 | 左手定则/回旋加速器/感应电荷量 |
| 物理必修第一册·第三章·相互作用 | 8 | 活结死结/摩擦角/力的合成实验 |
| 物理必修第二册·第七章·万有引力与宇宙航行 | 8 | 双星四星/卫星比较/轨道变化 |
| 物理必修第二册·第八章·机械能与能量守恒 | 7 | 变力做功/机械能守恒实验 |
| 物理必修第一册·第二章·匀变速直线运动 / 第四章·牛顿运动定律 / 第二册·第五六章·抛体运动与圆周运动 | 5/5/5 | 运动学、动力学、曲线运动 |
| 物理选择性必修第一册·第一章·动量 / 第四册·光学 / 第三册·热学 / 交变电流 | 5/2/4/2 | 碰撞爆炸、折射率与双缝、油膜法与气体定律、变压器与输电 |
| 生物 9 | — | 电子传递→细胞呼吸、减数分裂异常→遗传的细胞基础、启动子辨析→基因工程 等 |
| 化学 8 | — | 能源类型→反应与能量、化学与社会 4 条、食品添加剂→基本营养物质、晶胞投影→晶体常识、实验分析 2 条→新建主题层 |
| 数学 16 | — | 指对处理→指数对数、3 条→计数原理、2 条→三角函数、欧拉线/动点→解析几何 等 |
| 留"综合"桶 | 约 130 | **跨章通法与"知识体系"类**（三角函数法/量纲法/图像法/各科知识体系），语义上就该在跨册综合 |

**结构配套.** 数学原本没有综合桶，本次按其它三科同形补 `MATH` → `MATH·综合` → `MATH·综合·综合`
→ `MATH·综合·综合·综合`；化学补主题层容器 `CHEMISTRY·综合·实验与方案设计·实验分析`
（章层不能挂点）。工具对"目标主题必填、叶子不自动建"设了硬约束，避免表里写错名字长出垃圾主题。

**验证.** 知识点总数守恒 2,443；主题 462→468；章表同步（14 改 2 删）；
`chapter_no_book` 0、`chapter_layer_has_points` 12、`chapter_locator_mismatch` 1,690 —— 与改动前一致。
工具自带 14 条用例（移动/边界改写/守恒/幂等/未知目标与未知点报错/建主题父链/表校验/章表同步）。

### J-05 · 别名污染 【P1 · 已收口 2026-09-19】

**影响面（为什么要修）.** 别名不只是元数据，它是三处的输入：
`KnowledgeContextRetriever`（别名重合权重 7、精确短语加成 70）、
`RoomMistakeOrganizationRepository`（`aliases + canonicalName` 匹配学生文字）、
`OpenAiProblemOrganizationProtocol`（把别名清单发给模型选点）。指向别的概念＝把匹配引错。

**新判据（`tools/kb_build/rebuild_aliases.py`）**：主名永远保留；其它别名必须是
**该节点自己绑定材料的标题**且形状合格（≤24 字、无逗号/句末标点、非题面开头、虚词结尾、
括号配对、无 LaTeX 定界符、过坏名检测）；同科内**全局互斥**（一个名字只归一个节点）。

**结果**：幽灵别名 **919 → 0**；新增 `alias_collision` 指标（别名与同学科另一节点主名相同）
**0**；别名 7,853 → 15,223；丢弃候选 1,161 条（逐条原因见 `tables/alias_dropped.csv`，
其中 240+39 条是"被多个节点同时 claim"的歧义候选、85 条是撞其它节点主名）。

**检索回归（同一套 20 题，按科隔离）**：

| 指标 | 改前 | 改后 |
|---|---|---|
| Recall@5 | 0.90 (18/20) | **0.95 (19/20)** |
| MRR | 0.842 | **0.900** |
| Precision@5 | 0.72 | 0.72 |

**剩余未命中（如实记录，不做面向结果的补丁）**：化学「苯的分子式和结构」仍不中——
该节点在包内只有 1 条材料（标题《苯的结构与性质要点》，不含"分子式"），
属**内容缺口**而非别名问题；补法是在后续轮次为芳香烃章补材料，不是给评测集打补丁。

### L-01 · 父链修复规则过激，清掉了"跨分支挂接"的 17 个父级 【P1 · 已修】

**现象.** 新写的 `fix_topic_parents.py` 第一版按"父必须等于 slug 前缀"判定，于是把
`烷烃`、`醇`、`脂肪烃`、`酚`、`醛和酮`、`酯和油脂`、`胺和酰胺`、`氨基酸`、`分子间作用力`、
`晶胞与均摊法`、`晶体的性质`、`化学实验基础`、`物质的分离与提纯`、`铝及其化合物`、
`用高倍显微镜观察细胞` 等 17 个主题的父级（原本有意挂在教材章下）清成了 None。

**怎么发现的.** `test_kb_relocate_chapter_points.py::test_relocation_is_idempotent_on_shipped`
突然报"重放要再移 46 个"——那条用例正是为"重放不幂等"设的哨兵。

**修复.** 与 HEAD 逐条比对，还原全部 17 个父级；判定规则改成
**"只补缺失/修悬空，绝不删已有且有效的父链"**（已有且指向真实主题 → 原样保留）。

**教训（写进工具文档）.**
1. 批量结构修复必须有"对成品重放幂等"的回归测试，否则自伤只在很久之后以别的形式暴露；
2. 规则要能回答"为什么保留"：只按 slug 前缀推断父级，等于假设"父必是路径前缀"，
   而这份树里存在合法的跨分支挂接（`化学实验基础` 挂在实验章下）。

### L-02 · 别名取证反查错绑：96 条嫌疑 → 改绑 92 条 + 合并 9 条同物重复 【P0 · 已收口 2026-09-19】

**怎么发现的.** J-05 的别名重建规则是"别名必须是本节点绑定材料的标题"，撞车时丢弃候选。
逐条复核被丢弃的候选时发现一类不是脏数据而是**错绑**：
- 标题《波的图像》绑在「电磁感应中的图像问题」；《二次免疫：记忆细胞快速增殖分化…》绑在「细胞的分化与全能性」；
- 《二项式系数的性质》绑在「函数的对称性」（3 条）；《方差与标准差》绑在「平面向量的线性运算」；
- 《油膜法估测油酸分子的大小》绑在「物体看作质点的条件」；《轿量》绑在「速度定义」…（共 96 条）

**工具.** 新增 `tools/kb_build/audit_bindings_by_alias.py`：材料标题恰好等于**同学科另一节点主名**
且当前绑定不是它 → 出嫌疑表 `tables/binding_suspects.csv`；**只出表不自动改**，
因为"上位概念节点收下位材料"（《自由落体运动》绑在「匀变速直线运动」下）是合法的。

**裁定与处置（逐条人工判）**：96 条里 92 条改绑（`material_rebind.csv` +92 行，走 `rebind_materials`）；
4 条保留（「牛顿第二定律的理解：五性」这类上位/特化关系，标题泛指、留在特化节点合法）。

**连锁效果（这才是价值所在）**：改绑后 11 个知识点露出"零材料"——它们此前是被**错绑材料假装覆盖**着。
其中 9 个是**同物重复/残渣节点**，已按 `point_merge.csv`（+9 行）合并：
`递推数列→数列的递推公式`、`数列中的公共项问题→数列的公共项问题`、`运动分解-核心思想→运动的合成与分解`、
`路端电压→路端电压与外电阻的关系`、`表达式-e-n-n为线圈匝数→法拉第电磁感应定律`、
`规律的用途-利用天然放射性元素的半衰期…→半衰期`、`原子核→原子核的组成`、
`制作简单的燃料电池→燃料电池`、`主要因素→影响化学反应的速率因素`。
剩 2 个是**真内容缺口**（MATH「由线、面关系误解向量关系」、CHEMISTRY「自然资源的开发利用」），
如实留在 `unbound_points=2`，列入下一批转写的补料清单。

**门指标变化（全部向好或如实记录）**：点数 2443→2434（合并 9 条）、
`boundary_excerpt` 677→676、`locator_boundary` 581→579、`chapter_locator_mismatch` 1690→1683、
`unbound_points` 0→2（真实缺口，不是回归）、`ghost_aliases` 0、`alias_collision` 0。

**检索不受损**：改绑 + 合并后重跑四科基准，Recall@5 = 0.95 (19/20)、MRR = 0.900、Precision@5 = 0.72，
与改绑前一致。

---

## M. 别名上限与 topic 顺序轮（2026-09-19 · 三处缺陷）

起因是两处内容缺陷（25 个节点别名 >32、4 个 topic 排在父级之前）。逐条查证后，
**它们都不是内容缺陷，而是代码/工具的缺陷**——内容只是被拒被藏；同时查出一个此前没登记的 P0。

### M-01 · `MAX_ALIASES = 32` 让 25 个知识点进不了任何用户的库 【P0 · 已修】

**症状.** `KnowledgeBaseImportContract.validateAliases` 有一条 `require(aliases.size <= 32)`。
实测成品有 **25 个节点**越线（BIOLOGY 23 / MATH 1 / PHYSICS 1，最多的是生物「基因工程」84 条），
于是它们在调和时被逐条跳过（`problemWith` → `skipped`），**只在 `content_install_state` 里留一行**，
学生永远看不到这 25 个知识点。

**它为什么是缺陷而不是安全边界.** 这条上限**没有依据**：无 KDoc、无具名常量、不从任何预算推导。
真正的预算是 `KnowledgeSearchFeatureExtractor` 的 **16 个片段 / 192 个特征**，而它**与别名条数无关**——
84 条别名与 39 条别名都只产出 16 个片段。全仓检索确认：没有测试、文档、门或审计脚本依赖 32。

**修复.** 删除该上限（保留逐条约束：每条 ≤200 字、去空白、大小写不敏感唯一）。
**这不是"修别名膨胀"**——不削减任何别名，只是不再因为条数把整条节点拒掉。

**证据（红/绿都跑了）.** 新增 `BundledKnowledgeNodesContractTest`：用调和器判跳的同一个函数
`KnowledgeBaseImportContract.problemWith`、同一份 `byId`，逐条过内置包的全部节点，要求零拒绝。
把上限临时加回去 → **FAILED**，报出的正是这 25 个节点（`math:三角形面积公式`、`biology:细胞学说`…）；
删掉 → 绿。

### M-02 · `KnowledgeBaseNodeContext.MAX_ALIASES = 8` 静默藏起 538 个节点 【P0 · 已修】

**这一条是本轮新发现的，此前没有登记。** 与 M-01 是同一类错误的另一个出口：

`KnowledgeBaseNodeContext` 有 `require(aliases.size <= 8)`，而适配函数
`toKnowledgeBaseContext` 用 `runCatching{…}.getOrNull()` 包着它、调用处再 `mapNotNull` ——
**9 条以上别名的节点静默变成 null**，从"整理模型"的候选菜单里消失，**无日志、无计数**。实测 **538 个节点**受影响。

**方向最坏**：能进候选菜单说明它本来最相关，而**被藏起来的恰恰是材料最多的那些节点**
（`基因工程` 86 条材料 → 84 条别名 → 永远到不了模型）。

**修复.** 构造前把别名**确定性截断**（`sorted().take(8)`）而不是让构造失败；类型上的 `require` 保持不变
（模型类型的不变量应当严格，"把内容适配到不变量"是数据层的责任）。残余的其它丢弃原因
（如主名超 96 字）现在**记账并记一行日志**（截断数 + 丢弃数），不再无声。

**为什么是截断不是抬高上限.** 这个上下文最多带 64 个节点进提示词；上限存在的意义是
**提示词有界**，截断保住了这个意义，同时把"节点消失"换成"别名变少"——后者无害（主名是独立字段，永远在）。

### M-03 · 4 个 topic 排在父级之前（会让安装撞外键） 【已修 · 成品已重排】

**症状.** `topics` 是平铺数组、按顺序逐个变成节点，而 `knowledge_node.parent_knowledge_node_id`
是自引用外键、逐行检查 → 子级排在父级前面就 `FOREIGN KEY constraint failed`。
成品里有 4 处（如 `MATH·综合` 排在 `MATH` 之前）。成因是 `relocate_points.py` 的
`ensure_synthesis_chain` 无条件把新建的「综合」链 `append` 到数组尾。

**诚实说清价值**：生产安装路径已被调和器的 `parentFirstOrder` 容忍，所以这次修的是
**成品自身的正确性 + 防复发 + 关掉一个顺序盲的公开端口**（`importKnowledgeBase` 校验用 map、
写入用数组顺序），**不是解锁任何生产功能**。

**处置.** 新增 `topic_order.parent_first`（纯函数、稳定、父级先序）与 `order_topics.py`（报告 / `--write` +
无损三查：topic 集合、`parentSlug`、点数逐条不变）；接进 `relocate_points.py` 的 `dump_json` 前防复发；
成品重排 4 → 0（458 行增删，**已验证是纯置换**：删除集与新增集作为多重集相等）。

**本轮验证.** 门：只有 `topic_parent_after_child` 一项变化（**4 → 0**），其余 18 项与 HEAD 逐字相同
（用 HEAD 的包在临时目录重跑门做的对比）；未通过项 11/19 → 10/19；`roundtrip` 7/7 OK。

**未做（用户裁定）**：不削减别名（别名膨胀是过度绑定的症状，已登记）、不减材料绑定
（1019 个节点绑 >4 条材料）、不把排序放进解码器。

**顺带更正一条 overstated 的说法**：`roundtrip.py` 的结论文案写的是"生成器可忠实重放现行成品"，
但它**不运行生成器**——它是"读文件 → 重新序列化 → 与磁盘比对"，证明的是**序列化器保真**。
不影响本轮结论，登记为观察。

### M-04 · 文本修复算得出、改不到成品（LaTeX 194 + 控制字符 80） 【P1 · 已修】

**症状.** 门指标 `latex_damage` 194、`control_chars` 80（61 条 TAB + 23 条孤立 CR），
而 `report_latex` 实测"受损 194；**修复成功 194**；仍受损 0"——修复器能把全部修好，
盘上却一条都没修。

**根因.** 修复逻辑有**两处**（`textfix` 的库函数 + `build.py._repair_material_text`），
后者是唯一把结果写回的地方，而它写的是**内存**里的 `self.sidecars`，且 `build.py` 已停用。
成品由一批逐项工具直接改写，谁都不负责文本修复 → 断链。

**修复.** 新增 `tools/kb_build/fix_material_text.py`：与 build.py **同一套修复、同一顺序**
（`strip_control_junk(repair_latex_commands(...))`——顺序不能反，形态 2 的损坏拿控制符本身
当证据），带无损三查（材料集合与其余字段逐条不变、只改真坏的材料、幂等）后写回。
结果：**194 → 0、80 → 0**，其余 18 项门指标与改前逐字相同。

**回归锁.** `tools/tests/test_kb_fix_material_text.py`：成品哨兵（sidecar 里不该再有残迹）+
幂等 + "只改那 5 个文本字段"（bindings 不许被碰）。

### M-05 · 65 条坏名定稿停在作废的表里，无人执行 【P0 · 大部已收口】

**症状.** 成品里仍有 `定义：通电导线在磁场中受到的力叫安培力。`、
`(1)写出分子式为C5H12的烷烃的结构简式：`、`表达式：ΔU＝Q＋W。` 这类**残句当知识点名**，
且它们直接挂在章层（正是 `chapter_layer_has_points` 那 12 条的来源）。

**根因.** `fix_bad_names.py` 早有 65 条定稿（rename/merge/delete，逐条写明判据与原文），
但它只把决定写进 `node_actions.csv`——**那张表已作废**（`node_actions.README.md`：
其 slug 与成品不是同一坐标系），执行者 `build.py` 亦停用。实测 65 条里 **45 条仍停在纸上**
（23 改名 / 12 合并 / 10 删除），其中绝大多数用的**就是成品的 slug**——
"坐标系不一致"这个作废理由对它们并不成立。

**处置（本次移植并执行 31 条）.** 逐条核验后移植进权威表并跑执行器：

| 动作 | 条数 | 结果 |
|---|---|---|
| 改名 → `point_rename.csv` | 22 | 名字变成人可读的概念名（`键线式`、`淀粉水解产物的检验`、`追及相遇问题的判别式法`…） |
| 合并 → `point_merge.csv` | 9 | 属性条目/碎片并入主节点（`定义-…安培力`→`安培力`、`表达式-δu-q-w`→`热力学第一定律`…） |
| 合计 | 31 | 点 2434 → 2425；材料总数不变；悬空绑定 0 |

**排除 13 条（不是能搬的活，逐条写明理由）**：1 条与权威改名表的决定冲突；
2 条幸存者不在成品里；**1 条内容不符**（`应用-光纤通信…` 的边界讲的是**全反射**，
并入「激光」会把内容放错节点）；**10 条删除各有 1 条材料绑定**，违反 `point_delete.csv`
"只列无绑定节点"的准入条件——要先逐条读那条材料，决定"改绑到正确节点后删点"还是
"连材料一起删"（材料本身可能就是同一段题干）。**这 10 条留给下一轮，见 M-06。**

**顺序坑（已踩过并修正）**：改名后必须**跟着重建别名**（`rebuild_aliases`），否则旧主名
留在别名里 → `ghost_aliases` 0→29、`alias_collision` 0→1；重建后回到 0/0。

**回归锁.** `tools/tests/test_kb_bad_name_actions.py`：凡 slug 在成品里的定稿行，要么已生效、
要么在**显式例外表**里；例外表还做反向校验（条目不再 pending 就必须删掉），不让它变成垃圾桶。

### M-06 · 10 条"原判删除"的节点：读完材料后改判为改名保留 【P0 · 已收口 2026-09-19】

它们被判为"题干/答案原文、不含知识点"，但**每条都还绑着 1 条材料**：
`查阅资料-了解工业生产中提高电镀质量的方法`、`硫酸是中学化学实验室的常见药品-其性质有`、
`在一定温度下-已知以下三个反应的平衡常数`、`1-根据的结构特征-预测其可能的化学性质`、
`黄酮哌酯是一种解痉药-可通过如下路线合成`、`以fe3o4为原料炼铁-主要发生如下反应`、
`步骤③和步骤④的操作顺序能否颠倒-为什么`、`手机-电脑的锂电池是一次电池还是二次电池`、
`na22`（还被别的点当前置引用）、`表达式-f-g`。

**读完那 10 条材料后，原判被推翻**：每条绑的都是**成体系的知识内容**（不是题干），
而**材料标题本身就是该知识点应有的名字**——`na22` 绑着《过氧化钠的强氧化性与还原性》、
`表达式-f-g` 绑着《微元柱体的体积、质量与动量表达式》、`步骤③和步骤④…` 绑着
《坩埚灼烧固体的操作要点》。这正是 `fix_bad_names` 自己写下的判据：**绑定数不为 0 就是
"它是真知识点"的证据**（材料是照着这个知识点抽的）。

**处置（用户批准后执行）**：10 条全部**改名保留**，写进 `point_rename.csv` 后跑执行器——
`提高电镀质量的方法`、`硫酸的性质与实验应用`、`平衡常数之间的关系`、`由官能团预测反应类型`、
`由定量关系推断官能团数目`、`Fe3O4与稀硝酸、氢碘酸的反应`、`坩埚灼烧固体的操作要点`、
`锂电池与新型电源`、`过氧化钠的强氧化性与还原性`、`微元柱体的体积、质量与动量表达式`。
**不删任何节点、不 orphan 任何材料**；slug 一个字节不动（绑定/前置/检索稳定码都不受影响）。
随后 `rebuild_aliases` 清掉旧主名残留（别名 15201 → 15193，变更 10 个节点）。
门：**19 项逐条无变化**（这 10 个名字此前都绕过了以 24 字为界的坏名判据，所以本来就不在门上）。

**教训（价值高于这 10 条本身）**：判"残渣"之前**必须用它绑定的材料复核一遍**——材料标题
常常就是正确的节点名。仅凭名字像题干就判死，会把真知识点连同它的材料一起丢掉。

**回归锁**：`test_kb_bad_name_actions.py` 的例外理由已更新（FIXES 判 delete、实际改判 rename，
delete 永不该执行）；改名本身的"已应用"由 `test_kb_rename_points.test_rename_is_idempotent_on_shipped`
钉住。

### M-07 · 章层挂点 12 条：混着"合法点"与"题干残渣" 【P2 · 已收口 12→2（余 2 为结构问题）】

`chapter_layer_has_points` 的 12 条 = 化学「物质结构与性质」2 +「有机化学基础」7 + 生物「生物技术与工程」3。
读了内容之后可以确认它们**不是一类东西**：

- **已改名的合法点**（3 条：`键线式`、`淀粉水解产物的检验`、`石油裂解的产物`）——现在可以
  正当地归位到主题，只是本轮的 `chapter_point_relocation.csv` 里没有它们（该表是 167→12 那一批的）。
- **仍是题干/碎片**（`(1)写出分子式为C5H12的烷烃的结构简式：`、`一种2-甲基色酮内酯`、`结束后`、
  `碳元素的单质有多种形式`、`晶体类型与晶胞构型的判断` 等）——按 `relocate_chapter_points` 自己的
  规矩（"硬塞进某主题等于把垃圾也归了位"）**不该搬**，该走 rename/delete 通道。

**为什么本轮没做.** 我原先把它当成"跑一遍既有工具"（工具确实在），读了内容才发现 12 条要逐条判
归属或判生死——那是内容判定，不是搬运。**如实纠正，不为把指标刷绿而硬塞。**

**收口进展（2026-09-19，用户批准"改"之后）.** `chapter_layer_has_points` **12 → 6**（移动 6，
点数 2425 守恒、slug 集合不变、**topic 总数 468 不变**——6 个主题名都命中子树里已有的主题，
没有新建）：

| 归位的点 | 目标主题 |
|---|---|
| `键线式` | 有机物的结构与表示 |
| `淀粉水解产物的检验` | 糖类 |
| `石油裂解的产物` | 化石燃料的综合利用 |
| `晶体类型与晶胞构型的判断` | 晶胞与均摊法 |
| `卤代烃中卤素原子的检验` | 卤代烃 |
| `乙醇的性质与转化` | 醇 |

**剩余 4 条刻意不搬，各有理由**
- 化学 1（`(1)写出分子式为C5H12的烷烃的结构简式：`）→ 它**和它唯一的材料都是题干**
  （边界是"(2)写出所有分子式为C4H8的烃的结构简式："、材料是《氰[(CN)2]…》的题干），
  停在"连材料一起删"的裁定上。
- 生物 3（`实验：探究抗生素对细菌的选择作用`、`隔离在物种形成中的作用`、`结束后…`）
  → **归属**问题：前两条其实是必修2 第六章「生物的进化」的内容而挂在选必3 的章下
  （`生物技术与工程` 的子树里还混着 `生物有共同祖先的证据`、`协同进化与生物多样性的形成`
  这两个必修2 第六章的主题），走 `chapter_locator_clusters.csv` 的簇判定更合适；
  第三条**名字/别名/边界三方不一致**（名字"结束后"、别名"培养基的分类/鉴别培养基"、
  边界讲人黑猩猩血红蛋白 α 链差异），要先看它绑的材料。

**又归位 2 条（同日读完绑定材料后）**：`碳元素的单质有多种形式`（4 条材料全是成体系知识）
改名 `碳单质与同素异形体` → 主题 `物质的聚集状态与晶体的常识`；
`一种2-甲基色酮内酯`（材料是《黄鸣龙还原法》真知识）改名 `黄鸣龙还原法` → 主题 `有机合成`。
两条都命中已有主题、**零新建**，`chapter_layer_has_points` **6 → 4**。

**再删 1 条（用户批准）**：`(1)写出分子式为C5H12的烷烃的结构简式：` 连同它唯一的题干材料
（`point_delete.csv` 加 `drop_materials=yes`）→ 点数 2425 → 2424、材料 15862 → 15861、
`chapter_layer_has_points` **4 → 3**。

**收口（12 → 2，同轮读完最后 3 条生物挂点绑定的材料）**：
- `结束后-…`：材料是《培养基的分类》《鉴别培养基与指示剂的使用》（成体系知识）→ 改名
  `培养基的分类与使用` + 归位 `微生物的培养技术及应用`（**3 → 2**）；
- `实验：探究抗生素对细菌的选择作用`：6 条材料里 4 条错绑（蓝细菌/醋酸菌/选择培养基/
  质壁分离复原）——**目标节点其实都存在**（`原核细胞`/`传统发酵技术`/`质壁分离`/
  `培养基的分类与使用`），此前"全库没有它们该去的节点"是查漏了 → 4 条已改绑；
- `隔离在物种形成中的作用`：定位串已随 1306 条对齐到必修2 第六章，但挂在选必3 子树里。

**剩余 2 条刻意不归位**（`隔离…`、`实验：探究抗生素…`）：属"选必3 子树混着必修2 第六章
主题"（`DNA是主要的遗传物质`、`生物有共同祖先的证据`、`协同进化与生物多样性的形成` 也在
同一子树）的**主题树归属**问题——归位到哪个主题都只是在错误结构里挪位置，留待主题树
归属整理那一轮。

### M-08 · 表驱动工具的 **CLI 从没被执行过**（跑不动也没人知道） 【P1 · 已修 + 已加锁】

**症状.** 执行上面那条删除时，`delete_points` 的 CLI 一跑就 `TypeError: string indices must be
integers`——`main()` 里"用前后集合求差算出被删 slug"那两行写的是 `s["subject"]`，而
`iter_points` 产出的第一项是 subject **字符串**。

**为什么一直没被发现.** 库函数级用例（`delete()`, `_purge_table_refs()`）**全绿**，而没有任何
门会去跑 `main()`。这段求差代码是"删除也记退役台账"那一轮加的，加完没人执行过整条 CLI。
**与 M-04/M-05 同一形态：改动没有对应的执行验证。**

**处置.** 修那两行（`s["subject"]` → `s`），并做两件防复发的事：
1. 新增用例 `test_cli_runs_without_crashing`——直接跑 `main([])` 要求返回 0（成品上幂等）；
2. **把所有表驱动工具的 CLI 冒烟跑一遍**：`rename_points / merge_points / delete_points /
   rebuild_aliases / relocate_chapter_points / relocate_points / fix_topic_parents /
   shorten_topic_names / order_topics / align_chapter_locators / fix_material_text /
   check_pack_contract / blank_nodes` 全部返回 0（只有下面那条 `verify_tables` 例外）。

**顺带查出一条死工具**：`verify_tables` exit=1 报"70 条改名的低重合必须回读原文"，但**那些
slug 一个都不在成品里**（它的输入 `node_actions_reviewed.csv` 是从已作废的 `node_actions.csv`
派生的，与成品不是同一坐标系）。也就是说它现在**测的是废表**，输出不可执行——本次改动的
0 条与它相关。要么把它重新指向权威表，要么明确标记为溯源工具、不参与门。

---

## N. 定位串与章表不一致：分类与裁定请求（2026-09-19）

门指标 `chapter_locator_mismatch` = **1676**。它原先只是**一个数字**，而里面混着两类东西，
处置方向相反。新增 `tools/kb_build/report_chapter_locator_mismatch.py` 把判据写成可重算的分类
（用例钉住"总数必须等于门的数字"，另一条钉住"两类都不为空"），结果：

| 类别 | 条数 | 含义 | 可否机械处置 |
|---|---|---|---|
| 只差 `第X章 ` 前缀 | 147 | 同一章、同一个名字 | 可 |
| 同章、两种写法（归属一致） | 266 | `铁与金属材料` vs `第三章 铁 金属材料`、`数列` vs `第四章 数列` | 可 |
| 同册、章不同 | 786 | **归属真的不同** | 需逐条判 |
| 连册都不同 | 477 | 同上 | 需逐条判 |

分学科：化学 687 / 物理 514 / 数学 291 / 生物 184。清单落盘 `tables/chapter_locator_disputes.csv`
（1676 行，带 kind 列）。

**把规模打下来的观察**：1263 条"需判"不是 1263 个决定，而是 **83 个簇**——同一个
`(章表章 → 节点写法)` 对只需一次判定。最大的簇：73 条（章表 `化学选择性必修1/第二章
化学反应速率与化学平衡` ⇐ 节点写 `化学必修第二册/化学反应与能量`）、49、48、39、36…

**要你裁定的分叉（权威源在哪边）**
- **A. 章表权威**：413 条"同章两种写法"按章表机械重写（工具 + 无损校验 + 门对比，与 M-03 同形态）；
  1263 条逐簇判后再改。
- **B. 节点定位串权威**：反过来修 `chapter_by_source.csv` / `chapter_by_node.csv`。
- **C. 只对齐 413 条、1263 条先不动**（把"真不同"留在门上当待办，不做数字遮掩）。

**推荐：A 的前半段（413 条）+ 逐簇判 83 个簇。** 依据：`chapter_by_source.csv` 是人工定稿的
单元级权威（带 `keep_per_node` / `split` 这类决策字段），而节点的 `定位：` 串是抽取时写下的、
不同批次两套写法。但**像 73 条那种簇要单独读内容**（那些点讲的是反应热还是速率/平衡），
不能靠"章表权威"一句话带过。

**刻意保守处（已写进用例）**：章名改写幅度大时（`水溶液中的离子平衡` vs
`第三章 水溶液中的离子反应与平衡`）判据判到"同册异章"交给人，不硬认成同一章——错判成同章会
触发自动改写（把真错固化），错判成异章只是多一条待判；两个方向的代价不对称，所以宁可少认。

### N-01 · 裁定与执行结果（2026-09-19 · 用户裁定"章表权威，先做写法类"）

新增 `tools/kb_build/align_chapter_locators.py` 并按裁定执行：**369 条已对齐**，形态为
`定位：{册}·{章号 章名}[·{节}]`（`gate._place_of` 明文承认的第二种老写法；章号从"单独成段"
并为章名的一部分，节级定位原样保留）。门的 `chapter_locator_mismatch` **1676 → 1307**，
其余 18 项逐字不变；`roundtrip` 7/7；四科检索基准 Recall@5 = 0.95、MRR = 0.90 不变。

**无损与幂等**（工具内自证，用例复核）：只改第一个 `。` 之前那一段、`。` 之后逐字节不变；
其余字段逐条不变；点数与 slug 集合不变；重放 0 改动；**不丢信息**——原章名（去章号与虚词）
必须被新章名包含，不满足就跳过。

**413 里剩下 44 条没对齐，原因写在跳过理由里**：章名改写幅度超出包含关系，例如
`概率统计 ⊄ 第十章 概率`、`概率统计 ⊄ 第九章 统计`——那是**单元跨章/归属**问题，
不是写法问题，硬改会把归属判死。它们与 1263 条一起构成门上的 **1307**：
1263 条归属分歧（83 个簇，逐簇判）+ 44 条大幅改写。

### N-02 · 裁定执行到底：1306 条全部对齐（2026-09-19 · 用户裁定"章表权威"后全量执行）

**先把"1263 条归属分歧"拆到最小决策单位**：它们不是 1263 个决定，而是 **116 个来源单元**
（同一单元的所有节点共享同一个章表声明；工具报告里按单元留档了 299 组映射）。
再查结构：1262 条里 **84% 的节点的父主题定位串与节点自己一致、都跟章表不同**——
即"树+节点"对"章表"，不是零散错误，是整批专题（五三《专题0X …》）的节点与主题
带着抽取时的旧定位串进的包，而章表是后来对照人教版目录人工定稿的。

**执行**：`align_chapter_locators` 扩展为对**全部**有章表声明的不一致条目按章表对齐
（不再挑"写法类"），无损机制不变（只改 `定位：` 段、`。` 之后逐字节不变、幂等）。
`chapter_locator_mismatch` **1306 → 0**——门上第 10 项红项清零，未通过 7/19 → **6/19**。
其余 17 项逐字不变；四科基准 Recall@5 = 0.95、MRR = 0.925 不变。

**为什么敢一次对齐 1306 条**：抽查最大的 12 个单元（约 350 条）逐一对人教版 2019 目录核对，
章表全对、节点串全错（节点写的是册名/章节俗称/别的章）；且每个单元的改写由
`chapter_by_source.csv` 的人工定稿驱动、同单元统一改，不存在逐条语义判断。

**刻意不做（登记为结构问题，不是本次改文字能解决的）**：
1. **主题（topic）的 sourceLocator 没动**——它是章表做单元匹配的 join 键，改了 join 就断。
   主题定位串与节点不一致是展示瑕疵，且 199 条"三方不一致"里有主题树本身要重划的，
   留到主题结构那一轮。
2. **选必3 子树混着必修2 第六章主题**（`DNA是主要的遗传物质`、`生物有共同祖先的证据`、
   `协同进化与生物多样性的形成` 等）——这是主题树的跨册混挂，`隔离在物种形成中的作用`、
   `实验：探究抗生素对细菌的选择作用` 两条章层挂点就卡在这上面，归位前先要把树理清。

## O. 文本判定主循环首轮（2026-09-19 · 化学/生物讲义·知识清单 9,405 块）

**范围.** 待判定池 164,347 块 / 7,897 文件里的最高价值切片：化学 + 生物的「一轮复习讲义·学生版」
与「知识清单·学生版」（学生版是知识最干净的一侧；教师版=学生版+答案解析、课件=同内容幻灯片，
判定它们等于重复入库，本轮明确不判）。9,405 块切成 30 片（31 个代理跑了 30 片）。

**数据.** 材料 **15,861 → 20,456**；知识点 **2,424 → 2,852**（新建 428 个，其中化学 347 / 生物 81）；
新增 sidecar 卷 **v2-07**（单卷 2.5M 字符上限触发滚动，4,606 条）；主题仍 468。

### O-01 · 【P0】materialize 滚动新卷时静默丢数据（已修，影响 4,606 条）【已收口】

**现象.** `materialize --write` 报 `materialized: 4606` 且状态机推进 491 个文件，
但磁盘上**一个字节没写**；随后的 `extraction_state --verify` 报 491 条 `output_ref 悬空`。

**根因（两处叠加）.** ① 需要滚动新卷时，新 doc 没有放进 `changed_sidecars` → 永不落盘；
② `next_sidecar_path()` 不登记索引，于是**每一行都重新滚动到同一个"未登记的新卷名"**并各自新建空 doc
→ 4,606 条里只有最后 1 条活下来（实测 v2-07 只有 1 条材料）。

**修法.** 开新卷即 `write_sidecar_index([*paths, new])` 登记；`_pick_sidecar` 对"已登记未落盘"的卷
返回 None 并让调用方复用 `changed_sidecars` 里的累积 doc；列表加载跳过不存在的卷。
修后同一命令写出 **v2-07 = 4,606 条**、索引含 07、状态机账本完整。

**证据.** 修前/修后 `find core/data/.../moe-2025-teaching-support-v2-07.json` 的材料数 1 → 4,606；
材料总数 15,861 → 20,467 → （去重后）20,456；HTTP 无关。这一条是**静默丢数据**类，
状态机会照报成功，只有 `--verify` 的悬空门能抓——所以那道门必须每次入库后都跑。

### O-02 · 同块被两判产生"同指纹重复材料"11 组（App 契约拒绝整包）【已收口】

**现象.** `BundledTeachingMaterialsContractTest` 报
`Knowledge teaching-material content fingerprints must be unique`（450 条里 1 条红）。

**根因.** 切片重切后同一个块被两个代理各判一次；合并按出现顺序重排 `midx`（''/b/c/d），
其中两条行内容逐字相同 → 两条 slug 不同、载荷相同的材料。

**修法.** 新增 `kb_coverage/dedupe_judged_materials`：按 `(chunk_id, node_slug, type, title, content)`
判重，判定表删 11 行、侧车删 11 条；复算重复指纹组 **0**。修后 `:core:data` **450 条全绿**。

### O-03 · 判定表与侧车不一致 27 行（上一轮改绑只改了侧车）【已收口】

**现象.** 入库时报 slug 撞车（同块同 midx、节点不同）。追查发现：上一轮"别名取证反查"改绑了
**侧车绑定**，但判定表里对应行仍写着旧节点（如侧车已指 `协同进化与生物多样性的形成`、
判定表还写 `共同进化`）。

**修法.** 以侧车为准对齐判定表（27 行）；同批顺手把"目标节点已被合并/删除"的旧判定 102 行标为
`SKIP` 并注明作废原因（这些行此前会让 materialize 整批拒绝）。

### O-04 · 门指标与检索（本轮对照）【已收口】

| 指标 | 上轮 | 本轮 | 说明 |
|---|---|---|---|
| 材料 / 知识点 | 15,861 / 2,424 | **20,456 / 2,852** | +4,595 材料、+428 节点 |
| ghost_aliases / alias_collision | 0 / 0 | 0 / 0 | 别名重建后仍守得住 |
| latex_damage / control_chars | 194 / 80 | **0 / 0** | 并行会话的文本修复生效（我独立扫描复核为 0） |
| boundary_excerpt | 676 | **675** | |
| locator_boundary | 579 | 579 | |
| chapter_locator_mismatch | 1,683 | **1,306** | 新节点的章表行补齐 |
| 章层挂点 | 12 | **3** | |
| unbound_points | 2 | 2 | 仍是那两个真缺口 |
| 检索 Recall@5 / Precision@5 / MRR | 0.95 / 0.72 / 0.900 | **0.95 / 0.74 / 0.925** | 同套 20 题、按科隔离 |

**遗留（不编造）.** 两个零材料知识点（MATH「由线、面关系误解向量关系」、CHEMISTRY「自然资源的
开发利用」）在待判定块池里只搜到真题碎片，没有干净知识块——留给后续轮次带源补料。

### O-05 · 已判定源文件物理隔离 1,074 个【已收口】

按"扫过的必须隔离、禁止重复扫"：`state=EXTRACTED` 的源文件（含此前各轮的文本源与视觉转写过的
PDF）搬进 `<源根>/zz-已判定隔离/`，镜像原路径、字节校验、可逆日志
（`knowledge-production/processed-isolation-log-2026-09.tsv`）。清单由
`tools/kb_coverage/tables/processed_movable.txt` 给出，脚本 `isolate_processed.sh`（`--dry-run/--undo`）。
状态机的 `verify()` 只读仓库内两张表、不碰源树，故搬移不影响账本完整性（实测仍"账本完整"）。

---

## P. 主题去重轮（2026-09-19 · 468 → 396 topic，点全部守恒）

### P-01 · 同名主题在旧教材位置与 2019 版位置各存一份 【P1 · 已收口】

**症状.** 定位串对齐（N-02）做完后复查结构，发现 **23 组同名主题两边都带节点**（「化学平衡」曾 4 份、
「物质的量」4 份、「原子结构与元素周期表」4 份、「1 圆周运动」2 份…）。成因是旧教材结构
（`化学必修第二册·第六章·化学反应与能量` 是 2007 版章序）与 2019 新结构并存，抽取时各建各的主题，
从没做过**主题级**去重。检索特征按主题名派生时同名主题互相竞争，App 树里同一知识点出现多份。

**决策依据.** 每个组的 canonical 由**节点定位串**（N-02 之后已全部对齐到人工定稿的章表）投票 +
2019 人教版目录核对决定；不凭主题名猜。例如「化学平衡」4 份里 15 个节点的章表定位全是
选必一 第二章 → canonical = `化学选择性必修1·第二章·化学平衡`；「1 圆周运动」5+1 个节点全指
必修二 第五六章 → canonical = `物理必修第二册·第五六章·…·1 圆周运动`。

**执行.** 新增 `tools/kb_build/merge_topics.py`（表驱动 `topic_actions.csv`，动作
`create`/`merge`/`delete_empty`）+ 80 条动作两批执行：

| 动作 | 数量 | 说明 |
|---|---|---|
| merge | 56 | stale 主题的**点全部**并入 canonical（点 slug/材料/前置/别名都不动，只换父）；同名子主题递归并入；stale 空了才删 |
| create | 4 | 补建 2019 版位置漏建的章节主题（选必1「3 简谐运动的回复力和能量」、选必2「化学键」、选必3「有机化合物的结构特点」、必修二「2 运动的合成与分解」） |
| delete_empty | 20 | 0 点 0 子主题的空壳（含生物必修1「细胞与生命活动」旧结构残留 8 个） |

**点迁移 142 个，点数 2852 守恒**；topic **468 → 396**；取代台账 +76 条（主题节点
`kb:{packId}:{subject}:topic:{slug}` 记 MERGE/DELETE，学生库里对旧主题的引用经调和器重定向）。
另 11 个**同名消歧改名**（名字相同但讲不同内容的：`2 摩擦力`→`摩擦力做功与能量关系`、
`带电粒子在电场中的运动`→`带电粒子在磁场中的运动`、`烷烃`→`烷烃的结构与性质` 等）——
改名不改 slug，材料/检索稳定码不受影响。

**无损与幂等**（工具内自证 + 用例复核）：点数与点 slug 集合守恒、剩余主题父级全部有效且无环、
合并后 canonical 点集 = 两侧并集（重复 slug 直接拒绝整批）、重放 0 改动。

**刻意保留的同名对（2 个，用例钉死）**：`3 电磁感应现象及应用`（教材位置 8 节点 + 跨册综合桶
的线框模型 1 节点——综合桶是设计上的跨切面位置）与 `细胞的多样性和统一性`（教材位置 4 节点 +
「细胞的生命历程」下的 `细胞的全能性` 1 节点——该点没有更深的归宿主题，归位会制造新的章层挂点）。

### P-02 · 一个 pin 的合法下调（如实记录）

`ReviewedKnowledgePackJsonCodecTest` 的 topic 下界 **400 → 380**。该用例自己写明"只守下界、
防半棵树，精确规模在台账"；主题去重把 468 个 topic 降到 396（删掉的全是重复杂份与空壳，
点 2852 一个没少、父链完整），下界随结构变化调整并留 5% 余量，不变量（节点=topic+原子点、
父链完整、材料=绑定）原样保留。

---

## Q. 主题树结构收尾轮（2026-09-19 · 用户指令"把这些都完成了"的项 1–3）

### Q-01 · 选必3 子树混挂必修2 主题（生物）【已收口】

选必3「生物技术与工程」子树里混着必修2 的内容主题：`DNA是主要的遗传物质`（空壳，真身在
必修2·第三四章）、`生物有共同祖先的证据`、`协同进化与生物多样性的形成`（都是 0 点空壳）。
处置：两个空壳从选必3 删除、在 `生物学必修2·第五六章·变异与进化` 下补建（2019 必修2 第六章
「生物的进化」第 1/4 节）；被它卡住的 2 条章层挂点随之归位到
`种群基因组成的变化与物种的形成`（隔离/物种形成=第六章第三节、抗生素耐药性实验=自然选择
演示，都是该节内容）。**`chapter_layer_has_points` 2 → 0**——章层挂点这个指标清零。

### Q-02 · 平面向量主题混 32 个空间向量节点（数学）【已收口】

`数学必修第二册·第六章·平面向量` 47 点里 32 个定位串（章表）全指选必一 第一章 空间向量与
立体几何——该主题在选必一根下**根本不存在**（必修二里只有一个 0 点的注释主题
「第八章（空间向量见选择性必修第一册）」指向它）。经 `new_topics.csv` 显式补建
`数学选择性必修第一册·第一章·空间向量与立体几何`（+祖先「第一章」），32 点归位、
15 个平面向量点原地不动。定位串随后按章表重新对齐（`align_chapter_locators` 34 条，幂等）。

### Q-03 · 21 个占位主题定位串补录【已收口】

历轮建主题给不出章表定位的一律写 `定位：待补章表。`（工具惯例），成品里积了 21 个。
新增 `backfill_topic_locators.py`：取值=**该主题下节点定位串的众数**（节点已被对齐到人工
定稿章表，众数即章表声明），众数 <50% 的跨章主题**跳过不硬写**（实测 21 个全部 ≥80%，
2 个 0 点新主题按设计跳过）。只动主题的 `sourceLocator` 字段，其余逐字节不变、幂等。

## N. 教材来源联网核查（2026-09-19 · 用户要求"教材这个你可以去联网搜索一下"）

**结论：本轮未能取得可用的教材来源情报——三条通道全部受阻，如实记为"未验证"，不据以入库任何教材原文。**

| 通道 | 结果 |
|---|---|
| WebSearch（内置检索） | 不可用：当前 provider 不编码 provider-native WebSearch（`Provider API kind openai-compatible does not encode provider-native WebSearch`） |
| `https://basic.smartedu.cn/tchMaterial`（国家中小学智慧教育平台·教材） | HTTP **403 Forbidden**（站点拒绝非浏览器访问） |
| `https://www.bing.com/search?q=…` | 返回与本查询无关的垃圾结果（页面自带 "Content was generated with AI" 声明），不可用 |
| `https://html.duckduckgo.com/html/?q=…` | 人机验证挑战页，无结果 |
| `https://www.pep.com.cn/`（人教社官网） | **可用**：站内确有"教材电子版"栏目，并在"高中"下列出分学科入口（`jc.pep.com.cn/?filed=高中&subject=数学` 等）；但**是否在线仅读、是否可下载、许可条款均未读到**（页面为 JS 渲染，`/banquan/` 也只返回跳转壳） |

**据此能说到什么程度（本报告与下游只允许用这一段）**：
- 可以确认：人教社官网存在官方教材电子版入口（第一方页面观察到，单一来源）。
- **不能确认**：能否批量下载、能否用于产品内再分发、许可条款内容 —— 均 **未验证**。因此**不抓取、不落盘、不入库**教材原文。
- 与既往研究一致（见 `external-kb-benchmarks-2026-09-15`）：无开放中文高中 OER；教材类内容只能靠教辅（讲义/知识清单/精讲册）落地，这条边界不变。
- **复检建议**：用带 JS 渲染的浏览器会话重试上述两个站点；若确认到"可下载 + 许可允许内部使用"，再立项抓取（届时走 `office_extract` 直抽通道 + 逐条判定）。

## O. 缺料登记册（2026-09-19 建立 · `knowledge-production/node-material-gaps-2026-09.csv`）

**它解决什么**：用户要求"还缺的节点材料，你先把它登记好，这样以后就知道"。此前只有门禁的
`unbound_points` 一个数字（只报零材料），"只有 1 条材料"的薄弱节点不可见。

**两级缺口（可复算，每次入库后重跑 `kb_build.report_material_gaps --write` 对比增减）**：

| 级别 | 数量 | 含义 |
|---|---|---|
| `zero` | **2** | 零材料节点（必须补）：MATH「由线、面关系误解向量关系」、CHEMISTRY「自然资源的开发利用」 |
| `thin` | **814** | 仅 1 条材料的薄弱节点（后续加厚） |

每行带 `subject/slug/name/topic/material_count/tier/note`；`zero` 行的 `note` 写明**为什么缺 + 去哪里找**
（如"块池里该主题只有真题碎片，待从该章节讲义/教材正文补"）。下轮起把"zero 归零、thin 递减"
作为补料轮的可验收指标。

## R. 内容裁定执行轮（2026-09-19 · 前置 1246 条审计 + 边界 1254 条撰写）

> 节号说明：并行会话在我重编 O 节之后又追加了两个 `## N`/`## O`，字母出现重复。
> 本节用 R，重复的留待双方轮次都收口后一次性重编（避免改动对方正在写的章节号）。

### R-01 · 前置 1246 条逐条语义审计：1246 → 406 条（门 `undeclared_prereq` 1246 → 0）

21 个子代理逐条读（点名 + 边界头 + 双方材料标题）判定每条前置边：
**valid 259 / inverted 147 / unrelated 840**——印证 09-12 审计"前置是假链"：
67% 的边根本不成立，另有 12% 方向反了。机械抽查复核（36 条人工过目）判定质量可用。

应用（`apply_round4_verdicts`）：valid 保留、inverted 翻转（P←Q 变 Q←P）、unrelated 删除；
按学科环检查（0 条成环边需删）；**`prereq_map.csv` 由应用后的最终图整体重建**（406 行）——
门 m7 的口径（包内每条边都必须被权威表声明）归零。改写 1019 个点的 `prerequisiteSlugs`。

### R-02 · 边界 1254 条（579 占位 + 675 原文摘录）全部重写（门两项 → 0）

26 个子代理逐点撰写"边界正文"（接在已对齐的定位串之后，1–3 句 40–120 字，讲该点范围/限度/
与易混概念的区别；只依据该点材料与名字，禁止逐字抄原文——合规：来源政策不允许保存原文段落）。
机器校验全过：`has_verbatim_excerpt` / `is_locator_only` 双双为 0、长度、控制字符、覆盖完整
（1254 行一行不多一行不少）。**11 条因正文里用了"易错辨析/正误判断"这类概念词被摘录判据
误伤**（判据是为识别粘贴的习题页设计的）——按"不放宽门"的规矩，改的是这 11 条正文的措辞
（换成"易混点/正误辨析"），不是判据。

### R-03 · 裁定表落盘 + 常驻执行器（M-05 形态的再防）

两份裁定是**版本控制内的表**（`tools/kb_build/tables/prereq_verdicts.csv` 1246 行、
`boundary_verdicts.csv` 1254 行），执行器 `apply_round4_verdicts.py` 常驻、幂等、可重放
（共享工作树被并发改动后重跑一遍即恢复）。写裁定表时踩了一个真 bug：
`csv.writerows(dict 列表)` 写的是 dict 的 **key**——两份表第一版整表变成表头行，
被执行器的预检（非法 verdict）当场拦下。**"执行器预检是最后一道闸"在这轮再次成立。**

### R-04 · 验证与门现状

干净基线端到端验证（HEAD 的隔离 worktree + 执行器重放）：门 5/19 → **2/19**
（剩 `unbound_points 2` / `unbound_materials 892`，均为缺料缺口，已在 O 节登记册管理）、
roundtrip 8/8、幂等重放 0 改动。回归锁：`test_kb_apply_round4_verdicts`（6 用例：
CLI 可跑、边全被声明、**包内边不得超出裁定表依据**、边界双判据哨兵、合成包语义、
不合格正文整批拒绝）。

**共享工作树竞态（如实记录）**：执行期间另一条会话正向同一个 pack 写 733 个新点
（其 v2-08 sidecar 未落盘）。我的 4a/4b 改动经抽查**幸存**（对方加载在我的写盘之后）；
主树上的全量门/stamp 等对方落盘后复跑，执行器幂等，届时重放即可对齐。

## S. 材料文本损坏普查与修复（2026-09-19 · 数学+物理轮收尾时发现）

起点是收尾数学+物理轮的合并/别名表时，门样例里露出一个 math 材料的标题：
`周期判定式：$f(x)=f(x+a)\Rightarrow\1=a$`。**顺着它查下去，坏的不是一条材料，是一整类文本事故**
（本条登记 M-06 / KD-21 / KD-22）。

### S-01 · 四类损坏与它们的机械证据 【P0 · 已收口：门三项 0 / 表与包同步】

| 类 | 现象 | 判据（可机械复核） | 规模 |
|---|---|---|---|
| A 回指残迹 | `\Rightarrow\1=a`、`\Delta\1`、`\forall\1_1` | 反斜杠后跟非法字符（数字/行尾/全角标点） | 2,283 处 |
| B `$$`→PID | `310243n=\frac{a-xb}{2}310243` | 同一 6–7 位数在 ≤120 字内出现两次且中间夹公式记号 | 9 个字段 |
| C `$0`→脚本名 | `只能读到 /usr/bin/bash.1\ \mathrm{g}$` | 字面 `/usr/bin/bash` | 13 处 |
| D 分隔符被吃 | `必须控制在 .5\sim10.5$`（缺 `$6`） | 字段内 `$` 计数为奇数 | 20 个字段 |

**共同机理**：A 是「一个空白 + 一串 ASCII 词元」被整体替换成 2 字符 `\1`（原文用 LaTeX 细空 `\`
时反斜杠残留，于是出现 `\\1`）；B/C/D 是文本被丢进 **shell 上下文**（`$$`→PID、`$0`→脚本名、
`$名` 连名一起吃）。两条线索都指向**生成/落盘环节用了 shell 或正则替换**，而不是知识内容本身错。
B/C/D 的实锤来自成品包本身：`/usr/bin/bash` 与 6 位 PID 都不可能是教材里的字符。

### S-02 · 修复：30 个子代理逐字段修 + 不依赖代理自述的独立复核

切片（`make_text_fix_slices.py`：517 条材料 / 538 字段 / 30 片，附源头切块作取证）交给 30 个
子代理，每个字段交回**修好的整字段原文**（`text_fixes/slice_NN/<slug>.<field>.txt` + `notes.csv`），
**不要求复述原文**——抄写正是这批损坏的来源。

写回器 `apply_text_fixes.py` 的三层独立复核（**这是本轮的关键**：代理说修好了不算证据）：

1. `与 cffda989^ 同字段逐字一致`（损坏前提交）；
2. `历史版本 N 里逐字存在`（该卷近 40 次提交里按 JSON 转义原文匹配）；
3. `入库前产物 .agent_*.csv 里逐字存在`。

结果：**485 条复原**（在历史或入库前产物里逐字存在，属"取字"不是"猜字"）、
**53 条重建**（该材料首次入库即带残迹，无干净前身）——53 条逐条人工过目后接受，其中带化学
数值的几条都用材料自身算术交叉核过（如 `0.05×99/6=82.5%` 反推取样量 `$6\ \mathrm{g}$`、
莫尔法 pH 6.5~10.5、p-π 共轭）。退回 0 条、UNCERTAIN 0 条。

### S-03 · 顺带修掉另一类"被掩盖的"损坏：`\ text{`（textfix 形态 4）

修好 `\1` 之后，门 `latex_damage` 从 0 变 5——**不是新坏，是原先被 `\1` 掩盖**：
`$9.8\ text{m/s}^2$`（应为 `$9.8\ \text{m/s}^2$`）里的 `ext{` 尾部特征本来是坏的，
但同一字段里 `\ text` 恰好被 `\1` 吃掉成 `\1`，判据看不见。补 `textfix` 形态 4
（`\ 空格 命令名{` → 补回反斜杠；要求命令名紧跟 `{`，因此合法的 `\ mol` 细空不受影响），
7 处全部修好，并**镜像回判定表 46 个文本列**（判定表是入库上游，不修会在下次 materialize 时带回来）。

### S-04 · 防回归与验证（本轮实测）

- 门新增三项指标（`invalid_escape` / `shell_expansion` / `dollar_unbalanced`）→ 全部 0，从此作哨兵；
- `gate.field_text_defects` 的 allowlist 与 PID 三条限定都写进了单测（正反两侧都钉住：
  `$9.8\ \text{m/s}^2$`、`10000` 换算系数、`$f'(x)$` 必须不报）；
- `check_pack_contract` 0 问题（主题 398 / 点 3,581）、别名重建 24,702（0 变更）、门 2/22
  （只剩两项已登记的缺料缺口）；
- tools 套件 **288 passed / 30,844 subtests**；判定表残留 `\1` 0、`\ 命令名{` 0。

### S-05 · 如实记录的未验证项

- 53 条重建里带数值推断的（如 `$6\ \mathrm{g}$` 取样量、`$6.5\sim10.5$` 下限）是**推理而非逐字**，
  已在 `text_fixes/_REVIEW.md` 与各片 `notes.csv` 里逐条写明依据；
- shell 上下文究竟发生在哪个环节（代理自用命令 or 某次落盘），**没有拿到当时的命令记录**，
  只能从"`$$`→PID、`$0`→脚本名"反推机制；因此防线放在**门与判据**，而不是指认某个脚本。
- A 类残迹在 `material_judgments.csv` 侧已镜像干净，但 `knowledge-production/` 下的候选层
  （若还有同源副本）未逐文件普查——登记为遗留。

### S-06 · 已判源文件物理隔离：本轮再隔离 1,221 个（累计 2,295）

`make → isolate_processed.sh`（幂等、可 `--undo`、逐文件字节数比对、日志 `processed-isolation-log-2026-09.tsv`）：
本轮清单 1,221 条**全部移动成功**（缺失 0），累计 2,295 个 `state=EXTRACTED` 的源文件进入
`<源根>/zz-已判定隔离/`（镜像路径）。"扫过的不要重复扫"从此仍是物理事实。

## T. 零材料节点补料：CHEMISTRY「自然资源的开发利用」（2026-09-19 夜 · 门 2/22 → 1/22）

### T-01 · 这条缺口此前被判为"块池无干净知识块"，这次复查有了来源

缺料登记册（O 节）原来的结论是"块池只有真题碎片"。这次把块池按**特征词共现**重排
（金属矿物/海水资源/化石能源/综合利用/煤的干馏/石油的分馏…，见
`_find_gap_source.py` 的判据），并顺手做了件此前没做的事：**把 chunk_id 反查回源文件路径**
（`chunk_id` 前 10 位 = `sha256(rel_path)[:10]`，见 `office_extract.py:163`），
于是能看到"哪本书、哪一讲"里出现了本节内容。

结果：本节内容确实散在块池里，但没有一整段干净的（命中的多数是带填空/答案的习题页）。
可用的两处来源：
- 专题04 高效培优讲义的**备考目标原文**：`认识化学在金属矿物等自然资源综合利用和实现物质间
  转化等方面的实际应用`——这正是本节的教学目标表述；
- 知识清单专题01 的**易混变化清单**：煤的干馏/气化/液化、石油的裂化与裂解属化学变化，
  石油的分馏、蒸馏、升华、潮解属物理变化。

### T-02 · 补材料：两条改写材料，各挂一个来源块（不做数字遮掩）

`tools/kb_coverage/add_gap_materials.py`（幂等追加，拒绝含 ASCII 逗号/引号的字段）写入两行判定：

| 材料 | 类型 | 绑定节点 | 来源块 |
|---|---|---|---|
| 自然资源的开发利用：三条主线与基本思路 | CONCEPT_EXPLANATION | 自然资源的开发利用 | `3b8d34b524-002`（专题04 高效培优讲义·学生版） |
| 资源加工工艺中的物理变化与化学变化 | MISCONCEPTION_GUIDE | 同上 | `2b8a636a64-017`（知识清单·专题01 物质及其变化·学生版） |

两条都按 `REVIEWED_SYNTHESIS` 纪律**改写**（不抄原文），`note` 里写明是章级框架材料及其来源。
顺带把 4 行"节点已合并/删除"的失效判定行置为 `SKIP`（materialize 由 4 错 → 0 错）——
它们指向今天被合并掉的 `递推数列`/`原子核`/`定义-由于地球吸引使物体受到的力叫做重力`。

### T-03 · 验证与遗留

- 门 **2/22 → 1/22**：`unbound_points 1 → 0`（该 pin 从"必须非零"移到防回归哨兵）；
- 剩余唯一红项 `unbound_materials 892`：**早期入库批次的材料本身没有绑定**
  （仓库内没有它们的判定来源表，`grep` 只在成品包里能查到 slug）。这不是能靠数字遮的：
  要么按来源表补绑定，要么逐条语义绑定——登记为下一轮工作项；
- 别名 24,702 → 24,704（新材料的标题可用作别名 2 条）、契约镜像 0 问题、
  `:core:data` 450 用例 0 失败（新材料的字段与指纹均过 App 契约）。

## U. 892 条"无主材料"绑定轮（2026-09-19 夜 · 门首次 22/22 全绿）

### U-01 · 问题：892 条材料在包里没有任何绑定

它们全来自 `registry-*` 批次（《狂K》系列、知识清单、讲义…）：材料本身是改写好的知识条目，
但入库时没绑任何知识点。**检索按节点取材料，没绑定就等于这些材料永远用不到**——
门 `unbound_materials` 一直红着（892），此前只当"已登记的量"处理。

### U-02 · 做法：切片 → 30 个子代理语义裁定 → 三重校验写回

- `make_binding_slices.py`：892 条切 30 片，每条附材料全文 + 机械预筛候选；节点表按学科分文件
  （MATH 873 / PHYSICS 1099 / CHEMISTRY 1202 / BIOLOGY 407）；
- 代理回填 `subject,material_slug,node_slug,confidence,evidence`，**宁缺勿错**（可以写 NONE）；
- `apply_unbound_bindings.py` 只做可机械验证的部分：材料存在且确实无绑定（幂等）、节点同科存在、
  一材料一节点（同材料两片给不同节点 → 冲突不写）、只动 `bindings` 字段（其余键指纹比对）。

结果：**890 条绑定**（high 590 / medium 296 / low 4）、代理判 NONE 2 条；
两条 NONE 由主循环补依据后绑定（`unbound_binding_addendum.csv` 记录原因）：
1. `math-mo-zhongguo-shengyu-dingli`（中国剩余定理）——库内惯例是"每个背景定理一个节点"
   （托勒密/月牙/斯特瓦尔特/四平方和同源同系列），故**新建节点** `中国剩余定理` 并绑；来源单元
   `专题12 …（会一题通一类系列）` 登记为 `keep_per_node`，节点级定位落 `数学必修第一册 第三章`；
2. `bio-textbook-biological-weapons-ban`（禁止生物武器）——节点 `转基因生物的安全性` 已经收着
   「生物武器包括致病菌类」「生殖性克隆」「设计试管婴儿」等材料（选必3 安全与伦理的落点），
   按库内既有先例绑。

**质量信号**：复跑错绑嫌疑审计（`audit_bindings_by_alias`，判据"材料标题 == 别的节点名"）
**237 条嫌疑里没有一条来自本批 890 条绑定**（全部是早期批次遗留）。

### U-03 · 顺带修掉一个真 bug：`create_points` 会把已合并的节点复活

新建 `中国剩余定理` 时跑了 `create_points --write`，它一次新增了 **5** 个点——多出的 4 个是今天
已合并掉的 `递推数列`/`数列中的公共项问题`/`原子核`/`定义-由于地球吸引…重力`：
它们仍留在 `new_points_manual.csv` 里，执行器按表重建。
修法：从该表删掉这 4 行（**合并后的节点必须同时从新建表里清掉**），再重放 `merge_points`（点数 3586→3582）。
这条与 M-05(有决定没执行者) 同族：**两张表说的是同一件事（建点与合并），必须一起收敛**。

### U-04 · 验证与门现状（本轮实测）

- **门 22/22 全绿，缺陷归零**（本文件里第一个全绿的轮次）：`unbound_points` 1→0、`unbound_materials` 892→0；
- 别名 24,704 → **25,369**（+665：新绑定材料的标题成为候选别名，全局互斥后入表）；
- `check_pack_contract` 0 问题（主题 398 / 点 3,582）；
- tools 套件 **289 passed / 30,845 subtests**（`unbound_materials` 从"必须非零"列表移入哨兵组）；
- `:core:data` **450 用例 0 失败**（新绑定过 App 契约）。

### U-05 · 遗留（下一轮）

- 错绑嫌疑 237 条（早期批次，含《等角定理》《半角公式》这类"材料标题就是另一个节点名"的强嫌疑）
  待逐条语义裁定与改绑——判据是启发式，须人/代理判断，不自动改；
- `chapter_by_source.csv` 里 `keep_per_node` 类来源单元的定位复核（本次新增 1 条）。

### U-06 · 错绑嫌疑第一刀：MATH 33 条全部改绑（237 → 204）

237 条嫌疑里 MATH 33 条，判据强（材料标题与目标节点**同名**且目标节点边界与该题吻合），
逐条读过材料标题/摘要与两侧节点边界后**全部改绑**（`material_rebind.csv` + `rebind_materials.py`，
沿用 A 轮既有执行器）：如《等角定理》从「空间向量的有关定理」回到「等角定理」、
《辅助角公式》《半角公式》从「二倍角公式」回到各自节点、《分式不等式的解法》从易错点节点回到解法节点。
改绑后门仍全绿、别名数不变（25,369）。

**顺带发现 4 组近重复节点**（改绑时按"标题同名者"落位）：
`倒序相加法` vs `逆序相加法求数列的前n项和`、`三角形解的个数判断` vs `三角形解的个数问题`、
`分式不等式的解法` vs `分式不等式及其解法`、`三角形垂心的向量特征` vs `平面向量与三角形的垂心`。

其中后两组**当场收口**：改绑后它们已无任何材料（门 `unbound_points` 由 0 变 2 暴露出来），
按 `point_merge.csv` 合并进同名节点（点数 3582→3580），`rebuild_aliases` 清掉 2 条幽灵别名，
门回到全绿。前两组（倒序/逆序、垂心向量特征/平面向量与三角形的垂心）两组都还有材料，
合并需语义判断，登记为下一轮。

剩余 204 条（PHYSICS 108 / CHEMISTRY 75 / BIOLOGY 21）需读材料内容逐条裁定，属下一轮。

## V. 旧资料树清空（2026-09-20 · 用户裁定"扫描过的删掉、未扫描的也删掉"）

**裁定**：旧 2026 资料树（`C:\Users\听云\Desktop\知识库原始数据资料`，10,482 个文件）整体清空——
"已经扫描出来内容的不要舍弃"指**内容**（全部在仓内：sidecar 材料 + `extracted_chunks.jsonl`
177,761 块 / 252MB），源文件本身扫描过的直接删（09-20 上午），未扫描的随后追加裁定也删。

**执行**：两批共删 **10,482 个文件 / 65.68 GB**，源树现存文件 0（空目录 2,636 个已清）。
- 批 1（9,679 / 54.80 GB）：EXTRACTED 2,295 + CHUNKED 6,185 + EMPTY 1,139 + 已转写扫描件 60；
  删除前逐文件核过：无缺失、15 分钟内无改动（不碰对方在写的文件）。
- 批 2（803 / 10.88 GB）：REJECTED 392 + SKIPPED_NO_CONTENT 411，全在 inventory 内、无意外文件。
- 审计清单：`knowledge-production/source-deletion-log-2026-09.tsv`（10,482 行，
  rel/state/location/sha16/size/action）。

**状态机终态**（`--verify` 账本完整）：EXTRACTED 2,295 / CHUNKED 6,185（冻结不再读取，
块文本保留可捡回）/ EMPTY 1,139 / SKIPPED_NO_CONTENT 411 / REJECTED 452；ERROR 0。
旧轮次的隔离脚本（`isolate_processed.sh --undo` 等）对已删文件只会记 missing，**不可用于恢复**；
恢复能力只存在于仓内内容（材料 + 块）。

**后果声明**：W-3"块判定主线"正式关闭（详见 `docs/kb-outstanding-work-2026-09-20.md`）；
后续补料唯一新源 = 桌面 `最全知识库原始资料`（2027 版《53知识清单》四科，已登记、未启用，
启用前三道前置：来源登记 / 2019 版次核对 / 状态机入库）。

### U-07 · 错绑嫌疑 204 条（物化生）全部裁定 + 8 组近重复节点合并收口

12 个子代理逐条读"材料内容 vs 两侧节点边界"裁定 204 条（另有 1 片网络失败后重试）：
**REBIND 183 / KEEP 21 / NONE 0**。执行链：`apply_rebind_verdicts.py`（校验：材料存在、**现挂未过期**、
目标同科存在、材料一条只一裁定）→ `material_rebind.py` → `rebind_materials.py --write`。

**KEEP 21 条的性质（不是漏判，是判据边界）**：这些是"两节点同题近重复、只差用字"的情形
（磁感线/磁感线的特点、电磁阻尼和电磁驱动/电磁阻尼与电磁驱动、遗传病的检测和预防/与…、
硒、碲/硒和碲…）。它们的正确处理不是拆材料，而是**合并节点**——于是有了下面一段。

**改绑抽空 6 个节点 → 与兄弟节点成对合并**（门 `unbound_points` 把它们暴露出来）：
库仑力作用下的平衡→…平衡问题、游标卡尺的读数→游标卡尺的构造与读数、小量程电流表-表头→电表的改装、
水电离出的 c(H+)（或 c(OH-)）的计算→水电离出的离子浓度的计算、试纸的使用方法→试纸的类型与使用方法、
装置气密性检查方法→装置气密性的检查方法。加上第 13 片子代理裁定的 2 组
（逆序相加法求数列的前n项和→倒序相加法、三角形垂心的向量特征→平面向量与三角形的垂心），
本批共 **8 组合并**（点数 3,580→3,572）。

**审计闭环**：给 `audit_bindings_by_alias` 加豁免（读 `binding_suspects_reviewed.csv`）——
判过的 KEEP 不再每轮复报。复跑结果：**错绑嫌疑 0 条（豁免 21 条）**。

**验证**：门全绿（点 3,572 / 材料 27,182 / 别名 25,360）；`check_pack_contract` 0 问题；
tools 套件 289 passed / 30,835 subtests。`:core:data` 本轮**未能重跑**（见下）。

**UNVERIFIED**：`:core:data` 450 用例在本轮数据改动后未重跑成功——`:core:domain:compileKotlin`
报重载歧义（`TutorContextComposer.compose(List<TutorMessage>)` vs `(List<TutorChatHistoryEntry>)`），
那是**另一条会话正在改的代码**，与本轮知识库数据改动无关；Python 侧契约镜像（0 问题）是当时的替代证据，
Kotlin 侧待对方修复后补跑（登记为待验证项，不当已通过）。

**顺带登记的重复节点迹象（只报不改）**：四心家族有 5 个近义节点
（平面向量与三角形的四心 / 三角形四心的向量特征 / 三角形四心的向量表示 / 三角形的四心 /
四心与极化恒等式、奔驰定理的速用技巧），下一轮按第 13 片同法裁定。
