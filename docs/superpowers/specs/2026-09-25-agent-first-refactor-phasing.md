# 智能错题本 · 智能体优先重构 — 分阶段划分

- 日期：2026-09-25
- 状态：待用户确认
- 上游：决策台账 `docs/agent-first-refactor-decisions-2026-09-23.md`（D1→D-S 全部裁定）
  与设计规格 `docs/superpowers/specs/2026-09-25-agent-first-refactor-design.md`。
- 原则：**每阶段开始时才用 writing-plans 出该阶段的详细实现计划**（用户裁定：
  一次出全量计划会降低质量）。本文件只做阶段划分与依赖排序。

## 划分原则

1. **先收口未决，再动代码**——插眼清单里还有三件待裁定，未裁定不动对应代码。
2. **依赖驱动排序，不是"由表及里"**——地基（会话模型/统一底层）是一切栏的底座，先做。
3. **每阶段有独立出口门**——出口门过不了，不进入下一阶段。
4. **与另一会话共享工作树**：涉及 Room schema 的阶段必须与之协调（历史教训：
   并发构建污染 schema 导出）；知识库内容生产与本重构尽量错开表结构改动窗口。

## 阶段划分

| 阶段 | 内容 | 依赖 | 出口门 | 主要风险 |
|---|---|---|---|---|
| **0 收口** | ① 插眼 6 研究：掌握库机制外部对标（OATutor-BKT 等），门常数校准依据落 `docs/research/`；② 插眼 1（考前加权改法）与插眼 5（挂起恢复机制）的机制设计 | 无 | 台账无未决项；研究落盘 | 研究走偏（插眼 6 范围失控） |
| **1 地基** | K1 会话事实模型收敛（唯一文本权威/空会话不落库/标题截取/会话区判别列/3 表并入退役）+ D-K4 统一底层（删纸门：契约注册表/manifest 主路径分支/TTL/`prohibitedData`）+ 权限三档落地 + **Q3 首装后台化** + **Q5 死重删除**（visual-ui/visual-runtime/benchmark 模块/Filament/隔离链/窄端口/死类型） | 0 | 旧行全部可读（迁移矩阵绿）；唯一槽不破；契约测试降格为测试锁后全绿；APK 不再含 Filament | Room 迁移；与另一会话 schema 窗口协调 |
| **2 智能体一条交互面** | C1/C2（一个 `TutorConversationViewModel`、一条交互面）+ 致命 6 条（A1 不接管旧会话/A2 停止重试复制/A3 流式全阶段统一/A4 确认落库/A5 输入框永不消失/A6 Route B 不进正文）+ 严重 7 条（B1 灰色小字/B2 真按钮/B3 附图 saveable/B4 无题轮空/B5 删 UI 预算/B6 多轮不重置/B7 标题截取）+ S2 来源标签 + S5 试一句 + 工具面泛化（K2 白名单/本地动作/Codex 呈现） | 1 | A1–A6 缺陷全部关闭；真机走查一条完整会话 | 最大改动面；流式与工具环交互 |
| **3 掌握库** | M1/M5 删 legacy 表与 fixture 系统 + M2 内核侧（hint/retry 通道）+ M3 重放触发（全量）+ M4 证据写通道收敛 + M6 删 V1 调度器 + M7 advisory 一等工具 + K3 生命周期（重拆 hook/冷却占位/留痕） | 0.1、1 | 投影回归全绿；重放实测；门常数有校准依据 | 账本/投影改动，回归面大 |
| **4 错题本与录入** | L1–L7（标题单源/归类收敛到模型建议+知识树选/筛选排序两时间值/录入统一/待处理入录入界面/导出后台任务/删 TRASHED）+ 插眼 7 导出 A4 排版 | 2 | 列表与详情同源；录入一条流；导出不随页丢 | 导出后台化与 SAF/打印 |
| **5 复习栏** | 错题复习卡片流 + 知识点复习卡片流（同栏两入口、会话区入口级）+ 判题限定（未作答只提示/判定后放开）+ 备注回响 S1（披露式）+ M2 UI 侧（提示/重试真实接线） | 3 | 判题限定真机走通；三档"答对"是三个数 | 判题限定与曝光的耦合 |
| **6 学习档案/设置/悬浮球** | 只读学习档案 + 复习历史 + 视觉插眼 4（**先问用户再动**）+ 设置栏整理 + 悬浮球总闸 S3（在跑任务+各栏待确认，无最近动作清单） | 3、5 | 复习历史在位；档案无证据链 | 视觉插眼 4 依赖用户单独裁定 |
| **7 收尾** | spec §5 文档作废清单逐条执行 + 全量门禁（单测/仪器/lint/assemble/真机冒烟）+ **Q4 迁移链重置（已裁定）**——在此定单一基线，所有 schema 改动完成后最后做；旧安装靠"旧归档导入迁移"兜底 | 全部 | 全部门绿；文档世界与代码一致 | 作废清单遗漏 |

## 各阶段内部再分步（到该阶段时执行）

- 阶段 0：研究 → 三件裁定 → 机制设计，三步各自有产出。
- 阶段 2 最重，内部按 2a 状态收敛（C2/B3/B5）→ 2b 致命档（A1–A6）→ 2c 呈现与工具（B1/B2/B4/B6/B7/S2/S5/Codex 形式）分三步走。
- 阶段 3 内部按 M1/M5 删 → M4/M6 收敛 → M2/M3/M7/K3 接线 分三步走。
- 每步结束时跑该模块定向测试 + 全仓编译，不攒到最后。

## 顺序的刚性

- 1 → 2 刚性：智能体交互面重建依赖会话事实模型；颠倒顺序会把"唯一文本权威"和"两条订阅"绑在一起改，质量必降。
- 0.1 → 3 刚性：掌握库机制研究是插眼 6 的硬门槛（用户点名"动工前必须研究"）。
- 3 → 5 刚性：复习栏的判题限定与 hint/retry 接线都骑在掌握库投影上。
- 其余阶段可在其依赖满足后小幅并行（如 4 与 3 的后半可并行），但**每次只并行一个**，共享工作树下不搞多线。
