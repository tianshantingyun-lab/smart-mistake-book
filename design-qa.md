# Design QA

治理边界：本文件是带日期的视觉与交互回归记录，不是 M1–M4 验收报告；当前阶段、实现状态和证据等级见 [`docs/scenario-registry.md`](docs/scenario-registry.md)。本文的 P0/P1/P2 仅表示 QA 缺陷严重度，旧 Phase 0 统一称为 pre-M1。

- 基准画面：`design/screens/review-home.png`、`tutor-chat.png`、`mistake-library.png`、`my-profile.png`；`*-draft.png` 只记录被否决或过渡方案，不参与最终对照。
- 实机环境：Pixel 7 AVD，Android 16 / API 36；本轮原始截图为 `1080 × 2400 px`
- 校准方式：每个页面都把原稿放在左侧、模拟器截图放在右侧，作为同一张图片检查；没有用单张截图代替对照。

## 最终对照

- 复习：`design/qa/final/review-comparison.png`
- 讲题：`design/qa/final/tutor-comparison.png`
- 讲题（最终回归、同视口初始态）：`design/qa/final-live/tutor-comparison-latest.png`
- 错题本：`design/qa/final/library-comparison.png`
- 我的：`design/qa/final/profile-comparison.png`
- 2026-07-21 最新错题本实机：`design/qa/final-live-20260721-postcopy/library.png`
- 2026-07-21 最新录入入口：`design/qa/final-live-20260721-postcopy/capture-entry.png`
- 2026-07-21 校对流程实机（全局学生化措辞收口前一版）：`design/qa/final-live-20260721-postcopy/correction-ready.png`；最终“可编辑题面/题面内容/初步文字识别”文案由后续 `24/24` 设备回归验证。
- 2026-07-21 最新“我的”低数据状态：`design/qa/final-live-20260721-postcopy/profile-final.png`
- 2026-07-22 当前四根页与 API 设置：`design/qa/final-live-current/20260722-004533-975-e4b370be/`
- 2026-07-22 最终讲题交互态：`design/qa/final-live-current/20260722-post-hint/tutor-active-current-green.png`；带固定底栏的完整讲题壳：`design/qa/final-live-current/20260722-post-hint/tutor-saved-shell-current.png`
- 2026-07-22 最终新安装四根页：`design/qa/final-live-current/20260722-final/20260722-123037-165-57afb7d5/`；方案 1 同屏图在其 `comparison-formal/`，既有实机基线同屏图在 `comparison-live-baseline/`
- 2026-07-20 当前三列对照（基准 / strictOffline / localFirst）：`design/qa/room-live/20260720-052621-764-610fa721/visual-comparison/*.png`
- 讲题键盘回归：`design/qa/room-live/20260720-052621-764-610fa721/tutor-ime.png`

## 历史视觉结果（截至 2026-07-21）

以下结果只描述当时构建与当时截图；凡与 2026-07-22 冻结合同冲突的交互或文案均已失效，不能作为当前实现、当前测试或当前视觉通过证据。

- P0：四个根页、底栏与主要 CTA 功能路径无阻断；但本轮一次全新安装冷启动约 34 秒并触发 Android “failed to complete startup” ANR，选择等待后才恢复，已作为真实启动性能阻断项开放，不能用“模型思考中”文案掩盖。
- P1：当时未记录阻塞项。没有内容被底栏遮挡；讲题页首屏曾显示固定四项选择、动态动作和输入框。固定选择不再代表当前讲题合同：当前 GUI 按题目需要动态出现，也可以完全不出现。
- P2：无阻塞项。保留了 Android 系统状态栏/手势区与 Material 图标，因此不会逐像素复制原稿中的装饰性线稿，但色彩、字号层级、分隔、触控尺寸和内容锚点一致。
- 触控：核心操作最小 `48dp`；所有自动化点击均由 UI 树 bounds 计算，未使用截图坐标猜测。
- 当时核心实机路径覆盖固定选项分支、三种追问、保存重建、筛选、搜索、内部 strictOffline 变体、系统照片选择器、系统相机、复习作答与解释。固定问题与三种追问已退出当前合同；`strictOffline` 只用于内部权限/数据边界诊断，不是面向学生的离线智能模式。

## 当前回归（2026-07-22）

- 方案 1 原稿与当前 Compose 截图已在同一视觉输入中逐页复核，而不是分别看单图。复习、讲题、错题本、我的四个根页保留一致的暖白底、深色标题、Jade 主色、细灰边框、大触控区和固定 `复习 / 讲题 / 错题本 / 我的` 底栏；未发现裁切、底栏遮挡、错误字重、错误圆角或异常内外边距。最终新安装截图、UI 树及同屏图位于 `design/qa/final-live-current/20260722-final/20260722-123037-165-57afb7d5/`。
- 错题本相对早期方案稿存在经过确认的产品差异：可见分类只保留 `科目 → 板块 → 知识点`，掌握程度独立；错因、来源、题型不再出现。总目录为空时显示“还没有错题”和拍照/上传首题入口，搜索与分面隐藏；有题但筛选为空时使用独立筛选空状态。当前首屏同时提供单题拍照/上传和多照片批量导入，正常录入不要求学生替识别系统录入。
- 讲题交互态把 `design/screens/tutor-chat.png` 与 `design/qa/final-live-current/20260722-post-hint/tutor-active-current-green.png` 放入同一次原始尺寸视觉检查。当前页只围绕用户给出的题目呈现一个有价值的选择场景，并提供非评分式“我不确定，给我一点提示”和自由追问；未加入额外测评题。首次检查发现该提示入口继承测试主题紫色，已改为 Jade 令牌并重新截图；第二轮未再发现可见配色偏差。讲题根页的固定底栏由 `design/qa/final-live-current/20260722-004533-975-e4b370be/tutor.png` 证实，交互态截图有意只截取讲题内容组件。
- 2026-07-23 基础电路动态图在 API 36 上完成协议、渲染和读屏回归：画面只显示“电路图 / 电阻 / 灯泡 / 电源 / 开关”等课本语言，电阻、断开开关、灯泡、电源与导线没有裁切或重叠；连接点不作为额外可见文字或读屏负担。当前原始截图为 `design/qa/tutor-spatial-circuit-current.png`。该证据只覆盖受限串联电路，不扩大到桥式、多支路电路或实验装置。
- “我的 → 智能能力”在同一视觉输入中与方案 1 设置语言复核。页面明确 BYOK 直连、API Key 仅存本机、合成样例能力测试和发送范围；未显示密钥，也没有把内部任务、队列或校验状态交给学生。
- 全新生产数据库冷启动不自动出现 M1 或其他演示题；复习显示“暂无学习记录”，错题本显示“还没有错题”，未出现虚假计划数、虚假错题或空分类控件。复习页已完成同屏视觉复核，未显示“证据不足”“完全未知”或空薄弱点模块；最终方案 1 对照位于 `design/qa/final-live-current/20260722-final/20260722-123037-165-57afb7d5/comparison-formal/review-formal-current.png`。
- 真实 `ACTIVE` 错题进入复习后，页面逐字展示已保存题面，并只提供“我已独立做完 / 这里还卡住”两个低负担动作；没有虚构答案、另一道题或额外能力测试。“这里还卡住”进入同一题同一修订的讲题；两类自报均不展示正确/错误结论，也不直接改变知识点掌握度。
- 本轮实际刷新生成的 JVM XML 合计 `495/495`，失败、错误、跳过均为 0。双 flavor Lint 与构建共 `449` 个 Gradle 任务成功；11 份 Lint XML 合计 `0 error`、46 条非阻断警告。`app-localFirst-debug.apk` 与 `app-strictOffline-debug.apk` 均由同一最终源码重新生成。Gradle 控制台任务数只用于当次诊断，不作为长期证据；本节以 XML、Lint 报告、APK 和截图文件为准。
- Pixel 7 AVD、Android 16 / API 36 的设备套件全部按模块串行执行：app `localFirst 20/20`、app `strictOffline 20` 项中 `14` 执行通过且 `6` 项按边界跳过、Room `88/88`、`core:data 54/54`、导出 `11/11`、录入 `23/23`、错题本 `21/21`、讲题 `46/46`。XML 汇总为 `283` 项、`277` 执行通过、`6` 跳过、`0` 失败、`0` 错误；跳过项只属于不提供对应语义能力的内部 strictOffline 诊断 flavor，联网主版本无跳过。新增讲题设备回归覆盖精确待执行动作的重建恢复与超长回复可见边界：答案底部未真正进入可见区域时不记账，滚动至完整可见后只幂等记录一次；曝光写入被取消时会移除进程内占位并由重启后的收集器补记。
- 仍未关闭的验证边界是用户真实 Provider 的教学质量/费用/远端幂等、物理设备强杀组合、流式讲题，以及整页多题和跨页判断在真实题图上的质量；本节不把模拟器与合成协议测试扩大解释为这些能力已验收。

## 历史回归（2026-07-21）

本节保留旧证据用于追溯，已被 2026-07-22 冻结合同取代，不能据此判断当前版本。

- 2026-07-21 新题讲解页曾从“等待模型讲解”占位进入固定选择流程；当时的 API 36 结果只证明旧路径。当前合同没有独立的“检查/校准”步骤；只有确实有助于理解当前题时才可给出有意义的当前题内选择，GUI 可省略且不得另出题探测能力。配置/鉴权失败进入设置并回到原请求，发送许可失效只回到本次发送确认。真实 Provider 内容质量仍未验收。

- 真实 Provider 切片新增 5 个网关协议/安全单测后，`core:model 58/58`、`core:data 58/58` 通过；联网与严格离线两个 flavor 均编译。Pixel 7 AVD、Android 16 / API 36 的应用根体验 `10/10` 通过。最终 `core:data:testDebugUnitTest + 双 flavor lint + 双 flavor assemble` 执行 `436` 个 Gradle 任务成功，Lint 无 Error/Fatal；联网版与严格离线版 APK 均重新生成。
- 当时新增“把这张题图交给模型整理？”单次授权：学生在发送前能看到具体 Provider/模型、只发送当前题图、用途以及“不发送其他错题、作答记录或 API Key”，并可选择“允许这一次”或“暂不发送”。当前合同不把暂不发送等同于让学生手动校对；Provider、模型、配置版本或题图改变会使批准失效，失效后只重新确认本次发送。该旧证据不能冒充真实 Provider 的线上视觉或识别质量验收。
- 出站合同回归：`core:data 33/33`、录入 `14/14`、应用根体验 `localFirstDebug 10/10` 均为 0 失败、0 错误、0 跳过；受限资产流会在打开私有图片前重新核对持久授权、Room canonical 记录和 vault 文件哈希。缺失授权时 Gateway 不会收到调用。
- 本轮冷启动真实观察到一次系统 ANR：全新安装后首屏约 34 秒未完成，选择“等待”再约 25 秒恢复。另一次模拟器运行态严重退化使测试 runner 类校验超过 15 分钟；非清空重启后同一录入测试 38 秒通过。这两者不能证明是同一根因，后续需分别建立应用冷启动基准与模拟器健康检查。
- 强制重跑 JVM 测试 `248/248`；Pixel 7 AVD、Android 16 / API 36 上按模块串行重跑 Room `45/45`、`core:data 31/31`、PDF `10/10`、录入 `11/11`、错题本 `3/3`、讲题 `2/2`、应用根体验 `10/10`，设备合计 `102/102`，失败、错误、跳过均为 0。
- `JVM test + 两个 flavor Lint + 两个 flavor assemble` 同轮执行 `460` 个 Gradle 任务并成功；两个 APK 均重新生成。Lint 没有 Error/Fatal。
- 学生化文案修正后又执行录入/错题本单元测试、`lintLocalFirstDebug` 与 `assembleLocalFirstDebug`，`395` 个任务成功；随后串行重跑受影响的录入 `11/11`、错题本 `3/3` 和应用根体验 `10/10`，合计 `24/24`。
- “我的 → 智能能力”去除开发者术语后，`lintLocalFirstDebug + assembleLocalFirstDebug` 再跑 `385` 个任务成功，应用根体验设备测试再次 `10/10`；其中包含 API 配置保存和安全存储反馈路径。
- 独立审查后的全局学生化措辞收口，再跑录入/错题本/我的单元测试、`lintLocalFirstDebug` 与 `assembleLocalFirstDebug` 共 `397` 个任务成功；设备端录入 `11/11`、错题本 `3/3`、应用根体验 `10/10`，合计 `24/24`。第一次应用回归准确暴露一条旧文案断言，修正后完整 `10/10` 重跑通过。
- 旧实机曾走通“系统 Photo Picker 选图 → 私有 canonical 原图 → 结构化候选 → 逐块确认 → 正式提交 → 精确修订详情 → A4 PDF”。“逐块确认”已退出当前合同；当前要求候选可用后直接继续，学生只有发现题面有误时才修改。根任务追加新证据前，旧路径不能证明当前流程已验收。
- 当时把 `design/screens/mistake-library.png` 与 `library.png` 放在同一次视觉输入中复核。旧版“五个可切换维度”已失效；当前可见分类固定为 `科目 → 板块 → 知识点`，掌握程度独立，错因、来源和题型不进入分类。
- 旧错题本首屏曾隐藏未闭环批量入口。当前多照片批量导入，以及 2–30 页 PDF 的本地逐页渲染、逐页落盘、暂停/继续、失败页重试或跳过和待处理恢复均已形成实现切片；单页多题可自动拆分，整卷连续页可在一次明确授权后自动归为同一道题，学生不需要手工合并或重排。真实 Provider 题图质量与强杀组合仍待验收。
- 连续页设备回归使用 4 张独立题图覆盖 3 个相邻边界，只产生 1 次模型请求并在约 0.36 秒内完成确定性测试用例；当前窗口上限为 8 页/7 边界，因此 30 页整卷最多 5 次模型请求。该结果只验证本地编排、事务与请求数量，不替代真实 Provider 的网络耗时和判断准确率验收。
- 旧 Photo Picker 截图包含实现状态与手动校对措辞，已不再作为当前文案基准。正常候选直接继续，发现题面有误时才修改；识别失败不开放人工题面补录，只提供重试、重拍或补拍、换图、稍后继续，并保留原草稿。不得向学生展示 OCR、模型任务或强制数据录入。
- 旧“我的”低数据截图使用“证据不足”，已被“暂无学习记录”取代。智能能力当前合同要求：保存配置只写本机且不联网，能力测试由用户主动点击并只用合成样例，分别说明连接、图片输入和结构化输出；配置变化使旧结果失效。`FLAG_SECURE` 黑屏仍不能作为像素级视觉对照。
- 当前未关闭边界仍包括用户真实 Provider 的线上质量验收、逐请求费用提示、流式模型讲题、自动打卡、图形可信裁切、整页多题/跨页判断质量和完整故障注入。多照片选择、PDF 逐页导入和连续页事务合并不再列为未实现，但不能由此推导任意整卷都已正确拆成独立题目、识别质量或 M1/M2 整体通过。

## 历史回归（2026-07-20）

- JVM 测试结果文件合计 `183/183`；Android 16 上 Room instrumentation `25/25`、`core:data 12/12`（含 bundled 中文 OCR 初始化与候选提升阻断）、录入校对 UI `2/2`；应用根体验 instrumentation 在 `strictOfflineDebug` 与 `localFirstDebug` 各 `8/8`，失败、错误、跳过均为 0。
- `test + 两个 flavor Lint + 两个 flavor assemble` 同轮执行 `433` 个 Gradle 任务并成功；两个 APK 均生成，Lint 无 Error/Fatal。
- 两个 flavor 的冷启动烟测均通过，四个底栏根页都由 UI 树定位并点击到达，未检测到崩溃或 ANR。输出分别位于 `design/qa/room-live/20260720-052507-370-f102ffa6` 与 `design/qa/room-live/20260720-052621-764-610fa721`。
- 当前截图与既有基准以相同 `1080 × 2400` 视口并排复核：四根页的内容锚点、间距、底栏和主要控件一致；`localFirst` 仅在“我的”页按能力合同显示 API 配置文案差异。
- 讲题输入框在系统键盘弹出时保持位于键盘上方；拍题与发送触控区均为 `48dp`。讲题切换 PracticeUnit 时会清空旧题揭示/选择/草稿并拒绝迟到结果，设备测试已验证 A 题解释和请求身份不会泄漏到 B 题。
- 复习选择提交现在以单一 Room 事务同时写入 Attempt 并推进队列；“提交后未点下一题就返回”与跨本地午夜重试都有自动化覆盖。讲题和复习的失败重试会持久化并精确重放首次完整命令，提交状态未知时禁止改选，避免数据库已提交但刷新失败后产生冲突载荷。
- 系统相机与 Photo Picker 输入会先进入应用私有临时缓存，再由 `AndroidCanonicalAssetVault` 做 `20 MB` 输入、`8192 px` 单边、`1600 万` 解码像素和 `24 MB` canonical 输出限制；校正 EXIF 方向后重编码到私有内容寻址文件，PNG 保持无损，正式文件不保留 EXIF。相同像素的并发导入只保留一个 canonical 文件，但各自拥有独立草稿。
- 单图导入后会使用随 APK 打包的 ML Kit 中文+拉丁文字模型生成本机候选修订；每个候选块保留来源区域、模型版本和可用置信度，并保持 `LOCAL_OCR/CANDIDATE`。校对页要求学科、标题、题干、印刷/手写/混合层以及对 OCR 题干候选的显式“逐字核对”完整后才可提交；数据层也会拒绝把未显式确认的 OCR 候选提升为用户修订。
- Room v3 以 append-only `ProblemDraftRevision` 和 CAS 拒绝迟到结果；`CommitProblemDraft` 在单一事务中创建 `Problem`、不可变 `ProblemRevision`、`PracticeUnit`、`ErrorBookEntry`、来源关联和提交回执。未知结果重试复用同一命令，不重复建题；真实 v2→v3 迁移已由导出的 v2 schema 建库验证。
- 这仍只是 M2 的单图印刷文字 OCR 候选切片。裁切/透视/模糊质检、手写/印刷自动分层、公式与图形识别、多题与跨页、模型分类、重复处理选择 UI、FTS 分面、飞行模式全链和全持久点强杀矩阵尚未完成，因此不能由本文件推导为 M1 或 M2 整体通过。

## 当前减负审计（2026-07-25）

- 审计范围：全新安装后的复习、讲题、错题本、“我的”，以及“我的 → 数据与隐私 / 存储与导出”。所有画面来自当前 LocalFirst APK、Pixel 7 API 36 AVD、`1080 × 2400` 视口。
- 对照方法：讲题、错题本和“我的”均把改前截图置于左侧、当前截图置于右侧后再检查；复习、数据与隐私、存储与导出逐页检查当前截图和 UI 树。证据位于 `.artifacts/ux-audit-2026-07-25/`。
- 主要结果：
  - 讲题页保留自由输入、拍题和从错题本选择，删除没有动作价值的常驻状态语；输入框与底栏跨空态保持。
  - 空错题本只保留录入与批量导入两个真实动作，不再显示不可点击的零值待处理行。
  - 新用户“我的”页直接展示学习掌握入口和设置，不再先堆 0 次、0 天、空薄弱点和重复空态。
  - 数据与隐私页只显示本机数据和当前智能服务发送边界；存储页只说明现有本机存储与 PDF 导出，不展示“尚未开放”、路线图或永久禁用按钮。
  - 当前截图未发现底栏遮挡、裁切、异常圆角、错误字重或明显触控区不足。
- 自动化证据：受影响主流程两类设备回归分别为 `14/14` 和 `10/10`，均为 0 skipped、0 failed；App 与 Profile Lint 为 0 error。
- 性能证据：SwiftShader 软件渲染产生的 57.11% 卡顿帧被判定为设备噪声；改用宿主 AMD GPU 后，20 次底栏切换共 595 帧、10 帧卡顿（1.68%）、0 missed vsync，p50/p90/p95/p99 为 18/23/28/34 ms。
- 未关闭风险：这一轮只关闭了 R19 的第一批共性症状；其他二级页仍需继续扫描空回调、永久禁用、路线图文案和零值状态。当前性能仍是 x86_64 Debug 模拟器数据，不替代 ARM 真机和 Release/Profileable 验收。
