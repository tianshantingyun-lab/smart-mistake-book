# 全项目通性问题与根因登记表

本文件不把用户指出的位置当作唯一修改点。每个具体问题都要向上归纳根因，再扫描同一通性在导航、UI、模型、数据、任务、性能和测试中的全部命中点。

状态：

- `已确认`：已有真实代码/界面证据。
- `待全局扫描`：根因成立，但仍需补齐所有命中点。
- `已形成统一约束`：已有跨模块合同和回归测试。

## R01 功能堆叠代替用户任务归属

- **根因：** 按“这个功能在哪里也能放”设计入口，没有为每个学生任务指定唯一主入口。
- **已确认症状：**
  - 复习页出现两个录入错题入口。
  - 讲题页把拍题/选题当成进入聊天的前置条件。
  - “我的”同时承载学习数据和内部知识库状态。
- **可能波及：** 桌面快捷方式、通知深链、空态、详情页次要动作、导出入口、设置内跳转。
- **统一修复：**
  - 每个用户任务只有一个主入口；
  - 跨栏动作只能是明确的上下文跳转；
  - 空态不能把用户带到其他栏的职责；
  - 建立导航职责矩阵和自动化深链测试。
- **归属：** A02、H04、G07、F01。
- **状态：** 已确认，待全局扫描。

## R02 内部系统状态直接暴露给学生

- **根因：** 把开发者需要观察的覆盖率、审校、能力探测、队列和技术状态直接当成用户信息。
- **已确认症状：**
  - “知识点与资料”显示资料数、分类依据、待完善分类。
  - 部分模型可见字段可能通过动态按钮标签泄漏技术词。
- **可能波及：** Provider 能力测试、离线 flavor、数据库整理、导入队列、错误恢复、知识缺口、导出诊断。
- **统一修复：**
  - 内部状态只进入诊断和开发工具；
  - 学生端只呈现会改变下一步的日常语言；
  - 所有模型字段统一经过学生语言边界；
  - 静态资源、语义树和真实 Provider 输出三层扫描。
- **归属：** A03、H01、F08、B03。
- **状态：** 已确认，待全局扫描。

## R03 让学生成为系统的默认校对员

- **根因：** 为降低系统错误风险，把每次确认、OCR 修正和分类维护转嫁给学生，而不是只在无法继续时求助。
- **已确认症状：**
  - 可用题面仍固定停在人工继续/修正页面。
  - 当前知识库页面向学生展示“待完善”，虽然没有可完成的动作。
- **可能波及：** 多图授权、重复题确认、分类纠正、模型设置测试、批量失败处理。
- **统一修复：**
  - 可信结果自动继续；
  - 可逆动作优先“完成后可撤销”，不弹确认；
  - 只有不可逆、高风险或系统无法继续时才请求一次最小输入；
  - 批量任务只汇总真正需要处理的少数失败项。
- **归属：** A03、E02、E03、B03。
- **状态：** 已确认，待全局扫描。

## R04 把 Prompt 当成权限和业务约束

- **根因：** 认为“提示模型不要做”就等同于系统不会发生。
- **已确认症状：**
  - 不得额外出题主要靠 Prompt，解析器仍接受任意诊断题。
  - 模型意图字段可以不当地阻止保存。
  - 动态按钮标签没有完整语言过滤。
- **可能波及：** 记忆写入、知识正式化、错题读取、答案曝光、GUI 命令、网络搜索。
- **统一修复：**
  - 模型输出一律视为不可信建议；
  - Schema、能力令牌、本地策略、事实验证和事务共同执行；
  - 对抗模型输出成为每个能力的必测项。
- **归属：** B03、D03、F03、F05、F08。
- **状态：** 已确认，待全局扫描。

## R05 数据录入事实与学习证据混淆

- **根因：** “用户保存了一道错题”被过早解释为“用户现在不会这个知识点”。
- **已确认风险：**
  - 批量导入可能只是迁移历史错题，却会以当前新题进入复习候选。
  - 普通闲聊、阅读讲解、浏览动画若没有统一事件边界，容易污染掌握投影。
- **更深影响：**
  - 学习掌握页虚假变差；
  - 大批量导入制造复习洪峰；
  - 旧错题被当成今天再次出错；
  - 模型对学生能力形成错误印象。
- **统一修复：**
  - 分离 `录入时间 / 原始错题时间 / 实际作答时间 / 投影时间`；
  - 录入只建立题目事实和待观察关系；
  - 只有可归因的真实作答、选择、答案暴露和复习结果产生能力证据；
  - 导入来源不明时不得推断“近期再次做错”。
- **归属：** D04、D06、G01、E03、H02。
- **状态：** 高风险根因，待全局证据扫描。

## R06 题目中心代替知识中心

- **根因：** 数据结构虽有知识点掌握，但讲题检索和复习调度仍主要围绕单个 `practiceUnitId`。
- **已确认症状：**
  - 新拍题不读取同科全局相关掌握。
  - 已存题只读取本题绑定知识点。
  - 普通导入题使用每题唯一 family，代表题去重失效。
- **统一修复：**
  - 当前题先映射细化知识点、步骤和题族；
  - 讲题读取同科相关知识邻域；
  - 复习债务同时存在知识点、题族和原题三个层级；
  - 原题保留，调度不再逐题机械运行。
- **归属：** D05、E04、G01–G05。
- **状态：** 已确认。

## R07 原始数据量直接变成用户任务量

- **根因：** 缺少“数据池 → 学习债务 → 今日预算”之间的削峰层。
- **已确认症状：**
  - 已有 20 分钟预算，但普通导入题没有真实题族聚类。
  - 同质题可能逐日占据队列，缺少完整防饥饿和单科/知识点软上限证据。
- **统一修复：**
  - 三层模型：错题事实、知识复习债务、今日计划；
  - 时间和题数双预算；
  - 代表题、相似证据封顶、轮换、公平老化；
  - 10–5000 题性质测试。
- **归属：** G01–G07。
- **状态：** 已确认。

## R08 时间语义只有一个“现在”

- **根因：** 捕获、导入、原始错误、作答、复习和投影使用相近时间字段，容易把旧数据当新证据。
- **可能症状：**
  - 历史错题批量导入后全部显示为最近错误；
  - 更换时区/设备时间影响排序和掌握；
  - 修订题面改变历史事实。
- **统一修复：**
  - 明确发生时间、观察时间、录入时间、处理时间和本地日期；
  - 未知原始时间显式为空，不用导入时间代替；
  - 排程和趋势按正确时间语义运行；
  - 时钟回拨、时区和旧数据迁移测试。
- **归属：** D04、D06、G04、H02。
- **状态：** 待全局扫描。

## R09 没有数据被误读为能力差

- **根因：** 完整知识目录与个人学习证据没有严格分离，容易把未接触内容显示成薄弱或零分。
- **已确认相关问题：**
  - 学生端当前看到知识库覆盖，而不是自己的证据覆盖。
  - Profile 使用精确百分比，但缺少完整个人时间线与状态解释。
- **统一修复：**
  - 只对有真实个人证据的知识显示掌握判断；
  - 未学习内容不计入薄弱；
  - 证据不足时用“还没有学习记录”等日常描述，不显示虚假精确百分比；
  - 课程范围和年级只用于筛选，不自动产生负面结论。
- **归属：** H02、H03、D06、C02。
- **状态：** 待全局扫描。

## R10 专用组件增长代替通用表达能力

- **根因：** 按场景类型不断增加独立模型类和渲染器，最终演变成逐题型维护。
- **已确认症状：**
  - 当前 GUI 由多个专用 scene 类型组成，每轮只能一个。
  - 物理运动按四种专用类型解析。
- **统一修复：**
  - 版本化声明式 `VisualCommand`；
  - 通用原语和组合层；
  - 统一动画状态、表达式、单位和时间轴；
  - 新题只增加命令数据；
  - 跨学科“零渲染源码改动”验收。
- **归属：** F05–F07、`dynamic_gui_architecture_plan.md`。
- **状态：** 已确认，已形成架构方向。

## R11 知识内容、评测样本和学生数据边界不够显式

- **根因：** 都与“题目/知识”有关，若只靠目录习惯，未来容易把评测题或示例题带入知识库。
- **风险：**
  - 知识库悄然变题库；
  - 评测数据泄漏导致指标虚高；
  - 学生个人题目进入公共资产；
  - 发布包携带无授权题目。
- **统一修复：**
  - 正式知识包、评测题、个人错题、待审资料物理隔离；
  - 不同构建任务和访问接口；
  - APK/AAB 内容扫描；
  - Schema 允许只服务于概念和方法讲解的例子、推导与答案，但从根上拒绝可布置、可评分、可记录作答或可进入复习调度的题库身份字段。
- **归属：** C01–C06、`knowledge_base_architecture_plan.md`。
- **状态：** 架构风险，已形成隔离方案。

## R12 失败兼容逐渐演变为第二套产品

- **根因：** 为离线或模型失败增加功能分支，长期保留本地 OCR、strict-offline 产品路径和重复行为。
- **已确认症状：**
  - strictOffline 仍是正式 flavor。
  - 本地 OCR 仍作为过渡候选链存在。
- **统一修复：**
  - 离线只保证本地数据查看、任务保留和失败关闭；
  - 不扩展离线智能讲题；
  - 诊断构建不进入学生信息架构；
  - 删除重复语义判断，仅保留文件级校验。
- **归属：** B01、B02、E01、E02。
- **状态：** 已确认。

## R13 缺少统一的资源预算与背压

- **根因：** 单项功能各自处理输入，没有统一限制任务数量、并发、流时长、模型成本、内存和重试。
- **已确认症状：**
  - 图片/PDF Provider 输入流缺少完整时间/进度边界。
  - PDF 特殊 Unicode 可造成数量级性能退化。
  - 大批量模型调用的成本、限流和暂停仍未形成完整产品合同。
- **统一修复：**
  - 文件、网络、模型、数据库、GUI、PDF 和批量任务共享预算模型；
  - 有界队列、有限并发、取消、暂停、恢复、指数退避和总重试预算；
  - 批量开始前只在成本显著时给一次简洁预估；
  - 超限不转嫁成逐项手工操作。
- **归属：** B04–B06、E03、F05、I03。
- **状态：** 已确认，待全局扫描。

## R14 生命周期和版本兼容被当作页面细节

- **根因：** 恢复、幂等、Schema 版本和迁移在各功能内零散实现，没有统一跨任务合同。
- **风险：**
  - 模型任务重放；
  - Provider/配置授权串用；
  - 旧 GUI 会话升级后打不开；
  - 批量导入重复提交；
  - 学习证据重复写入。
- **统一修复：**
  - 所有长任务有稳定 ID、租约、输入指纹、状态机和幂等结果；
  - 模型/GUI/知识包/数据库分别版本化；
  - 旋转、强杀、升级和重复回调成为公共测试套件。
- **归属：** B02、D01、E03、F02、F05、I03。
- **状态：** 待全局扫描。

## R15 可逆动作与不可逆动作使用同样交互成本

- **根因：** 缺少风险分级：一些可撤销的普通动作反复确认，另一些影响学习事实的隐式写入又没有清晰边界。
- **已确认相关问题：**
  - 讲题保存和结束存在多处确认路径。
  - 答案曝光可以隐式进入学习账本。
- **统一修复：**
  - 可逆、低风险动作自动完成并提供撤销；
  - 不可逆、外发、删除或改变长期学习事实才需要明确确认；
  - 确认只出现一次，并准确描述影响范围。
- **归属：** A03、B03、D03、D04。
- **状态：** 待全局扫描。

## R16 用随包样例证明系统质量

- **根因：** 测试数据和实现内容同源，容易只证明“自己能命中自己”。
- **已确认症状：**
  - 当前知识检索回归只有与 19 个随包节点对应的 19 条内联正样本。
  - 动态 GUI 主要由已实现类型的受控场景证明。
- **统一修复：**
  - 独立真实题评测集；
  - 未见样本盲测；
  - 两家真实 Provider；
  - 跨九科、恶意输出和超预算命令；
  - 真机而非只在模拟器证明性能。
- **归属：** C06、I01–I06。
- **状态：** 已确认。

## R17 模型费用和限额不是技术细节

- **根因：** BYOK 下，批量图片、重试和多轮讲题直接消耗学生自己的额度，但当前计划若只关注网络成功会忽略真实负担。
- **风险：**
  - 数百题导入产生不可预期费用；
  - 重试放大成本；
  - 视觉模型不可用时任务反复失败；
  - 为省费转而要求学生手工录入。
- **统一修复：**
  - 批量任务有调用数/图片数/重试预算；
  - 能力探测结果与真实任务一致；
  - 显著批量任务开始前一次简洁预估，可暂停/继续；
  - 失败不自动无限重试，也不退化为强制手工 OCR。
- **归属：** B05、E03、I01。
- **状态：** 深层产品风险，待实现。

## R18 页面主能力随内部状态消失

- **根因：** 页面用多个提前 `return` 替换整页，而不是保持稳定任务骨架并局部降级。
- **已确认症状：**
  - 讲题空态、自适应暂停和能力不可用会换成不同整页，输入框消失。
  - 全局学习数据错误条只说明失败，没有可执行恢复。
  - `SOURCE_UNAVAILABLE` 提示重新拍摄，但恢复页只能返回待处理列表，形成循环。
- **统一修复：**
  - 一级页面的主任务和输入骨架跨状态保持；
  - 错误态必须提供能改变状态的动作；
  - 用状态转换图验证每个空态/错误态存在出口；
  - 禁止只显示系统原因而没有下一步。
- **归属：** A04、A07、F01、E03。
- **状态：** 已确认。

## R19 未实现能力和零值状态占据生产界面

- **根因：** 把路线图、禁用按钮和空工作流当作“完整界面”的一部分。
- **已确认症状：**
  - 讲题历史按钮为空回调且永久禁用。
  - “加密备份 · 即将支持”为空回调禁用按钮。
  - 隐私页罗列多项“尚未开放”能力。
  - 错题本无待处理题时仍保留不可点击入口。
- **统一修复：**
  - 生产界面只展示当前可执行能力；
  - 未来功能不占学生交互位置；
  - 零值统计和空工作流入口默认隐藏；
  - 自动扫描空回调、永久禁用和“即将支持”生产文案。
- **归属：** A08、H04、I06。
- **2026-07-25 首批落地：**
  - 讲题空态删除无动作的常驻状态语，保留自由输入、拍题和从错题本选择；
  - 空错题本不再展示不可点击的“没有待处理题目”，只有确有待处理记录时才出现恢复入口；
  - 新用户“我的”页隐藏 0 次、0 天、空薄弱点和重复空态，只保留可理解的学习掌握入口与真实设置；
  - 数据与隐私、存储与导出页删除“尚未开放”、路线图文案和永久禁用按钮，只展示当前真实数据边界与现有 PDF 导出路径；
  - 上述状态已由两轮 API 36 设备回归和实机截图验证。
- **状态：** 部分实施；全仓空回调、永久禁用、路线图文案和其他零值状态仍需继续扫描。

## R20 可追溯被误解为默认全部展示

- **根因：** 数据库需要保留版本和审计，不代表学生每次都要看到全部轨迹。
- **已确认症状：**
  - 单一题面版本仍固定展示版本区块。
  - 错题详情同时堆题面、原图、版本、导出、讲题、分类、相关题和整理状态。
  - 批量汇总显示“需重试 0 页”“剩余 0 页”。
  - Capture 同屏展示大量本机、外发、阶段、任务和校对状态。
- **统一修复：**
  - 默认只显示当前任务、结果和主动作；
  - 版本、来源、审计和诊断按需展开；
  - 零值不展示；
  - 为每页设首屏信息预算和主要动作数量上限。
- **归属：** A03、A08、E02、H03。
- **状态：** 已确认。

## R21 模型语义自报被升级成学习事实

- **根因：** 结构合法被误当成语义真实，模型字段直接驱动长期状态。
- **已确认症状：**
  - `solutionRevealed` 由模型自报，随后生成 `ANSWER_REVEALED`，改变稳定度、难度和复习时间。
  - 模型 `isCorrect` 被持久化为学生选择正误，污染后续 Tutor 历史。
  - 分类页面称“建议待确认”，生产链却可依据模型自报 confidence 自动落库。
- **统一修复：**
  - 学习事件必须来自本地可验证的渲染/用户动作事实；
  - 无可信答案键时只能保存“模型判断”，不能命名为客观正误；
  - 候选、策略接受和用户确认是三个独立状态；
  - 结构校验与语义事实来源分别测试。
- **归属：** B03、D04、D08、E04、F03。
- **状态：** P0 已确认。

## R22 授权发生在数据披露之后

- **根因：** 同一次外部调用既让模型判定意图，又预先携带可能不需要的学习摘要；模型还同时自报“用户明确请求”和查询词。
- **已确认症状：**
  - `explicitActionRequest`、confidence、capability 和 lookupTerms 都由模型自报，未绑定学生原文证据。
  - intent 字段缺失时默认当前题帮助，而非无权限模糊态。
  - 非讲题意图在分类完成前已携带当前题相关弱项、强项和题目记忆。
  - 读取预算分列表计算，materialization 和最终展示数量不一致。
- **统一修复：**
  - 先做不带敏感学习数据的最小意图判断；
  - 本地从用户原文验证明确动作和检索词；
  - 第二阶段才构造按 purpose 裁剪的数据视图；
  - 缺字段、低置信或协议不符一律无能力；
  - 预算按最终消费者的总字段、总条数计算。
- **归属：** B03、D02、D03、D08。
- **状态：** P0 已确认。

## R23 临时会话、保存题目和学习证据共用生命周期

- **根因：** “不保存题目”“不保留对话”“不形成学习证据”没有独立状态，临时记录可在后续锚定时追溯升级。
- **已确认症状：**
  - 未锚定会话的答案曝光先持久化，建立 anchor 后可批量物化为学习证据。
  - 结束且不保存只废弃 draft，不清理 Tutor task、interaction 或 pending exposure。
  - “禁止长期写入”只是 `rememberSaveable` UI 状态，不是持久领域授权。
- **统一修复：**
  - 分离 `TEMP_SESSION / SAVED_PROBLEM / LEARNING_EVIDENCE`；
  - 每条事件带 retention、consent、purpose 和 provenance；
  - 最终 Repository 写边界验证授权；
  - 长期升级需要明确 consent epoch，不能按 session 追溯批量升级。
- **归属：** D01、D04、D07、D08。
- **状态：** P1 已确认。

## R24 持久化状态不等于可恢复任务

- **根因：** 任务能落库，但没有统一 lease、owner、heartbeat、过期回收和应用级调度器。
- **已确认症状：**
  - 模型任务只有再次执行相同请求时才把 `RUNNING/STREAMING` 恢复为队列。
  - 协程取消不落明确“中断待恢复”状态。
  - 批量导入无条件把所有 `IMPORTING` 改回 `QUEUED`，没有 worker 身份或 lease 年龄。
  - 一致性失败常提示学生重试、重进页面或重新进入本题。
- **统一修复：**
  - 统一持久任务合同：lease owner/expiry、heartbeat、attempt、next eligible time、取消原因、dead-letter；
  - 应用级恢复器，不依赖页面进入；
  - 本地写使用 durable outbox 和自动有界重试；
  - 只有新增权限、成本或用户选择时才打断学生。
- **归属：** B08、E03、F02、I03。
- **状态：** 已确认。

## R25 去重身份层级混淆

- **根因：** 请求幂等、URI、物理内容、规范化题面、语义题目和题族被当成同一个“重复”概念。
- **已确认症状：**
  - 批次指纹只哈希 URI 和顺序。
  - 同内容不同 URI 可建立多份 page 和 draft；资产层虽按 SHA-256 复用文件，却不减少学生待办。
  - 题目合并直到人工提交后才发生，且依赖精确 Markdown/指纹。
  - 普通错题默认每题一个 family。
- **统一修复：**
  - 明确请求幂等键、内容哈希、感知/文本规范化题目键、正式题目 ID、题族 ID；
  - 重复采集记录为同一题的再次出现证据；
  - 在进入学生待办前完成可安全的重复收敛；
  - 近似合并可撤销，不误删原始资产。
- **归属：** E04、E06、G02。
- **状态：** 已确认。

## R26 局部上限代替统一工作量调度

- **根因：** 各模块各有数量/字节限制，但没有贯穿导入、模型、数据库、复习和 UI 的 workload scheduler。
- **已确认症状：**
  - 批量导入取得互斥锁后整批处理，新 job 可能长期排在旧 job 前。
  - 模型任务只合并相同 fingerprint，没有 Provider 级总并发/额度。
  - 复习没有 `lastSelected`、连续落选或 aging debt。
  - PDF 在 `NonCancellable` 中整份渲染；缓存无目录总量配额。
- **统一修复：**
  - admission → staging → decode → model → persistence → UI 共享工作量信封；
  - 时间片、公平 aging、最迟服务时间、Provider 配额、全局并发、磁盘/内存/耗时预算；
  - 批量任务按用户可消化速度逐步释放，不把每页立即变待办。
- **归属：** B07、B09、E03、G05。
- **状态：** 已确认。

## R27 全量快照和内存重建作为默认数据访问

- **根因：** 数据库、仓库和 UI 把全集合作为状态单位，没有分页、增量计数、独立候选查询和覆盖索引。
- **已确认症状：**
  - 错题变化会重建全部快照、排序、复习候选和目录。
  - Library 在内存重建四个 facet 并对完整列表搜索/筛选。
  - 待完成箱、批次观察、学习快照和 full replay 大量全量读取。
  - `batch_import_job.updated_at`、`source_uri + status` 等热查询缺覆盖索引。
- **统一修复：**
  - 数据库游标分页、增量聚合、按需详情、独立复习候选查询、FTS/facet 索引；
  - 每条热查询用真实 `EXPLAIN QUERY PLAN` 验证；
  - 5000 错题、数百草稿、30 页 PDF 和多个并发批次记录 p50/p95、内存和 GC。
- **归属：** B09、H02、G06、I07。
- **状态：** 已确认。

## R28 针对题目或验收样本写特殊分支

- **根因：** 用题号、图片指纹、题面文本、测试名称或固定样本身份选择算法，会让测试通过却无法处理同类真实情况。
- **风险：**
  - 动态 GUI 退化为逐题页面或逐题动画；
  - 知识归因、掌握权重和检索排序只对已知样本有效；
  - 评测集泄漏到生产分支，指标失真且新题静默失败。
- **统一修复：**
  - 生产策略只能依据版本化的结构化能力、证据属性、资源预算和可复用场景合同；
  - 新增原语必须是基础几何能力，或能被至少三个独立场景、两个知识主题复用；
  - 掌握校准只能按预先冻结的跨科时间留出协议整体发布，禁止按题号、题面、图片哈希、Provider 返回样本或单个测试调参；
  - 真实题集与实现、正式知识包物理隔离，并加入生产源码特殊身份扫描和未见样本回归。
- **归属：** A05、A06、C06、D06、F05–F07、I01–I06。
- **状态：** 已形成统一约束；真实独立题集与 ARM 真机验收仍未完成。

## R29 先截断再清洗导致隐藏字符前缀吞掉正文

- **根因：** 输入在完成控制字符/组合字符净化前先按长度截断，隐藏字符可占满预算并让真实文本丢失；空值判断也会把全零宽输入误判为非空。
- **已确认症状：**
  - `"\u200B"` 重复 10 万次再加“函数”的段落，经 `takeText` 后变成空串；
  - `SafeInlineMarkdown.parse/literal/requiresPlainTextFallback`、`RestrictedFormulaText.sanitize/hasUnsupportedCommand`、文档 ID/选项/图注/未知块兜底存在同序问题。
- **可能波及：** 任何带长度上限的模型/OCR/文件文本入口、检索、标题、选项、导出、日志和学生语言过滤。
- **统一修复：**
  - 所有显示文本统一“先清洗再截断”，空值判断基于清洗后文本；
  - `take` 后紧跟 `stripControlCharacters` 的路径全部纳入同一回归测试；
  - 对组合字符与零宽伪装使用完整 NFC/NFKC 后校验。
- **归属：** B06、A05、A06、F08。
- **状态：** 已形成统一约束（`StructuredContent` 系列）；仍需扫描 Provider/OCR/导出等其他入口。

## 每次修复必须执行的“同类问题扫描”

1. 写出具体症状。
2. 追到数据和权限根因。
3. 定义跨模块不变量。
4. 用全文/符号/调用链扫描全部同类命中点。
5. 修复共享合同或公共组件。
6. 为根因写属性测试/对抗测试/导航矩阵。
7. 再回到用户点名页面验证症状消失。
8. 全量回归，确认没有把负担移动到另一个页面。
